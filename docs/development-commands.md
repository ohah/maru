# 개발 명령

Maru 작업에서 사용하는 기본 명령이다.

## 도구

- 도구 설치/선택: `mise install`
- Zig 버전 확인: `zig version`
- mise가 선택한 Zig 확인: `mise current zig`
- Bun 버전 확인: `mise current bun`. `.mise.toml` 이 단일 출처다(현재 1.4.0) — `web/` 과 `packages/` 가 같은 툴체인을 쓴다.

테스트된 Zig 버전은 정확히 `0.16.0`이다(`.mise.toml`, `build.zig.zon`의 `minimum_zig_version`). 0.16 개발 주기에서 `std.Io`(I/O 인터페이스), Writer/Reader, process 진입 API가 크게 바뀌었으므로, 같은 `0.16.0`이라도 다른 스냅샷/커밋에서는 빌드가 깨질 수 있다. 빌드가 std API 불일치로 실패하면 먼저 `mise current zig`로 정확한 버전을 확인한다.

### Zig 0.16 테스트 runner

`zig build test`와 이를 호출하는 `mise run test`는 프로젝트의 `tools/simple_test_runner.zig`를 모든 Zig test artifact에 명시적으로 주입한다. Zig 0.16 기본 Build runner의 `--listen=-` IPC 경로가 일부 macOS 환경에서 stdin `EndOfStream`으로 종료되는 문제를 피하기 위한 것이다.

- runner는 `builtin.test_functions` 전체를 실행하고, 테스트별 testing allocator 누수·error log·실패를 exit code로 반환한다. 예외 하나: `MARU_TEST_SKIP_PREFIX`(CSV, 이름 머리 일치)가 걸린 테스트는 `FILTERED`로 찍고 실행하지 않는다 — `MARU_TEST_KEEP_PREFIX`가 그 예외의 예외다. `MARU_TEST_KEEP_ONLY_PREFIX`(CSV)는 반대로 그것으로 시작하는 테스트만 돌린다 — 루트만 다른 바이너리가 다른 바이너리의 테스트를 통째로 다시 도는 번짐을 자기 몫만 남기고 끊는 용도(예: `client_external_rx_read_test_support`). macOS 전용이고, argv가 아니라 env인 이유는 fixture 판정자들이 `_NSGetArgc()`로 «fixture 게이트인가»를 판단하기 때문(`tools/simple_test_runner.zig` 주석이 단일 출처). 현재 유일한 사용처는 `file explorer` 잡의 app-host-abi 바이너리 — `session_host.*` 판정자는 `session host macOS (Debug)` 잡이 모듈 그래프째 이미 돈다. 따라서 `zig build test`의 의존 artifact 범위, test filter, 실패 판정은 줄지 않는다.
- `MARU_TEST_SHARD="i/n"`(macOS 전용)은 컴파일된 테스트를 **인덱스 mod n**으로 나눠 이 프로세스는 자기 몫만 돌린다. 같은 바이너리를 n번 띄우는 용도라 컴파일은 한 번이고, 러너의 pid 루트 격리가 샤드마다 다른 session-host 네임스페이스를 준다. prefix 필터 뒤에 적용되므로 `kept`·`filtered` 가드와 `--maru-expect-tests`(컴파일 수)는 샤드와 무관하다. 다른 샤드의 몫은 줄을 찍지 않고 끝에 `shard i/n: N tests belong to other shards.`로 요약한다. 샤드가 하나도 안 돌리면 빨개진다. `--maru-expect-passed`와는 함께 쓰지 않는다(그쪽은 한 프로세스가 다 돌았다는 전제). `test-macos-app-host-abi`가 4샤드로 돈다(`build.zig`의 `macos_app_host_abi_shards` — CI 실측 4샤드 172초, 3샤드 249~272초; 부하에 약하던 반복 횟수 대기는 벽시계 마감으로 바꿨다). 샤드 넷은 **run 스텝 하나 안에서** `tools/run-test-shards.sh`가 동시에 띄운다 — Zig 0.16 빌드 러너는 stdio를 물려받는 run 스텝을 stderr 잠금으로 전역 직렬로 돌리므로(run 스텝 넷은 차례로 돈다) 병렬은 스텝 안에서 해야 한다. 각 샤드 줄에는 `[shard i/n] ` 접두가 붙는다.
- 제품 artifact 경로를 전달해야 하는 전용 테스트는 `/usr/bin/env` wrapper로 test binary를 직접 실행하고 exit code `0`을 요구한다. 이 경로도 `enableTestRunnerMode()`를 호출하지 않아 IPC listener를 다시 도입하지 않는다.
- 이 설정은 fuzz server protocol을 대체하지 않는다. Maru의 기본 test graph에는 fuzz test가 없으며, fuzz를 도입하면 server-mode runner 지원을 별도 gate로 추가해야 한다.
- Zig를 올리거나 upstream IPC 경로를 재검증할 때는 custom runner를 제거한 상태의 `zig build test`와 CI를 먼저 green으로 만든 뒤에만 이 우회를 삭제한다.

### 테스트가 끝나지 않을 때 — 하네스가 자식을 못 거두는 형태

**증상**: `zig build test`가 출력 없이 안 끝난다. `ps`로 보면 test 바이너리가 **CPU 0%로 정지**해 있고,
그 자식 셸이 **코어 하나를 100% 태우고** 있다. 부모를 Ctrl-C로 끊으면 자식은 고아로 남아 계속 돈다.

**진단**: 멈춘 pid에 `sample`을 걸면 어느 테스트의 어느 syscall인지 바로 나온다.

```sh
sample <pid> 1 -f /tmp/hang.txt && grep -E "test\.|__wait4|__kevent" /tmp/hang.txt | head
```

**실측(2026-08-26)**: 이 트리에 그렇게 멈춘 test 바이너리가 5개(최고 16시간 30분), 그 자식 스핀 셸이
26개 쌓여 CPU 약 1,020%(16코어 중 6.4코어)를 태우고 있었다. 스택은 전부 같은 곳을 가리켰다 —
`cli.ssh.expectNotifyLifecycle` → `__wait4`.

**원인**: `fork`는 부모의 signal disposition을 자식에게 물려주는데, **셸은 진입 시점에 이미 무시
(`SIG_IGN`) 상태이던 시그널을 `trap`으로 다시 잡지 못한다**(POSIX Shell Command Language §2.11).
그래서 부모가 SIGINT를 무시하고 있으면 자식 셸의 `trap … INT`가 무력화되고, 셸은 신호를 받아도 죽지
않는다 — 하네스의 `while :; do :; done`이 남고 `waitpid(…, 0)`은 영영 안 돌아온다.

**왜 간헐적이고, 왜 CI에서 안 잡혔나.** 갈림은 딱 하나다 — **셸을 띄우는 프로세스의 SIGINT가 무시
상태인가**. 포그라운드로 돌리면 `SIG_DFL`이라 통과하고, 백그라운드 잡(`&`)으로 돌리면 POSIX가
SIGINT/SIGQUIT를 무시로 만들므로 그 경로에서 재현된다. 같은 커밋이 어떤 날은 초록이고 어떤 날은
영영 안 끝나던 이유가 이것이다.

**규율 — 자식 프로세스를 기다리는 테스트 하네스는 둘 다 지킨다.**

1. **exec 직전에 disposition을 기본으로 되돌린다.** 안 되돌리면 자식이 부모의 무시 상태를 물려받는다
   (`src/cli/ssh.zig`의 `restoreDefaultSignals`).
2. **무한 `waitpid(…, 0)`을 쓰지 않는다.** 데드라인을 걸고, 넘기면 자식을 `SIGKILL`로 걷어낸 뒤
   **실패로 보고한다**(`waitpidWithin`·`reapRunaway`). 원인이 무엇이든 **테스트는 끝나야** 무엇이
   틀렸는지 볼 수 있다 — 멈춰 서면 실패조차 못 하고 빌드 전체가 인질이 된다.

**뒤처리**: 이미 쌓인 고아를 걷어내려면 부모가 죽은 스핀 셸을 찾아 죽인다. 부모가 아직 살아 있는
쌍은 **자식만 죽여도 된다** — `waitpid`가 풀려 부모가 스스로 끝난다.

```sh
ps -Ao pid,ppid,command | grep "[b]egin_notify" | awk '{print $1}' | xargs kill -9
```

### 파일 하나만 돌린다 (개발 루프 전용)

방금 고친 파일의 test만 보고 싶을 때 `zig build test` 전체를 돌리지 않는다. **`zig test <파일>`로 그 파일의 test 만 따로 컴파일·실행할 수 있다.**

```sh
zig test src/session/agent_hook_command.zig
```

실측(2026-08-22)이 이 절을 쓰게 만든 이유다. `src/session/agent_hook_command.zig`는 480줄에 test 16개인데 **전용 gate가 없다** — `check-agent-hook-command`는 실제 셸을 때리는 별개 통합 스크립트이지 이 test들을 돌리는 것이 아니다. 그래서 그 16개를 보는 유일한 방법이 전체 실행이었다.

| 방법 | 실측 |
|---|---|
| `zig test src/session/agent_hook_command.zig` | **6.5초**, `All 56 tests passed.` (import한 `agent_hook_event.zig` 포함) |
| `zig build test` | **12분 19초**, `127/127 steps succeeded`(2026-08-26 실측, CPU 111%). 2026-08-22에는 "20분+ 출력 0바이트로 완주 실패"로 적혀 있었는데, **그 완주 실패의 원인은 캐시도 디스크도 아니었다** — 하네스가 자식 셸을 못 거두고 `waitpid`에서 멈춘 것이다(위 [테스트가 끝나지 않을 때](#테스트가-끝나지-않을-때--하네스가-자식을-못-거두는-형태)). 디스크가 찬 것은 그렇게 멈춰 있는 동안의 결과이지 원인이 아니다. 같은 날 그 hang이 살아 있고 그 자식 스핀 셸들이 코어를 태우던 상태에서 잰 값은 **31분 08초**, CPU 53%였다 — 두 값의 차이는 hang 수정과 유령 프로세스 정리가 **함께** 만든 것이고 각각의 몫은 따로 재지 않았다 |

**어디까지 되는가.** 전이적으로 상위 디렉터리(`../`) import이 없어야 한다 — Zig는 root 파일의 디렉터리를 module 경로로 잡으므로 `../`가 하나라도 끼면 `error: import of file outside module path`로 멈춘다. test를 가진 466개 중 직접 `../` import이 없는 후보가 **346개**이고, 그 후보에서 5개마다 하나씩 뽑은 표본 70개를 실제로 컴파일해 보면:

| 구획 | 표본 | 단독 컴파일 가능 |
|---|---:|---:|
| `src/platform/` 밖 | 29 | **26 (90%)** |
| `src/platform/` 아래 | 41 | 17 (41%) |

즉 **`src/platform/` 밖이면 대개 그냥 된다.** `platform/macos/session_host` 계열은 절반 이하이지만, 그쪽은 이미 focused gate가 촘촘하다(`test-session-host-*`). 이 절이 메우는 것은 **gate가 없는 주변부 파일**이다.

⚠️ **gate 증거로 쓰지 않는다.** 이 경로는 위의 `tools/simple_test_runner.zig`가 아니라 Zig 기본 runner로 돌아서 판정 기준(테스트별 누수·error log·exit code)이 같지 않고, 의존 artifact 범위도 다르다. 아래 `--test-filter` 경고와 같은 이유다 — 빠른 확인용이고, PR 증거는 `mise run check`나 해당 focused gate가 만든다.

**테스트 로그는 `grep FAIL`로 판정하지 않는다.** 러너는 크래시를 `FAIL`로 안 찍고 `Segmentation fault`로 찍으며, 그때 **그 바이너리 자신의 요약줄이 없다** — 실제로 그렇게 세그폴트가 한 커밋에 실려 밀렸다(2026-08-23, 편집기 ⌘F 슬라이스). 대신:

```
zig build test > /tmp/t.log 2>&1;  mise run test-verdict /tmp/t.log
```

`tools/test-verdict.sh`가 러너의 비-0 종료 경로 전부(`failed`·`leaked`·`errors were logged`·focused-selection)와 빌드 실패·컴파일 오류·OOM kill을 보고, **진행 번호가 총계에 못 닿은 바이너리**를 따로 짚는다(크래시는 그렇게 드러난다). 실패가 있으면 0이 아닌 코드로 끝나므로 `&&`로 이어 쓸 수 있다.

**`N/N`은 완주일 뿐 통과가 아니다** — 실패한 테스트도 진행 번호를 올린다. 통과 여부는 러너 요약줄(`All N tests passed.` / `N passed; M skipped; K failed.`)이 말하고, 정식 게이트는 `mise run check`다.

## 빌드와 테스트

- signed-upgrade 릴리스 증거 하네스의 canonical v2 writer·UUID/runtime-count 정책과 제품 executable compile만 빠르게 검증(macOS, 서명 아티팩트 불필요): `zig build test-session-host-signed-upgrade-harness`.

- candidate evidence와 네 local attestation bundle의 atomic durable aggregate handoff 및 격리 APFS 실측: `zig build test-session-host-release-adapter-candidate-aggregate-handoff` (Debug·ReleaseFast에서 harness-owned 임시 루트만 사용하며 실제 앱 session-host 상태나 프로세스는 건드리지 않는다).
- baseline evidence와 authored manifest의 atomic stage-3 durable handoff 및 APFS 실측: `zig build test-session-host-release-adapter-candidate-preparation-handoff` (Debug·ReleaseFast에서 harness-owned 임시 루트만 사용한다).
- retained stage-3 preparation을 새 final-address owner가 다시 열어 canonical 의미와 trusted release context를 결속하고 APFS 비용을 재는 집중 gate: `zig build test-session-host-release-adapter-candidate-preparation-reopen` (Debug·ReleaseFast actual filesystem을 사용하되 실제 앱 session-host 상태·GitHub release·자격증명은 건드리지 않는다).
- 재인증한 current draft를 기존 publication graph의 ready draft authority로 채택하는 집중 gate: `zig build test-session-host-release-adapter-github-draft-adoption` (Debug·ReleaseFast에서 role/context/current identity, final-address·alias와 독립 owner 수명을 검증하며 credential·child process·remote mutation은 실행하지 않는다).
- candidate prerequisite부터 baseline·manifest author·durable preparation retained commit까지 한 owner로 닫는 집중 gate: `zig build test-session-host-release-adapter-candidate-stage3-preparation-product` (Debug·ReleaseFast에서 reducer와 concrete production 조립을 검증하고, actual filesystem의 harness-owned canonical evidence/manifest로 durable handoff를 실행한다. ReleaseFast 성공 20회는 local promote·fence·retained close·source cleanup·전체 경계의 median·p95·max, 실패 수, parent FD delta와 residue를 canonical diagnostic JSON으로 남긴다. prerequisite·baseline·manifest 시간이나 GitHub 원격 latency는 이 수치에 포함하지 않으며 실제 앱 session-host 상태·GitHub release·credential을 건드리지 않는다).
- stage-3 product를 exact `prepare-candidate` validator command와 audit-aware sealed driver에 연결하는 집중 gate: `zig build test-session-host-release-adapter-candidate-stage3-preparation-command` (Debug·ReleaseFast에서 closed options/bootstrap/dispatch, exact-one token/product call, local-vs-audit cleanup, closed exit code와 redacted stderr, 실제 최장 argv 수집 상한을 검증한다. harness-owned fixture만 사용하며 실제 앱 session-host 상태나 GitHub release를 건드리지 않는다).
- retained aggregate를 다음 process-equivalent owner가 다시 열고 네 local bundle을 fixed artifact role에 semantic bind하는 집중 gate: `zig build test-session-host-release-adapter-candidate-aggregate-reopen` (Debug·ReleaseFast actual filesystem과 fake verifier를 사용하며 실제 GitHub release·token·앱 session-host 상태를 건드리지 않는다).
- post-publish aggregate cleanup의 crash-recovery 집중 gate: `zig build test-session-host-release-adapter-candidate-aggregate-cleanup-recovery` (Debug·ReleaseFast pure reducer와 harness-owned actual filesystem을 함께 실행한다. intent publication부터 final parent sync까지 20개 checkpoint에서 첫 process를 종료하고 별도 recovery process가 exact tomb suffix를 끝내는지 및 fork→recover→wait median·p95·max를 검증한다. 실제 앱 session-host 저장소·GitHub release·credential은 건드리지 않는다).
- fresh-process current published asset-ID 재인증 집중 gate: `zig build test-session-host-release-adapter-candidate-published-cleanup-authority` (reopened aggregate와 held canonical role-A manifest에서 exact 네 local asset을 유도하고, 같은 deadline 아래 exact published release를 최초·최종 두 번 조회해 immutable lifecycle과 네 asset ID/name/size/SHA를 감싼 뒤 기존 tag-chain→release/네 asset attestation으로 sealed `VerifiedRelease`를 만든다. Debug·ReleaseFast에서 remote/local/CLI drift와 모든 실패의 publication·cleanup mutation 0을 고정하고, ReleaseFast 20회 injected aggregate source부터 재인증 reducer 전체의 median·p95·max, external-call count, failure count와 FD delta를 출력한다. actual filesystem reopen 비용과 실제 GitHub network latency나 live U5 성공을 뜻하지 않으며 앱 session-host 저장소·GitHub release·credential은 건드리지 않는다).
- stage-8 executable aggregate cleanup 집중 gate: `zig build test-session-host-release-adapter-candidate-published-cleanup-command` (token read 전 durable state를 initial/recoverable/audit로 삼분하고, recoverable intent/completion은 credential·GitHub·제품 ReopenedAggregate 없이 복구하며 initial만 current published asset을 단일 deadline으로 재인증해 crash-recoverable cleanup을 시작한다. Debug·ReleaseFast에서 closed command/outcome, exact ordering, owner/alias/cleanup과 두 fetch·attestation·cleanup의 injected timing JSON을 검증한다. harness-owned private root만 사용하므로 실제 앱 session-host 상태와 GitHub release/credential을 건드리지 않으며 실제 live 표본은 frozen signed U5 gate가 별도로 소유한다).
- release validator를 실제 별도 prepare/finalize process로 실행하고 경계 시간을 재는 집중 gate: `zig build test-session-host-release-adapter-candidate-aggregate-process` (harness-owned 임시 root와 test verifier만 사용한다. prepare reap 뒤 finalize spawn, 서로 다른 PID, token lookup 0, fixed semantic order, failure 뒤 durable-byte 정책을 검증하고 ReleaseFast 20회 median·p95·max 진단 JSON을 출력한다. 실제 GitHub/Apple release와 앱 session-host 상태는 건드리지 않는다).

- 영속 세션 호스트 P5c3c-3a1 TTY output/chord/deadline 집중 gate: `zig build test-session-host-3a1` (P5c3c-2b3을 상속하고 OS-neutral detach chord·stdout progress와 macOS 전용 final-address `DedicatedOutput`을 각각 Debug·ReleaseFast 3개, boundary 1개와 executable test-name sentinel로 검증한다. 전용 output은 `ttyname_r(stdout)` 경로를 `O_WRONLY|O_NOCTTY|O_CLOEXEC|O_NONBLOCK|O_NOFOLLOW`로 다시 열고 open 전후 stdout 및 새 fd의 character-device/`st_rdev`, inherited stdout status flags 불변을 확인한다. path/flags/세 fstat/open/identity 실패 주입은 publication 0과 새 fd exact close를, 실제 `openpty`는 별도 open-file-description·nonblocking·close-on-exec·stdout flags 불변·전용 fd만 close를 고정한다. 이 gate의 product caller는 exact 0이며 pre-raw owner와 raw commit은 3a2 전에는 구현됐다고 보지 않는다.)
- 영속 세션 호스트 P5c3c-3a2 pre-raw commit barrier 집중 gate: `zig build test-session-host-3a2` (3a1과 P5c3c-2b3을 상속하고 prepared TTY inspection 1개, final-address `PreRawOwner` composition 7개, imported drift regression 1개를 Debug·ReleaseFast로 실행하며 boundary 1개와 executable test-name sentinel을 고정한다. 실제 `openpty`에서 prepare 전후 termios·ANSI mutation 0, raw enter/leave exact bytes, stdin/socket/output/signal 4-entry poll set, leave 뒤 dedicated output 선-close와 5초 child deadline 안의 `TCSAFLUSH` return, byte-for-byte restore, pre-commit size drift, partial enter write rollback의 재-commit 거부, repaint allocation failure의 consumed-source/owner cleanup과 prepare/commit teardown 재진입의 typed busy 보존을 검증한다. `ExternalPumpOwner`의 retryable teardown은 busy에서 cleanup authority를 보존한다. 3b가 integrated CLI loop를 연결하기 전까지 `PreRawOwner`의 제품 caller는 exact 0이다.)
- 영속 세션 호스트 P5c3c-3b integrated external attach loop 집중 gate: `zig build test-session-host-3b` (3a2를 상속하고 final-address `IntegratedStackOwner`, 단일 제품 CLI caller와 raw/ANSI/resize/signal loop를 Debug·ReleaseFast로 검증한다. 경계 스캐너가 exact source를 읽는 상한은 저장소의 다른 제품 소스 스캐너와 같은 8 MiB이며, 이는 제품 파일 크기 제한이나 메모리 예산이 아니라 성장한 `src/main.zig`를 의미 검사 전에 잘라 내지 않기 위한 테스트 하네스 예산이다. cap을 넘는 입력은 계속 `StreamTooLong`으로 fail-close한다.)

- 파일 패널 웹 의존성 설치: `bun install --cwd web --frozen-lockfile`
- 파일 패널 zntc 번들+SRI+production third-party notice 검증: `mise run web:build`
- 파일 패널 renderer/sanitizer 테스트: `mise run web:test`
- 파일 패널 lint/format/license/전체 게이트: `mise run web:lint`, `mise run web:fmt-check`, `mise run web:licenses`, `mise run web:check` (기존 Zig `mise run check`와 분리된 opt-in 경로)
- 빌드: `mise run build`
- headless PTY 데모 실행: `mise run demo`
- app host smoke 실행: `mise run app-smoke` (`zig-out/maru-app-smoke/app-host.summary.txt`, `app-host.draw-list.txt`, `app-host.glyph-frame.txt`를 남긴다. 아직 실제 UI는 아니다)
- app frame loop smoke 실행: `mise run app-loop-smoke` (`zig-out/maru-app-loop-smoke/app-loop.summary.txt`, `app-loop.frames.txt`, `app-loop.screen.txt`를 남긴다. 실제 AppKit event loop는 아니지만, output frame, idle frame, termination frame을 반복 tick으로 만들 수 있는지 검증한다)
- live PTY app frame loop smoke 실행: `mise run app-pty-loop-smoke` (`zig-out/maru-app-pty-loop-smoke/app-pty-loop.summary.txt`, `app-pty-loop.frames.txt`, `app-pty-loop.raw.txt`, `app-pty-loop.screen.txt`, `app-pty-loop.snapshot.txt`를 남긴다. 실제 PTY reader thread와 반복 `FrameLoop`를 함께 검증하지만 아직 실제 UI는 아니다. PTY event drain은 기본 5000ms deadline을 갖고, summary에 `drain_timeout_ms`를 남긴다)
- interactive shell app frame loop smoke 실행: `mise run app-pty-interactive-loop-smoke` (`zig-out/maru-app-pty-interactive-loop-smoke/app-pty-loop.summary.txt`, `app-pty-loop.frames.txt`, `app-pty-loop.raw.txt`, `app-pty-loop.screen.txt`, `app-pty-loop.snapshot.txt`를 남긴다. `$MARU_INTERACTIVE_SHELL` 또는 `$SHELL -i`를 실행하고, `FrameLoop.handleKeyEvent -> KeyBindingResolver -> SurfaceRuntime.writeInput` 경계로 marker command를 보내 반복 frame artifact까지 확인한다. 사용자 dotfile/prompt escape 영향을 받으므로 기본 `check`에는 넣지 않는다. PTY event drain은 기본 5000ms deadline을 갖고, summary에 `drain_timeout_ms`를 남긴다)
- live PTY app host smoke 실행: `mise run app-pty-smoke` (`zig-out/maru-app-pty-smoke/app-pty.summary.txt`, `app-pty.raw.txt`, `app-pty.screen.txt`, `app-pty.snapshot.txt`, `app-pty.frame.txt`를 남긴다. 실제 PTY output이 app host renderer frame까지 들어가는지 검증하지만 아직 실제 UI는 아니다. PTY event drain은 기본 5000ms deadline을 갖고, summary에 `drain_timeout_ms`를 남긴다)
- macOS visible window smoke 실행: `mise run macos-window-smoke` (창이 너무 빨리 닫히면 `MARU_WINDOW_SMOKE_MS`로 노출 시간을 ms 단위로 늘려 수동 확인한다. 기본 1500ms, 상한 600000ms)
- macOS window smoke 계약 테스트: `mise run test-macos-window-smoke`
- macOS Metal 제품 atlas shader sampling smoke 실행: `mise run macos-metal-smoke` (창이 너무 빨리 닫히면 `MARU_METAL_SMOKE_MS`로 노출 시간을 ms 단위로 늘려 수동 확인한다. 기본 1500ms, 상한 600000ms)
- macOS Metal smoke 계약 테스트: `mise run test-macos-metal-smoke`
- macOS Chrome Lab Metal readback smoke 실행: `mise run macos-chrome-lab-smoke` (`empty`·`loading`·`retained-list`을 각각 test-only process로 실행해 `zig-out/maru-macos-chrome-lab/<scenario>.{ppm,png,json}`을 남긴다. 제품 `maru_metal_renderer_draw`의 오프스크린 readback을 쓰며, `text_rasterized=false`인 현재 slice는 rounded card/shadow·geometry만 검증한다)
- macOS Chrome Lab readback 계약 테스트: `mise run test-macos-chrome-lab-smoke`
- 도크 시각 골든: **`mise run macos-dock-visual-golden` 한 줄로 돌린다**(캡처를 다시 만든 다음 비교한다).
  `zig build test-dock-visual-golden`만 따로 돌리면 **디스크에 남아 있는 옛 캡처와 비교돼 조용히 통과한다** —
  코드를 고치기 **전에** 스모크를 돌려 뒀다면 그 캡처가 곧 옛것이다(2026-08-19에 그렇게 통과시켰다가 CI가
  `scm-list-rows`에서 잡았다. 그전에 편집기 세션도 같은 함정에 한 번 걸렸다). 골든을 갱신할 때는
  `MARU_UPDATE_GOLDEN=1 zig build test-dock-visual-golden`으로 다시 만들고 **눈으로 확인한 뒤** 커밋한다.
- macOS live PTY Metal smoke 실행: `mise run macos-app-pty-metal-smoke` (controlled PTY command output과 AppKit synthetic `keyDown:`에서 얻은 key event가 app host keybinding resolver를 통과하는 roundtrip을 실제 AppKit/CAMetalLayer 창의 CoreText glyph atlas shader sampling까지 태우고 `zig-out/maru-macos-app-pty-metal-smoke/app-pty-metal-frame.ppm` screenshot artifact를 남긴다. 마지막 visible frame 뒤에는 같은 Metal terminal window의 AppKit close delegate가 Zig callback을 통해 `FrameLoop.closeActiveLivePty -> LivePtyRegistry.closeActive -> LivePtySession.closeAndDetach` 경로를 호출하고, `terminal_close_*`, window close, registry mapping 제거 gate도 summary에 남긴다. PTY ready marker와 종료 drain은 기본 5000ms deadline을 갖고, summary에 `drain_timeout_ms`를 남긴다. 창이 너무 빨리 닫히면 `MARU_APP_PTY_METAL_SMOKE_MS`로 조절한다. 이 controlled smoke 자체는 실제 shell startup이나 제품 tab/window close button 검증이 아니다)
- macOS interactive shell live PTY Metal smoke 실행: `mise run macos-app-pty-interactive-metal-smoke` (`$MARU_INTERACTIVE_SHELL` 또는 `$SHELL -i`를 실제 AppKit/CAMetalLayer 창 경로에 태우고, AppKit synthetic `Cmd+B`에서 얻은 key event가 app host keybinding resolver를 통과해 marker command와 `exit`를 shell에 보내는지 확인한다. `zig-out/maru-macos-app-pty-interactive-metal-smoke/` 아래 raw/screen/snapshot/screenshot artifact를 남긴다. prompt/dotfile 영향을 받는 실제 shell을 visible renderer에 연결하지만, 아직 사용자가 계속 타이핑하는 제품 event loop는 아니다)
- macOS live PTY Metal manual keyDown smoke 실행: `MARU_APP_PTY_METAL_KEYDOWN_SOURCE=manual MARU_APP_PTY_METAL_KEYDOWN_MS=15000 mise run macos-app-pty-metal-smoke` (Metal terminal window가 뜨면 사용자가 `Cmd+B`를 직접 누른다. 이 경로는 물리 키보드 -> 같은 Metal terminal window의 AppKit `keyDown:` -> Zig `terminal.KeyEvent` -> app host resolver -> PTY write -> Metal screenshot까지 확인하지만, 여전히 한 번의 smoke이고 지속 실행되는 제품 shell loop는 아니다)
- macOS live PTY Metal smoke 계약 테스트: `mise run test-macos-app-pty-metal-smoke`
- macOS agent-session archive AppKit smoke 실행: `mise run macos-agent-session-archive-smoke` (격리 HOME의 synthetic Codex/Claude JSONL로 실제 titlebar/dock/card pointer와 loading→ready, detail `resume`·`로그 보기`의 pointer/`⌘↵`·`⌘L`을 각각 새 AppKit process에서 확인한다. Codex resume은 절대 경로 fake `codex`의 `codex resume <id>` direct argv verdict만, Claude model scenario는 직속 transcript의 `message.model`과 fake `claude`의 `claude --resume <id>` verdict만 남긴다. reveal은 같은 external-open consumer의 allowlisted count만 남기며 실제 provider/Finder는 실행하지 않는다. `detail-stale` process는 loading paint 뒤 source를 atomic replace해 disabled stale detail을 확인하고, `reveal-recheck-pointer`는 ready 뒤 source를 replace해 external-open count=0과 `StaleArchiveSource` admission count=1을 확인한다. summary는 `zig-out/maru-agent-session-archive-smoke/<scenario>.summary.txt`에 남는다.)
- macOS Swift/Zig app host ABI 계약 테스트: `mise run test-macos-app-host-abi` (Swift 제품 app host가 호출할 C ABI version, ownership capability, key/resize DTO layout을 Zig와 C header 양쪽에서 검증한다. aggregate에서는 process-global signal/seal/daemon namespace와 CoreText cache 행을 제외하고, 같은 명령이 실제 TTY exact 8 → external attach exact 1 → attach resolver exact 1 → generation event release exact 5 → CoreText import sentinel+cache reuse/eviction exact 2를 fresh process로 직렬 실행한다. CR6a-2 real-host launch 행은 `zig build test-session-host-cr6a2` 전용 artifact가 소유한다.)
- provider session continuity 제거 gate: `mise run test-provider-session-removal` (macOS 제품 AppSession 경로에서 provider config/mapping 무변경, **상태줄 훅 제거**(감쌌던 원본 복원·우리 파일 회수·남의 `statusLine`은 그대로), provider 훅 설치·제거와 codex 신뢰 기록, legacy 2-window parse/apply/write-new, interactive login-shell 복원 요청, ABI window count/active/apply를 무인 검증한다)
- 영속 세션 호스트 codec/서버 테스트: `zig build test-session-host` (persistent-session P3/P5 — MRSH 32-byte header/major 검증, partial I/O frame parser, kind별 payload cap·`maru.screen-stream.v1` snapshot/delta codec, `TerminalRuntimeRegistry` controller/observer와 prepared takeover/release state machine, 서로 다른 fd의 같은 local stream 1을 global subscription으로 구분하는 generation-CAS `controller.status/takeover/release` 및 revocation/response atomic control batch, old/new input 권위 전환·release·EOF/ABA/upgrade drain, exact-stream notification auth와 response-admission 뒤 generation-CAS consume, strict `runtime.spawn_full` dispatch, 실 unix socket/독립 `maru-sessiond` process smoke와 실제 빌드된 `maru __session-host <socket>` 제품 launch argv→hello→`host.info` 왕복, U0 inventory와 U1~U5 component seam을 검증한다. 이 명령의 green만으로 frozen release provenance·제품 rollback·multi-runtime exact reattach·앱 재실행을 포함한 U5 release gate 완료를 주장하지 않는다. 제품 launch smoke는 전용 test marker가 있으면 필수이며 artifact wiring이 빠져도 skip하지 않는다. 실 socket/fork/제품 exec는 macOS opt-in이며 기본 `test`에도 편입)
- 영속 세션 호스트 F3b 집중 gate: `zig build test-session-host-f3b` (compile-time `f3b` filter와 실제 Darwin socketpair→public `pumpRxTurn` cleanup replay sentinel을 사용해 revoke·whole-turn integration 회귀만 빠르게 실행한다. 이 focused gate만으로 F3 전체 완료를 주장하지 않는다. `zig build test-session-host -- --test-filter f3b`는 Zig compile-time test filter가 아니며 테스트 0개 성공을 오인할 수 있으므로 증거로 사용하지 않는다)
- 영속 세션 호스트 F3c0 집중 gate: `zig build test-session-host-f3c0` (`control_response_wire` request/response codec, `RemoteRuntime` request/decode parity, external pump typed admission·allocation fail-index·expectation tamper를 각각 compile-time `f3c0` filter로 실행하고 component별 executable sentinel이 zero-test green을 막는다. permit 발급·semantic success·recovery mutation은 의도적으로 포함하지 않으므로 이 gate만으로 F3c나 2b2 완료를 주장하지 않는다)
- 영속 세션 호스트 F3c1 집중 gate: `zig build test-session-host-f3c1` (exact completed-owner 예외를 가진 clean drain evidence, final-address permit/verdict preparation, immutable terminal evidence→binding/take/frozen destination seal과 private publish-before-free exact cleanup을 compile-time `f3c1` filter 및 executable sentinel로 검증한다. typed-success semantic consume·recovery mutation·제품 pump 호출은 후속 f3c2/2b2e-integration/f3d 범위이므로 이 gate만으로 F3c나 2b2 완료를 주장하지 않는다)
- 영속 세션 호스트 recovery contract 집중 gate: `zig build test-session-host-recovery-contract` (control request의 pre-token `RecoveryControlAuthority`, ACK 뒤 barrier-only `awaiting_snapshot`, barrier 이후 snapshot candidate DTO의 origin/epoch/range/nonzero committed-generation을 검사하는 pure `snapshot_in_flight` binding plan을 component filter와 executable sentinel로 검증한다. completed-aware whole-drain permit 발급은 f3c1-base가 소유하며, 실제 ledger commit receipt·storage seal·product callsite와 same-lease semantic commit은 후속 2b2e-integration/f3c2/f3d 범위다)
- 영속 세션 호스트 2b2e integration 집중 gate: `zig build test-session-host-2b2e-integration` (기존 recovery reducer의 exact `resync_ack` ACK transaction과 별도 snapshot transaction을 검증한다. snapshot source는 `commit_pending|already_committed` closed union이며 ledger actual-token final-address output·one-shot claim, first-exact bind/나머지 cleanup, barrier 전·걸침·후 disposition, committed binding의 awaiting/ledger/source/ACK provenance, copy·cross-storage·cross-lease·claim-owner·slot-generation ABA·replay/tamper, callback storage descriptor bounded-quarantine와 teardown final-zero를 component filter 및 executable sentinel로 고정한다. 이 gate는 private 2b2e consumer seam의 완료 증거이며 completed payload callback tail, bounded post-response side-intent 배열, 실제 ACK→side-intent→binding 제품 orchestration은 후속 f3c2/f3d 범위다.)
- 영속 세션 호스트 F3c2 집중 gate: `zig build test-session-host-f3c2` (final-address tagged semantic take가 resize/resync branch, ACK 전 projected-state side-intent preflight, actual-token binding, callback-free publication과 publish-before-free cleanup을 all-or-none으로 닫는지 검증한다. executable sentinel이 exact named behavior, 네 private entrypoint의 제품 callsite 0, first-write 이후 projected/retained aggregate/combined intent cleanup의 raw lookup·validator·fallible operation 0을 고정한다. Debug/ReleaseFast에서 changed true/false와 generation order/equivocation, candidate 0/1/64, copy/move/cross-owner/splice/replay, 실제 duplicate response의 protocol-terminal+final-zero, 실제 max request-ID 성공 뒤 peer/TX/correlation mutation 0 exhaustion, callback reentry와 payload·allocator `0x1` drift를 검증한다. 실제 `pumpRxTurn` branch 선택과 side-intent 제품 생산은 f3d 범위다.)
- 영속 세션 호스트 F3d 집중 gate: `zig build test-session-host-f3d` (기존 `pumpRxTurn`의 held whole-turn lease 안에서 completed-control evidence를 exact once resize/resync/terminal semantic consumer로 연결한다. resize는 canonical full-state를 게시하고, resync ACK는 `awaiting_snapshot` barrier를 게시하며, malformed response는 기존 terminal binding/cleanup으로 수렴한다. executable sentinel은 제품 orchestration callsite exact 1과 해당 구간의 새 clock/allocator/socket/lease 0을 고정하고, filtered tests는 세 제품 경로, response cleanup callback의 owner drift→invariant terminal+quarantine, authenticated destroyed-intent tombstone의 exact reset 및 forged field/digest/address 거부를 Debug/ReleaseFast로 검증한다. hostile socket 순열·allocation fail-index·stress는 f3e 범위이므로 이 gate만으로 F3나 2b2 완료를 주장하지 않는다.)
- 영속 세션 호스트 F3e 집중 gate: `zig build test-session-host-f3e` (pure 전수표 1개와 제품 5개를 exact-count한다. response↔revoke/HUP·control `none|queued|partial|fully_sent|response_wait|missing|invalid`·deadline 경계를 먼저 닫고, injected revoke 1/64/65·parser resident 65·readable+writable write 0·EINTR/EAGAIN·input/control offset 0/partial·input retired/control response-wait, 실제 Darwin socketpair의 response/FIN·partial frame·drip 순열, allocation fail-index와 bounded stress의 common TX/parser/ledger/completed/FD final-zero를 검증한다. 실제 socketpair가 skip되는 플랫폼이나 0-test filter는 완료 증거가 아니며, 이 gate만으로 stable owner binding 2b3을 주장하지 않는다.)
- 영속 세션 호스트 P5c3c-2b3 집중 gate: `zig build test-session-host-2b3`과 `zig build -Doptimize=ReleaseFast test-session-host-2b3` (최종 주소 owner 이동 거부, `Prepared` 일회성 consume과 stable adapter bind, adoption allocation callback의 source prebind TOCTOU typed reject, attachment-held charged lease의 storage teardown 차단, connection-fatal latch 뒤 attachment-first release→storage teardown, 실제 `external_attach.prepare`→one pump/control/teardown, parser partial·request ID·capability byte-exact 보존, owner init 전체 allocation fail-index의 source-or-owner exact cleanup, 실제 socket revoke와 control absolute timeout fail-close, attachment queue append·screen apply OOM의 stable charged release를 9개로 exact-count하고 executable sentinel로 0-test 통과를 막는다.)
- 영속 세션 호스트 2c3c C1 집중 gate: `zig build test-session-host-2c3c-c1` (raw extern `RuntimeControl`과 nested core-command tag의 0...255 raw-first decode, exact `ControlError`/facade signature, canonical capability·stream injection, 모든 core-command 최대 payload의 bounded encoder 적합성, dedicated scroll/core wire kind, lifecycle·role·thread·fork·stale·revoke authority와 allocator callback reentry permit, C1 product-callsite 0을 Debug·ReleaseFast runtime 7+boundary 1 exact-count로 검증한다. queue 배선 완료 증거는 C2/C3 gate가 소유한다.)
- 영속 세션 호스트 2c3d C1 집중 gate: `zig build test-session-host-2c3d-c1` (accepted/unknown admission·allocator seal, byte-canonical opaque `EventOwner`, binding별 monotonic generation과 same-address ABA, `.event` short permit, ordinary/idle/`ended_pending` take 및 제품 callsite 0을 Debug·ReleaseFast runtime 11+boundary 1 exact-count로 검증한다. release·quarantine은 C2, 제품 drain·actual socket은 C3 gate가 소유한다.)
- 영속 세션 호스트 2c3d C2 집중 gate: `zig build test-session-host-2c3d-c2` (public `releaseEvent`, cleanup pin, post-poison exact free, callback all-mutex release/reentry closure, 4,096-slot·1 GiB trusted quarantine와 corrupt handoff를 C2-exclusive runtime+boundary exact-count로 검증한다. 제품 `GenerationAttachment` drain·actual socket·source-zero는 C3 gate가 소유한다.)
- 영속 세션 호스트 2c3d C3-1 집중 gate: `zig build test-session-host-2c3d-c3-1` (`GenerationAttachment` inline owner·비권위 generation mirror·construction reserve와 teardown `Busy -> explicit release -> success`, zero/nonzero mirror drift의 canonical cleanup 우선, copy/foreign-thread mutation 0, release callback 재진입 Busy, stream-operation identity 소진의 trusted no-free 수렴, reserve 실패 rollback, same-address ABA current no-free handoff 및 owner-range/2 MiB aggregate budget을 Debug·ReleaseFast runtime 7+boundary 1 exact-count로 검증한다. 제품 purge-first drain은 C3-2, actual socket/source-zero는 C3-3이 소유한다.)
- 영속 세션 호스트 2c3d C3-2 집중 gate: `zig build test-session-host-2c3d-c3-2` (C3-1 전체를 상속하고 무인자 generation ended purge facade, release-pending→purge-first 제품 drain, `.ended_pending` exact budget 재시도, ended 우선 short-circuit, ordinary resized 결과와 injected failed outcome의 settlement 전 보존, deterministic release-result Busy seam의 다음-tick exact-owner 재시도, sibling/legacy 보존 및 generation→legacy fallback 0을 Debug·ReleaseFast attachment runtime 8+actual generation `RemoteRuntime` product drain 1+boundary 1 exact-count로 검증한다. 이 fault seam은 실제 다중 consumer E2E가 아니며 actual socket event의 revoked→fence→release와 generation raw Client event source-zero는 C3-3이 소유한다.)
- 영속 세션 호스트 2c3d C3-3 confirmed poison 집중 gate: `zig build test-session-host-2c3d-c3-3` (C3-2 전체를 상속하고 registered ClientSlot/node owner-thread admission, blocking deferred fd-open 수렴, external typed invalid/canonical teardown Busy의 durable mutation 0, partial pending exact free 1, allocator callback poison/input/control 재진입 Busy, first-reason 보존, `fd == -1`, peer EOF와 idempotency를 Debug·ReleaseFast runtime 1+boundary 1 exact-count로 검증한다. 기존 `EventAuthority` revoke class/derived aggregate·제품 failure settlement/source-zero·revoked actual-socket roundtrip은 후속 C3-3 slice다.)
- 영속 세션 호스트 2c3d C3-3a1 dormant authority 집중 gate: `zig build test-session-host-2c3d-c3-3a1` (C3-3 gate를 상속하고 기존 `AttachmentCleanupRegistry.EventAuthority`의 accepted-revoke class와 checked derived cache를 Debug·ReleaseFast registry runtime 7+boundary 1로 검증한다. ordinary/unknown class none, reserved/live/releasing, 정상·corrupt final consume, sibling `0→1→2→1→0`, pre-reserve `0→0`, post-reserve abort `0→1→0`, copied registry 거부, same-address generation ABA, typed stale/double settlement delta 0과 test-only bounded scan/cache 일치를 고정한다. production은 affected row+checked counter O(1) 검증만 하며 이 standalone gate가 고정한 product take/query caller는 활성화 전 exact 0이다. no-fail continuation/recovery replay·unauthorized underflow subprocess는 C3-3a3가 소유하고, 이 dormant gate의 green은 C3-3a2 final-admission이나 C3-3a3 제품 활성화를 뜻하지 않는다.)
- 영속 세션 호스트 2c3d C3-3a2 dormant final-admission 집중 gate: `zig build test-session-host-2c3d-c3-3a2` (C3-3a1을 상속하고 final-address owner/copy/replay·active tamper fail-stop, clear admit+teardown fence, queued blocker, injected aggregate+sibling projection, callback/foreign/active-permit·execution-lease contention을 Debug·ReleaseFast transaction runtime 7+current-family regression 5+boundary 1로 검증한다. current family는 blocking/nonblocking input, generation control, pending output, 모든 raw `callOrdered` RPC와 nonblocking/RPC resync 두 경로다. transport는 typed `Busy`, RemoteRuntime nonblocking/pending/resync stream은 progress `false`, raw RPC는 `AdminBusy`, observer resize는 success no-op와 exact owner retention을 유지한다. owner-query destination이 slot/node/operation registry/request owner/binding storage와 겹치거나 held-operation destination이 operation exact extent 또는 최대 4개의 추가 caller protected range와 겹치거나 range가 overflow/cap 초과이면 execution lease 전에 `InvalidOwner`다. active raw lifecycle/ownership tag는 `u8`로 선검증하고 self/ownership/content drift는 fail-stop하며 두 ownership mode의 foreign settlement는 mutation 0 `InvalidOwner`다. attach prepared request/execute는 기존 fence 회귀만 상속하고 future 2c3e typed execute는 signature type assertion·caller 0이다. 새 mutex/fence/generation 없이 기존 registered operation의 shared pin을 existing execution lease로 upgrade하고 held internal leaf 뒤 downgrade한다. already-operation wrapper의 caller는 a2 test harness뿐이다. a1 query와 a2 transaction은 각각 declaration exact 1·production caller exact 0이고 실제 연결은 a3가 소유한다.)
- 영속 세션 호스트 2c3d C3-3a3 revoke ordering activation 집중 gate(구현·활성화 완료): `zig build test-session-host-2c3d-c3-3a3` (C3-3a2를 상속하고 a1 take/release와 a2 final admission을 동시에 활성화한다. Debug·ReleaseFast product runtime 10+actual-socket 2+boundary 1로 permit→public prepare→registered operation→direct lease→held validate/borrow 뒤 각 take precommit fault와 ClientSlot-only held commit wrapper의 `Terminal|Corrupt|InvalidPrepared`와 reserve 전 direct-lease `Busy`가 public prepared abort/reset→operation release→permit abort를 거쳐 `(queue=1,prepared=pristine,aggregate=0,permit/pin/quarantine/reserved-authority=0)`으로 수렴하는지, queue commit 뒤 a1 publish→permit no-fail consume→lease downgrade→transaction consume→operation release final stage를 고정한다. private live-operation mint receipt의 SSOT는 4,096-slot process-global bounded registry이며 mutex 아래 O(1) free-stack/direct-slot을 쓴다. atomic registry token은 pre-lock locator만 소유하고 tuple/live는 mutex 안에서 slot index+generation+registry key+final address+Client/operation+PID/process nonce+thread exact key로 consume/abort한다. canonical `errdefer`가 capacity를 회수하며 deterministic consume-vs-abort는 winner 하나만 slot을 반환한다. PID/process nonce/TLS precheck와 lock 뒤 PID 재검증은 fork child를 상속 mutex/Client 접근 전 거부한다. held API는 raw pointer/public digest/local pin을 금지하고 opaque handle `{slot index,slot generation,private key,publication identity,operation identity}`만 받는다. receipt registry와 별도인 process-private bounded 4,096-slot keyed capability registry가 O(1) free-stack/direct-slot으로 active/closing/readers를 소유한다. 별도 bounded 4,096-slot O(1) reader-pin registry는 final pin address+reader slot/generation/key+capability slot/generation/key canonical row를 먼저 등록한 뒤 readers를 올리고, exact one-shot row consume만 unpin/close owner reader를 내린다. copied/moved/forged/double-unpin/sibling pin은 mutation 0이다. settlement pre-lock은 registry PID/nonce+reader-slot bounds만 읽고 public `pin.fields` authority는 0이다. mutex 안 final-address/reader slot-gen-key row exact consume→canonical cap slot-gen-key materialize→owner process/thread/TLS compare 뒤에만 readers--하며 close drain은 captured canonical cap tuple만 쓴다. OOB/foreign/fork fields tamper는 mutation 0이고 injected reader-capacity exhaustion seam은 out pristine/readers unchanged와 즉시 reuse를 검증한다. capability key는 module-private 256-bit production random secret의 keyed BLAKE3 transcript를 64-bit로 축약한 probabilistic private discriminator다. immediate reuse의 exact ABA authority는 slot generation이며 old/new key inequality 테스트는 관측 oracle이지 absolute collision-free 계약이 아니다. registry pin 뒤에만 private guard에 capability pointer/fence/body를 materialize한다. publish exhaustion은 pristine rollback한다. end는 active→closing→self reader release→readers 0→generation bump/free-slot 뒤 body lifetime을 끝낸다. closing 게시 직후 late pin reject+close wait, same-address immediate reuse, mmap-unmap stale, forged key/fork/replay와 caller/private-registry closure를 고정한다. fault/closing hook은 `builtin.is_test` conditional private storage/API라 설계상 production callable API가 없으며 nm/symbol-zero 검증은 주장하지 않는다. max-terminal issuer와 forged/copied/foreign/callback/publication-teardown/fork PoC, deterministic end-vs-reader·consume-vs-abort, abort-capacity 원복·O(1) free-count oracle의 typed reject·mutation 0을 고정한다. Zig build에 TSAN target은 없으며 Debug·ReleaseFast deterministic interleaving과 atomic ordering review가 검증 경계다. activation 중 callback reentry·foreign·teardown 거부와 persistent-session-host.md closed 7-row의 facade blocker 결과·owner 보존을 검증한다. boundary는 held wrapper의 ClientSlot-only exact activated caller inventory와 `idle|ended_pending`의 transaction/operation/lease/quarantine/pin/a1 0, `idle=(prepared pristine, permit consumed)`, `ended_pending=(prepared tombstoned, permit consumed)`을 고정한다. check 뒤 aggregate race에서도 public nonblocking input은 `0`, public control은 성공 반환+FIFO 유지, internal pump는 progress `false`다. target pending outbound offset 0은 exact free 1/wire 0, partial은 no-retry fail-close, sibling은 offset/owner 보존·flush 0 뒤 aggregate zero에서 재개한다. C3-3b failure settlement와 C3-3c actual socket/source-zero는 후속이다.)
- 영속 세션 호스트 2c3c C2 집중 gate: `zig build test-session-host-2c3c-c2` (C1 runtime 7+boundary 1에 더해 generation nonblocking adapter의 closed command projection, exact 오류 정규화, 실제 generation attach의 supported/unsupported wire 0, canonical frame-allocation OOM retain·무독성·exact-once retry, 포화 socket의 zero/partial/full pending 진행, pending input→core→coalesced scroll→input suffix 순서, peer close의 hard-error queue retain, queue 64/65 및 allocation fail-close를 Debug·ReleaseFast runtime 5로 검증한다.)
- 영속 세션 호스트 2c3c C3 집중 gate: `zig build test-session-host-2c3c-c3` (C2 전체 인벤토리에 더해 blocking generation RemoteRuntime 5+slice-exclusive Client write 1을 Debug·ReleaseFast로 실행한다. 실제 generation attach의 input→control→input 순서, Unsupported wire 0, input/control OOM queue 폐기 후 각각의 실제 terminate RPC, OOM queue retain·exact-once retry, active drain 재진입 AdminBusy/논블로킹 pump 보류와 queue 단일 소비, detach fail-close/RPC 0, 새 scroll/core frame의 injected 0·1·len-1·plain full/EINTR write suffix offset과 ambiguous partial fail-close를 검증하고 boundary 1로 generation direct Client/RPC fallback 0과 legacy/resync baseline을 고정한다.)
- 영속 세션 호스트 실제 PTY/RSS gate(macOS): `zig build test-session-host-slow-observer-macos` (driver와 fixture host를 ReleaseFast로 빌드해 별도 host PID의 제품 RuntimeManager/SocketServer/poll-owner loop, wire runtime.spawn의 실제 forkpty, controller/slow/healthy 세 connection, 실제 slow write stall 뒤 controller token→healthy screen 진행, `proc_pid_rusage:RUSAGE_INFO_V4` raw sample과 logical ledger/cleanup을 `tests/artifacts/perf/session-host-slow-observer-macos.json`에 남기고 exact-schema validator로 판정한다. test runner RSS·`ru_maxrss`는 쓰지 않는다)
- CR6f/P4 E3a 실제 PTY output-wake·screen-idle gate(macOS): `mise run session-host-cr6f-output-wake` (위 slow-observer 제품 fixture를 ReleaseFast로 새로 실행해 실제 `/bin/cat` runtime 1·10·100개에 각각 controller+observer를 붙이고 1초 steady idle마다 screen snapshot/delta call, screen-owned allocation, core-lock acquisition 증분 exact 0과 host user+system CPU 100ms 이하를 고정한다. 1-runtime 행은 output-wake notify/write/coalesce/drain 증가 0과 더 강한 CPU 25ms 상한도 함께 지킨다. 이어 controller marker 7개의 healthy observer raw monotonic latency와 실제 notifier/write/drain 증가를 기록한 뒤 추가 runtime 99개를 종료하고 512×256 slow-observer pressure/RSS 시나리오를 수행한다. validator는 delivery median 10ms·개별 tail 20ms, exact 1/10/100 행·1초 창·projector counter와 CPU cap, active/idle wake, stall 격리, RSS·FD·child/host cleanup을 fail-closed 판정한다. 이 gate는 E3b metadata sampler, 장시간 idle soak나 CR6d 실제 AppKit IME 성공을 뜻하지 않는다.)
- P4 E3a screen token 집중 gate: `zig build test-session-host-e3a` (server/runtime-manager를 Debug·ReleaseFast로 실행해 unchanged token projector 0, admission rollback 뒤 exact retry, change commit 뒤 재투영 0, revision rollover의 fresh snapshot, 화면 무변경 4회 뒤 5번째 metadata cadence publication, 실제 PTY output drain과 resize의 token/wake 전진을 검증한다. 같은 두 모드의 실제 fork host·socket·`/bin/cat` 제품 테스트가 초기 OSC 7/2/5379 event와 후속 input delta를 client Surface까지 고정하고, source boundary가 제품 vtable·read-before-project fence·actual 1/10/100 artifact를 고정한다. 실제 CPU와 slow-observer 격리는 위 macOS 제품 gate가 소유하며 E3b는 포함하지 않는다.)
- P4 E3b runtime metadata sampler 집중 gate: `zig build test-session-host-e3b` (E3a를 상속하고 runtime 단일 source token의 unchanged/stale/change, checked revision→incarnation rollover와 target-only exhaustion fail-close를 Debug·ReleaseFast로 검증한다. runtime-owned 100ms sampler와 output-drain 직후 sampler, stream별 admission rollback/commit, fresh observation·resync의 delivery base 정합, same-PID handoff 복원을 제품 vtable 경계까지 고정한다. `mise run session-host-cr6f-output-wake`의 schema v4 제품 아티팩트는 실제 `/bin/cat` runtime 1·10·100개 steady idle에서 metadata producer/materialization/core-lock 증분 exact 0과 100-runtime 중 단일 변경의 sampler 1회·producer 3 stream·materialization 1회·core-lock 3회를 함께 판정한다.)
- P4 E3c GUI client idle-pump 집중 gate(macOS): `zig build test-session-host-e3c` (ReleaseFast 별도 client process가 독립 제품 host와 generation-backed `RemoteTermBackend`의 실제 `/bin/cat` runtime 1·10·15·100개를 붙인다. 60 idle frame의 client PID CPU와 selected/pump/seal/registry/socket/event raw counter, 100-runtime target marker, client fd·host/socket/directory cleanup을 `tests/artifacts/perf/session-host-client-idle-pump-macos.json`에 남겨 exact-schema validator로 판정한다. 실제 AppKit `DispatchSourceRead` wake 반복 E2E가 추가 green이 되기 전에는 E3c 완료가 아니다.)
- P4 host-backed input parity 집중 gate: `zig build test-session-host-input-parity` (E2c를 상속하고 Debug·ReleaseFast에서 AppSession의 DECSET 1003 exact-mode 및 1000/1002·같은 셀·Shift/Option·chrome 억제, 알 수 없는 ABI modifier 비트의 trap-free 제거, 실제 forkpty host reader의 xterm SGR no-button motion byte, selection scroll-and-extend 뒤 authoritative copy를 검증한다. capability 없는 구 host의 motion 0·selection transaction no-op은 동적 legacy process E2E가 아니라 source-boundary 증거다.)
- P4 N1 bounded notification journal 집중 gate: `zig build test-session-host-notification-journal` (input parity를 상속하고 Debug·ReleaseFast에서 final-address host owner, stable host/runtime/event identity, GUI/OS delivery bit 독립 ack, event/resident/field cap, prepare-before-evict, allocator fail-index rollback과 N2a의 단일 `RuntimeManager` 제품 owner를 검증한다. OSC delivery·daemon-internal macOS sink는 N2b, cold-launch route는 N3 범위이므로 이 gate만으로 background notification 제품 완료를 주장하지 않는다.)
- P4 N2a notification admission 집중 gate: `zig build test-session-host-notification-admission` (N1과 input parity를 상속하고 Debug·ReleaseFast에서 PTY 비신뢰 bytes의 UTF-8/control-sequence sanitizer, normalized cap, OSC 9/777 overflow 분류, generation-CAS OOM/rejection rollback과 runtime 공정성, GUI/OS bit 독립, same-PID handoff의 row/ID/bit/counter 및 allocation/corruption fail-close를 검증한다. config/label과 daemon-internal macOS sink는 N2b, cold route는 N3 범위다.)
- P4 N2b notification delivery 집중 gate: `zig build test-session-host-notification-delivery` (N2a를 상속하고 Debug·ReleaseFast에서 N2b1의 fail-closed metadata/controller·config generation/stable GUI route와 N2b2 daemon-owned OS machine의 typed route, accepted-only OS ack, terminal degraded, 250ms→8s 6회 retry, sibling 공정성, async timeout, journal 축출 뒤 stored-route exact expire, fresh/restore macOS adapter 배선·bare bundle fail-close를 검증한다. 실제 Notification Center 표시는 provisioned signed runner가 필요한 별도 제품 gate이고, N2b3 GUI history 및 N3 cold-click route도 이 gate의 완료 범위가 아니다.)
- CR6e-a1 stalled-peer transport baseline gate(macOS): `mise run session-host-cr6e-baseline` (ReleaseFast 제품 `connectExistingHostUntilObserved`에 harness-owned exact manifest와 실제 Unix listener를 붙여 accept+hello read 뒤 reply를 보류하는 250ms absolute-deadline 행, listener 제거 뒤 10-attempt/9-wait exact-host backoff 행을 실행한다. OS release·machine model·logical CPU, monotonic phase/deadline/end, typed failure, attempt/wait, peer read/close, 전후 `proc_pid_rusage:RUSAGE_INFO_V4`와 `/dev/fd`, exact cleanup을 `tests/artifacts/perf/session-host-cr6e-baseline-macos.json`에 남기고 별도 strict-schema validator가 판정한다. 이 명령은 CR6e-a2 actual-AppKit 반복, CR6e-b 장시간 soak나 성능 상한 완료를 뜻하지 않는다.)
- CR6e-a2 actual-AppKit recovery baseline gate(macOS): `mise run session-host-cr6e-recovery-baseline` (ReleaseFast 앱 5개를 같은 daemon/runtime에 순차 실행해 매번 일반 discovery→행 publication→실제 `NSEvent` click→remote marker render→제품 Quit을 통과한다. 복구 뒤 smoke handshake가 PTY 지연 출력을 발생시키고 실제 `DispatchSourceRead` handler 증가와 handler 진입→Metal frame 뒤 screen probe가 예산 안(`cr6c_appkit_smoke.zig`의 `wake_apply_latency_budget_ns`가 소유한다 — 숫자를 여기 옮겨 적지 않는다)인지를 각 iteration raw 값으로 남긴다. 앱이 받은 iteration identity와 launch/row/click/visible/summary/exit monotonic timestamp, runtime 생존·controller/observer 0, iteration별 고유 before/after PPM 10개, 최종 FD 원복·child 0·daemon/socket/host artifact 정산을 `tests/artifacts/perf/session-host-cr6e-recovery-baseline-macos.json`과 격리 HOME 아래에 남기고 strict-schema validator가 판정한다. 이 opt-in 실제 WindowServer gate는 host 출력 시작부터의 전체 latency·CR6e-b hard cap·장시간 soak나 자동 reconnect 배선 완료를 뜻하지 않는다.)
- CR6e-b paired hard-budget gate(macOS): `mise run session-host-cr6e-budget` (CR6e-a1/a2 raw artifact를 새로 만든 뒤 `Mac16,9`·Darwin `25.5.0`·16 logical CPU 환경과 transport RSS/FD/CPU/latency, recovery 단계 latency·identity·capture·authority·cleanup hard cap을 fail-closed 판정한다.)
- CR6e-b long soak(macOS): `mise run session-host-cr6e-soak` (위 pair를 20 batch 반복해 stalled transport 40행과 실제 AppKit recovery 100회를 실행한다. recovery는 batch마다 같은 daemon/runtime을 유지한 채 앱 5개를 순차 실행하며, batch 사이에는 daemon/runtime을 새로 만든다. 각 raw JSON을 `tests/artifacts/perf/session-host-cr6e-soak/`에 보존하고 마지막 validator가 누락·추가 batch 없이 전수 budget/cleanup을 다시 판정한다. 실제 WindowServer를 쓰는 opt-in 전용이며 기본 `check`나 hosted CI에서 skip/pass로 대체하지 않는다.)
- CR6e-c1 자동 reconnect worker handoff 집중 gate: `zig build test-session-host-cr6e-c1` (Debug·ReleaseFast에서 app-global 32-host 고정 상한 owner의 same-host coalesce, immutable running receipt, stale generation, copied receipt·slot ABA, queued/running Quit cancellation과 exact-cap을 검증한다. c1은 socket/thread/Client/Runtime pointer와 제품 caller가 0이며 c2 worker issuer·c3 main-owner publication 전에는 자동 reconnect 완료를 뜻하지 않는다.)
- CR6e-c2 자동 reconnect worker issuer 집중 gate: `zig build test-session-host-cr6e-c2` (Debug·ReleaseFast에서 c1 `{Key, Snapshot}`의 exact-host·absolute-deadline 보존, `connectExistingHostUntil` 단일 호출, host-gone/deadline/retry-later 분류, move-only candidate completion의 copied/stale/duplicate take 거부와 abandon exact-once close를 검증한다. c2의 thread/queue·AppSession caller는 0이며 c3 worker 수명·main-owner publication 전에는 frame-thread block 0이나 자동 reconnect 완료를 뜻하지 않는다.)
- CR6e-c3a main-owner candidate adoption 집중 gate: `zig build test-session-host-cr6e-c3a` (Debug·ReleaseFast에서 128-bit incident nonce, HostPool membership/old connection generation, exact bound runtime admission과 absolute deadline을 모두 재검증하고, stale candidate의 ownership/runtime mutation 0 및 성공 candidate의 기존 CR5 final-address job 이전을 검증한다. c3a에는 thread/queue/Quit join·AppSession caller·CR5 transaction driver·actual disconnect E2E가 없으므로 자동 reconnect 완료를 뜻하지 않는다.)
- CR6e-c3b worker/coordinator 집중 gate: `zig build test-session-host-cr6e-c3b` (Debug·ReleaseFast에서 app-global로 설치될 단일 worker lane의 condition wake, c2 `Completion` final-address 직접 기록, invalid order 선행 거부, retained completion backpressure, idle 및 completion-retained Quit join/consume 순서와 c3b2a의 c1 선예약·실패 철회·prepared admission 선행 검증·최초 identity coalesce·physical/logical final-address receipt 왕복, c3b2b의 failed completion all-runtime bound admission 정산, actual daemon candidate의 coordinator→CR5 adoption→terminal lease 정산 E2E와 actual shared-Client CR5 frame당 closed-state 1단계 driver를 검증한다. 이 gate에는 아직 AppSession caller와 actual AppKit disconnect E2E가 없으므로 c3b 또는 자동 reconnect 완료를 뜻하지 않는다.)
- 서명된 session-host caller-attested N-1→current live-upgrade E2E(macOS, 릴리스 아티팩트 필요): `zig build test-session-host-signed-upgrade -Dsession-host-signed-n1-exe=/절대경로/N-1/maru -Dsession-host-signed-current-exe=/절대경로/current/maru -Dsession-host-release-test-uuid=<trusted-run이 만든 lowercase-RFC4122-v4-UUID>`. 두 실행 파일은 서로 다른 SHA-256, current UID가 소유하고 group/other-writable이 아닌 executable, strict codesign 통과 및 Apple certificate/team을 포함한 exact designated requirement 일치를 만족해야 한다. 하네스는 별도 release manifest나 binary version을 읽어 두 파일의 release 인접성/방향을 스스로 증명하지 않으므로, 현재 명령 성공만으로 frozen N-1 provenance를 주장할 수 없다. release job이 immutable manifest의 semver/tag/commit, protocol/handoff schema, bundle/build identity, SHA와 signer를 leaf에 교차검증해야 최종 release gate가 된다. 하네스는 supervised N-1 제품 daemon에 echo를 끈 실제 PTY child를 만들고 pre/post child-output marker, 같은 host PID·child PID·`host_id`·`runtime_id`, `upgrade_epoch + 1`, target build ID, `committed/none` 및 upgrade capability 유지를 자동 단언한다. 복원 뒤에는 raw stream attach가 아니라 GUI 제품 경계인 `RemoteRuntime.attachExisting`으로 saved exact runtime ID를 재접속하고, 그 객체의 최초 full-state와 input/delta 경로로 pre/post marker를 확인한다. 이어 PTY 종료 marker로 shell을 종료하고, host-owned reap 뒤 exact runtime ID가 inventory에서 연속 두 번 사라지며 direct child도 사라졌는지 확인한다. wire가 exit status를 노출하지 않으므로 숫자 23 자체는 관측 증거로 주장하지 않는다. `zig-out/session-host-signed-upgrade/summary.json`은 canonical `maru.session-host-signed-upgrade-e2e.v2` leaf이며 trusted run의 exact `test_uuid`, predecessor/candidate executable SHA-256, signer requirement digest, runtime set, GUI exact reattach와 runtime reap/inventory 제거 증거를 남기고 시작할 때 이전 leaf를 제거한다. UUID·실행 파일 입력이 없거나 signer가 맞지 않으면 skip하지 않고 실패한다. 이 성공 경로 하나만으로 frozen N-1 provenance·최대치 multi-runtime·앱 재실행 orchestration/soak를 완료로 보지 않는다.
- 서명된 near-max multi-runtime 제품 restore E2E(macOS, 같은 릴리스 아티팩트와 `-Dsession-host-release-test-uuid=...` 필요): `zig build test-session-host-signed-upgrade-near-max -Dsession-host-signed-n1-exe=/절대경로/N-1/maru -Dsession-host-signed-current-exe=/절대경로/current/maru -Dsession-host-release-test-uuid=<같은 trusted-run UUID>`. `max_runtime_count - 1`인 실제 PTY 255개를 만들고 한 attempt로 전량 restore한 뒤 typed inventory exact set, 각 ID의 `RemoteRuntime.attachExisting` 최초 화면과 고유 post input/delta, 모든 child reap과 최종 empty inventory를 검증한다. `zig-out/session-host-signed-upgrade-near-max/summary.json`은 같은 UUID와 runtime count 및 정렬된 exact ID set digest를 가진 두 번째 canonical v2 leaf다. 1-runtime gate나 codec fixture 성공으로 이 부하 gate를 대체하지 않는다.
- session-host Release evidence 집중 gate: `zig build test-session-host-release-evidence` (OS 중립 `release_evidence.zig`의 `maru.session-host-release-evidence.v1` canonical `baseline_a | upgrade_b` parser/writer를 Debug·ReleaseFast로 검증한다. role별 exact gate set, canonical UUID v4, repository/release/source/build run-attempt, DMG·frozen executable과 B predecessor 결속, 1-runtime/near-max leaf 교환·재사용 거부, 두 leaf의 signer requirement↔caller expected designated-requirement exact 결속, 모든 scope의 malformed/duplicate/unknown/missing/cap과 allocation fail-index가 범위다. 실제 leaf 파일의 no-follow 읽기·aggregate exclusive publication, signed app leaf 실행, artifact attestation, GitHub Release workflow 배선 또는 별도 G3 default-on evidence 완료를 뜻하지 않는다.)
- session-host Release evidence filesystem 집중 gate: `zig build test-session-host-release-evidence-files` (macOS actual filesystem에서 profile별 leaf를 기존 no-follow·bounded file adapter로 읽고 opened inode alias를 거부한 뒤 canonical core로 조립하여 absent output에만 exclusive atomic publication하는 `release_evidence_files.zig`를 Debug·ReleaseFast로 검증한다. 조립 gate의 symlink·hardlink alias, malformed·identity mismatch, 기존 output과 allocation fail-index publication 0이 범위다. 하위 file adapter의 special/oversize/read-drift 계약은 `test-session-host-release-adapter-files`가 별도로 소유하며, caller identity의 GitHub provenance, signed leaf 생산, artifact attestation 또는 release workflow 배선 완료를 뜻하지 않는다.)
- session-host held evidence input 행(macOS): `zig build test-session-host-release-adapter-files` (`PinnedReleaseFile.readHeldAlloc`이 final pathname identity를 전후 검증하면서 실제 bytes는 기존 held fd에서 bounded exact read하고 최초 inode/size/SHA와 EOF를 다시 결속한다. actual filesystem에서 exact bytes/identity, copied owner, pathname replacement 보존, cap과 allocation failure publication 0을 Debug·ReleaseFast로 검증한다. manifest semantic authoring과 GitHub upload는 후속 gate다.)
- session-host candidate manifest authoring 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-manifest` (trusted candidate/product/source/compatibility와 held evidence, role B predecessor authority graph에서만 canonical manifest와 exact asset set을 유도하고 encode+self-parse 뒤 output open 직전에 최초 fixed snapshot을 재검증한다. caller role/profile/identity/signing/compatibility/result scalar 0, copied/pre-owned/alias owner, malformed·교환 evidence, authority/path drift, 기존 output과 allocation fail-index publication 0 및 held manifest inode 반환을 Debug·ReleaseFast actual filesystem에서 검증한다. aggregate attestation, draft attach/publish와 frozen signed U5 제품 E2E는 후속 gate다.)
- session-host 단일-subject attestation action 정적 gate: `zig build test-session-host-release-attestation-action` (local composite action이 canonical absolute regular file 하나만 받아 pre/post device·inode·link-count·size·SHA를 결속하고 `actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6` provenance mode를 정확히 한 번 호출하는지 검증한다. glob·CSV 목록·symlink·hardlink·control character와 caller predicate/digest/action-ref 입력은 거부한다. live workflow의 권한·호출 순서와 GitHub service 성공은 후속 wiring gate다.)
- session-host authored pair attestation action 집중 gate: `zig build test-session-host-release-authored-attestation-action` (retained stage-3 owner-only exact-two-entry directory와 baseline evidence/candidate manifest를 전체 구간 pre/post fence 아래 evidence→manifest 순서로 기존 single-subject action에 exact once씩 넘기고, 두 local bundle locator를 preparation 밖의 subject와 서로 다른 canonical regular single-link·bounded file로 확인한 뒤에만 함께 출력하는지 검증한다. digest·attestation ID/URL·predicate·성공 scalar 입력과 direct `actions/attest` 선택은 0이며, bundle의 암호학적 의미 검증·resume/workflow/U5 E2E는 후속이다.)
- session-host stage-4 이후 resume authority product 집중 gate: `zig build test-session-host-release-adapter-candidate-resume-authority-product` (retained preparation reopen→aggregate four-bundle semantic verification→manifest의 DMG/frozen/evidence와 aggregate 관측 cross-binding→current draft 재인증→ready draft 채택을 하나의 final-address transaction과 deadline으로 묶고, bundle verifier의 token 0·current API 직전 local fence·실패 cleanup과 durable bytes 보존을 Debug·ReleaseFast에서 검증한다. fixture는 harness-owned `mkdtemp`만 사용하며 실제 앱 session-host 상태나 GitHub release를 건드리지 않는다.)
- session-host exact draft asset attachment 집중 gate(macOS): `zig build test-session-host-release-adapter-draft-assets` (exact `DraftAuthority.id`에 canonical manifest가 지목한 DMG→frozen executable→evidence→manifest 네 distinct held inode를 pathname 재open 없이 fd-streaming하고 strict upload response와 전후 typed graph를 결속한다. exact request/body/order, shared deadline, copied/pre-owned/alias owner, drift·duplicate·partial failure와 remote mutation 뒤 cleanup-required/remote-state-unknown 비재시도 상태를 Debug·ReleaseFast에서 검증한다. redownload validator, publish와 live workflow는 후속 gate다.)
- session-host exact draft asset redownload 집중 gate(macOS): `zig build test-session-host-release-adapter-draft-redownload` (ready draft/attached asset owner와 네 held file·typed attestation graph에서 exact asset ID를 유도해 DMG→frozen executable→evidence→manifest를 octet-stream으로 다시 읽고, body를 저장하지 않는 bounded streaming byte-count/SHA-256으로 원본과 결속한다. exact argv/clean environment/order, oversized·short·foreign body, timeout, copied/pre-owned/alias owner와 전후 authority drift publication 0을 Debug·ReleaseFast에서 검증한다. publish, post-publish attestation과 live workflow는 후속 gate다.)
- session-host exact draft publication 집중 gate(macOS): `zig build test-session-host-release-adapter-draft-publish` (ready attachment/redownload와 전체 typed graph에서 exact draft ID와 네 asset을 유도해 `draft=false` 단일 PATCH만 실행하고, strict published immutable response의 ID/tag/source/lifecycle 및 exact asset ID/name/size/digest/content-type 집합을 결속한다. clean argv/environment, shared deadline, copied/pre-owned result, duplicate asset authority, parser allocation fail-index, mutation 전 두 authority snapshot allocation failure의 mutation 0·empty 상태, 응답 후 snapshot allocation failure의 cleanup-required 상태, drift와 mutation 뒤 remote-state-unknown/cleanup-required 비재시도 상태를 Debug·ReleaseFast에서 검증한다. post-publish attestation과 live workflow는 후속 gate다.)
- session-host post-publish release attestation 집중 gate(macOS): `zig build test-session-host-release-adapter-post-publish-attestation` (ready `PublishedRelease`와 publication이 소비한 ready `DraftAssets`·`RedownloadValidation` 및 attachment 권위 graph에서 release/manifest/네 asset을 다시 유도하고, bounded tag chain을 source commit까지 수렴시킨 뒤 exact release verify 1회와 held DMG→frozen executable→evidence→manifest verify-asset 4회를 같은 deadline으로 수행한다. tag-ref/source 분리, 각 child 전후 CLI·inode·전체 graph fence, copied/pre-owned/alias result, drift·foreign capture·child/timeout/allocation failure의 publication 0을 Debug·ReleaseFast typed composition에서 검증한다. actual held filesystem 재검증은 이 owner가 호출하는 draft-assets gate가 소유하며, live workflow와 frozen signed U5 제품 E2E는 후속이다.)
- session-host post-publish aggregate retention 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-aggregate-retention` (sealed `VerifiedRelease`를 reopened aggregate·canonical manifest의 protected context와 네 artifact digest에 다시 결속한 뒤 retained directory를 exclusive cleanup tomb으로 바꾸고 tomb pathname을 held directory identity에 다시 결속한 후 다섯 held entry를 역순 삭제한다. Debug·ReleaseFast에서 실제 filesystem 삭제와 12회 삭제 primitive 구간 timing sample, foreign manifest name·hardlink·rename 전 directory replacement 보존, rename 직후 tomb replacement의 unlink 0, 모든 cleanup 단계 실패의 monotonic suffix 재시도, copied/corrupt owner와 descriptor-close 불확실성의 non-retry를 검증한다. harness 임시 directory와 synthetic receipt만 사용하며 실제 GitHub release·credential·앱 session-host를 읽거나 수정하지 않는다. executable stage-8 command, workflow 배선, abrupt process death 뒤 cleanup tomb 재발견과 frozen signed U5 E2E는 후속이다.)
- session-host baseline-A candidate evidence authority 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-baseline-evidence` (trusted candidate evidence identity와 backing file/product/source authority에서 common identity를 fixed snapshot으로 유도하고, baseline 두 leaf의 canonical aggregate 조립 뒤 output open 직전에 exact authority를 다시 검증한다. caller common/profile 입력, copied/pre-owned/alias owner, allocator callback identity drift, malformed·교환 leaf, 기존 output과 allocation fail-index publication 0 및 held output inode 반환을 Debug·ReleaseFast actual filesystem에서 검증한다. signed leaf 실행, aggregate attestation, manifest/draft publication과 frozen U5 제품 E2E는 후속 gate다.)
- session-host baseline-A signed leaf transaction 집중 gate: `zig build test-session-host-release-adapter-candidate-baseline-phase` (하나의 absolute deadline과 trusted candidate identity 아래 initial 재검증→default-false→재검증→signed-app-quit→재검증→aggregate→final candidate/deadline 검증의 닫힌 순서, 모든 setup 실패의 attempted-child 역순 cleanup, cleanup failure terminal 승격과 성공 owner 보존을 Debug·ReleaseFast에서 검증한다. 제품 leaf runner와 trusted workflow caller는 후속이다.)
- session-host baseline-A product execution ownership core 집중 gate: `zig build test-session-host-release-adapter-candidate-baseline-product` (preserved candidate/work root 아래의 app main·bundled CLI·서로 다른 child root 유도, copied/pre-owned owner의 callback 전 거부, shared deadline/order, callback 전 attempt 기록, 성공 owner 보존과 역순 cleanup/retry set을 Debug·ReleaseFast에서 검증한다. concrete typed authority와 app/frozen byte·signer 결속, actual filesystem leaf runner/aggregate publisher, workflow caller와 artifact attestation은 후속이다.)
- session-host baseline-A preserved candidate app authority 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-baseline-app` (`CandidateProduct`의 frozen SHA·designated-requirement digest에 보존 `Maru.app` main·bundled CLI의 held no-follow executable identity를 결속하고, main digest/signer 불일치, symlink·hardlink·같은 inode, candidate/path drift와 copied/pre-owned/alias owner를 Debug·ReleaseFast actual filesystem에서 child 실행 전에 거부한다. 실제 앱/CLI 실행, 격리 session root, leaf/evidence publication과 workflow caller는 후속이다.)
- session-host baseline-A runner workspace 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-baseline-workspace` (기존 descriptor-owned workspace SSOT로 private 0700 root를 만들고 두 HOME·두 leaf·aggregate exact path를 final-address owner에 봉인한다. ambient HOME/config·실제 `/tmp/maru-<uid>`를 유도하지 않으며 copied/pre-owned/alias, root 교체·잔여물에서 foreign deletion 0과 cleanup retry authority를 Debug·ReleaseFast actual filesystem으로 검증한다. substrate의 sync failure retry는 기존 pre-publish workspace gate가 소유한다. 실제 child와 leaf/aggregate publication은 후속이다.)
- session-host release Zig toolchain 권위 집중 gate(macOS): `zig build test-session-host-release-adapter-zig-toolchain-authority` (protected tag와 같은 source SHA의 GitHub-hosted macOS ARM64 runner에서 mise 뒤 workflow가 고정한 canonical absolute Zig pathname·size·SHA-256을 final-address owner로 결속하고, executable mode와 held/path/parent identity를 실행 전후 재검증한다. exact `0.16.0` version과 capture 순서는 workflow contract gate가 고정한다. copied/pre-owned owner, symlink·hardlink alias·mode/size/bytes/parent drift는 실패한다. 로컬 빌드·업그레이드는 이 공식 release evidence 권위를 요구하지 않는다.)
- session-host baseline-A leaf process adapter 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-baseline-child` (closed kind 하나가 typed candidate identity에서 UUID·digest를 유도하고, 보존 app/workspace authority의 exact 경로로 해당 build entrypoint만 held source cwd·고정 system PATH+비쓰기 HOME+source-local Zig cache의 closed environment·single remaining deadline 아래 실행하는지 Debug·ReleaseFast에서 검증한다. Zig executable은 raw caller pathname이 아니라 위 final-address toolchain authority에서만 오며 child 실행 직전·직후 재검증한다. authority snapshot, leaf 사전 부재와 exit 0 뒤 regular `0600` exact leaf, foreign capture·drift·기존/누락/symlink/public leaf·timeout/nonzero exit의 성공 0이 범위다. 두 child 순서·aggregate publication·cleanup retry owner와 live release workflow의 전체 phase 배선은 후속이다.)
- session-host baseline-A production runner 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-baseline-runner` (이미 보존된 candidate/app/workspace/toolchain/source 권위만 빌려 기존 product phase에 실제 두 leaf adapter와 aggregate publisher를 연결하고, caller scalar/path 재제출 0, 하나의 deadline, 성공 산출물 보존, 모든 fail-index의 aggregate→signed-app-quit→default-false exact child 역순 cleanup과 실패 retry set을 Debug·ReleaseFast에서 검증한다. 실제 filesystem 권위·publication은 runner가 호출하는 app/workspace/child/evidence 집중 gate가 각각 소유하며, app/workspace 생성, artifact attestation, manifest/draft publication과 live workflow 호출은 후속이다.)
- session-host upgrade-B candidate evidence authority 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-upgrade-evidence` (candidate identity와 authenticated predecessor identity의 backing authority graph에서 common·predecessor·signing requirement를 fixed snapshot으로 유도하고, 두 upgrade leaf의 canonical aggregate bind 뒤 output open 직전에 전 authority와 held inode를 다시 검증한다. caller identity scalar, copied/pre-owned/alias owner, allocator callback drift, malformed·교환 leaf, 기존 output과 allocation fail-index publication 0 및 held output inode 반환을 Debug·ReleaseFast actual filesystem에서 검증한다. signed leaf 실행, aggregate attestation, manifest/draft publication과 frozen U5 제품 E2E는 후속 gate다.)
- session-host candidate compatibility authority 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-compatibility` (final-address candidate files/product와 held frozen parent authority에서 `__session-host --release-compatibility`를 shared deadline으로 실행하고 current 검증과 같은 canonical parser를 사용해 frozen SHA·release/source/build identity와 compatibility를 fixed owner로 유도한다. 실행 전후 inode·parent seal·candidate graph 재검증, copied/pre-owned/alias owner, foreign capture, timeout, malformed/noncanonical output과 executable/parent/candidate drift publication 0을 Debug·ReleaseFast에서 검증한다. manifest authoring과 signed frozen 제품 E2E는 후속 gate다.)
- session-host upgrade failure matrix 첫 집중 gate(macOS): `zig build test-session-host-upgrade-failure-matrix`. U3 same-PID process failure 14개와 U5 zero-runtime/non-empty PTY 제품 rollback activation을 함께 실행한다. boundary inventory가 corrupt/divergent handoff, preflight fail/hang, exec 반환, adoption/path swap, 연속 rollback, promotion failure 및 두 제품 rollback leaf의 exact 포함을 고정한다. 이는 첫 named matrix이며 signed frozen artifact나 아직 포함되지 않은 manifest/socket/FD failure까지 검증했다고 주장하지 않는다.
- session-host upgrade component failure matrix 두 번째 집중 gate(macOS): `zig build test-session-host-upgrade-component-failure-matrix`. `handoff_store` 9개, `exec_fd_set` 6개, `host_authority` 3개, `upgrade_target` 5개의 exact module test를 함께 실행하고 boundary inventory가 네 source의 test title·개수와 build dependency를 고정한다. existing component seam과 reserved handoff syscall-boundary/cleanup identity/실제 kernel permission·non-empty 연속 실패 처리가 release 검증에서 빠지지 않게 하지만 socket/product coordinator end-to-end 주입까지 증명하지 않는다.
- session-host reserved handoff failure 집중 gate(macOS): `zig build test-session-host-upgrade-reserved-handoff-failures`. 제품 `commitReserved`의 primary/backup sync, attempt directory 두 sync, 두 unlink, attempt rmdir, owner sync를 test-only one-shot seam으로 각각 실패시켜 `Pair` 비게시, fd 정리, owner-pinned attempt residue 0을 검증한다. 실제 kernel disk fault나 cleanup syscall의 연속 실패 fail-stop을 대체하지 않는다.
- session-host reservation cleanup failure 집중 gate(macOS): `zig build test-session-host-upgrade-reservation-cleanup-failure`. reserved primary pathname을 다른 inode로 교체한 뒤 `Reservation.cancel`이 replacement를 보존하고 `CleanupFailed`를 반환하면서 original/backup/attempt/owner/readback fd를 모두 닫고 terminal inactive가 되는지 검증한다. coordinator의 cleanup 오류가 `invariant_violation`으로만 수렴하는 source boundary를 함께 고정하지만, outer-loop process fail-stop E2E나 여러 실제 kernel cleanup syscall fault 주입을 대체하지 않는다.
- session-host coordinator cleanup fail-stop 집중 gate(macOS): `zig build test-session-host-upgrade-coordinator-cleanup-fail-stop`. coordinator-private test hook이 budget reservation 직후 primary identity를 교체해 reserved commit 실패와 cleanup 실패를 연속 발생시키고, 실제 `processArmed`가 최종 `invariant_violation`을 반환하는지 검증한다. 같은 gate는 `upgrade_loop`가 그 결과를 `fail_stop`으로만 분류하는 exact test를 함께 실행한다. 실제 daemon process exit/socket closure E2E는 아직 대체하지 않는다.
- session-host 실제 kernel cleanup 연속 실패 집중 gate(macOS): `zig build test-session-host-upgrade-kernel-cleanup-faults`. 같은 named gate의 component 행은 실제 reserved attempt 디렉터리를 읽기 전용으로 바꾼 뒤 제품 `Reservation.cancel`을 실행해 pinned primary/backup 제거 syscall의 `EACCES`와 non-empty attempt 제거 syscall의 `ENOTEMPTY`를 관측하고, aggregate `CleanupFailed`, terminal inactive와 열린 fd 전량 회수를 검증한다. process 행은 ambient env나 MRSH test command가 아닌 `builtin.is_test` typed fixture로 같은 실제 filesystem 조건을 제품 coordinator에 만들고 `invariant_violation`→outer-loop `fail_stop`→daemon `ManifestFailed` nonzero exit, sibling/listener/socket/owner lease 폐쇄를 검증한다. component의 exact errno와 process의 종료 증거를 합쳐 해석하며, process 결과만으로 각 errno를 직접 관측했다고 주장하지 않는다. disk-full/fsync와 signed frozen release artifact는 별도 미검증 범위다.
- session-host 실제 disk-full admission 집중 gate(macOS): `zig build test-session-host-upgrade-disk-full-admission`. user-owned HFS+ disk image에 실제 fork daemon의 owner directory를 두고 target staging이 끝난 뒤 coordinator-private `builtin.is_test` fixture가 incompressible filler를 실제 `ENOSPC`까지 쓴다. 이어지는 제품 `budget_admission.prepare`가 quiesce 전에 `resumed/state_too_large`로 끝나고, accepted drain의 기존 sibling은 typed 폐쇄되지만 새 연결의 같은 PID `host.info`, attempt status와 exact runtime inventory가 계속 살아 있으며 reservation residue가 없음을 검증한다. fixture는 ambient env, MRSH command와 공개 coordinator `Context`를 넓히지 않고 syscall 결과를 합성하지 않는다. 이 gate는 actual write-side ENOSPC만 닫으며 delayed fsync fault와 signed frozen release artifact는 별도 미검증 범위다.
- session-host release manifest 집중 gate: `zig build test-session-host-release-manifest` (OS 중립 `release_manifest.zig`의 canonical A/B JSON round-trip, 모든 scope의 duplicate/unknown/trailing 거부, manifest/scalar 선행 상한, closed repository/release/source/build/compatibility/signing 정책, 정렬 architecture, asset role exact-count·basename·SHA·size, evidence asset 결속과 allocation fail-index unwind를 Debug·ReleaseFast로 검증한다. typed observation policy는 실제 canonical manifest bytes와 SHA, repository/release/source/build, frozen executable SHA에 결속한 compatibility/signing, regular/no-follow asset, artifact attestation subject name/digest, DMG 내부 executable, evidence summary SHA와 candidate executable, published immutable predecessor A의 canonical bytes와 release attestation asset set을 교차검증한다. publication policy는 같은 manifest/run identity 아래 trusted protected tag 환경, pinned Action, no-clobber, immutable B predecessor, manifest exact-one과 attached/release-attested asset exact-set, candidate attestation부터 post-publish release attestation까지의 exact 순서를 검증한다. GitHub API·codesign·DMG 명령을 수행해 이 typed input을 만드는 executable adapter와 release workflow 배선은 후속 슬라이스이므로 이 gate만으로 immutable release provenance 또는 G3 출하 준비 완료를 주장하지 않는다.)
- session-host GitHub tag-chain resolver 집중 gate: `zig build test-session-host-release-adapter-git-resolver` (final-address `Backing`이 소유한 caller-sized `[]Sha` 길이를 annotated object 상한으로 사용해 lightweight/nested tag가 exact manifest commit으로 수렴하는지, 0-hop 정책, self/earlier cycle, depth exhaustion, foreign current object/commit, replay·copied resolver/backing·cross-resolver reuse·owner/backing overlap·결속 뒤 descriptor drift fail-close를 Debug·ReleaseFast에서 검증한다. 제품 최대 깊이 선택, GitHub API transport와 executable authority는 검증하지 않는다.)
- session-host GitHub workflow run 응답 집중 gate: `zig build test-session-host-release-adapter-github-run` (현재 Actions context와 REST 응답의 exact run ID/attempt, push event, source SHA, canonical release workflow tag path, in-progress/null lifecycle, empty PR set, repository/head-repository 동일성을 Debug·ReleaseFast 및 allocation fail-index에서 검증한다. 이미 캡처된 REST bytes의 component 의미만 검증하며 API transport 권위나 현재 job의 protected `release` environment 통과 증거는 아니다.)
- session-host GitHub release environment 응답 집중 gate: `zig build test-session-host-release-adapter-github-environment` (exact nonzero `release` environment identity, 관리자 bypass 불가, 1~43,200분 wait timer, 1~6명의 typed/nonzero reviewer, mutually-exclusive branch policy와 unknown future rule의 비권위 보존을 Debug·ReleaseFast 및 allocation fail-index에서 검증한다. 이미 캡처된 REST bytes의 component 의미만 검증하며 현재 workflow job이 그 environment protection을 통과했다는 증거나 API transport 권위는 아니다.)
- session-host GitHub release deployment 결속 집중 gate: `zig build test-session-host-release-adapter-github-deployment` (attempt-scoped jobs의 exact current release signing job과 source/tag/environment로 좁힌 deployment 및 각 status 전체 이력을 함께 해석해 공식 `pending`과 exact-one `in_progress`, exact-one job URL 결속을 Debug·ReleaseFast 및 allocation fail-index에서 검증한다. REST가 보장하지 않는 배열 순서나 비공식 `waiting` 값을 권위로 쓰지 않는다. 이미 캡처된 component bytes의 의미만 검증하며 API transport·pagination·현재 실행 executable·workflow 배선을 대신하지 않는다.)
- session-host GitHub API transport 집중 gate(macOS): `zig build test-session-host-release-adapter-github-transport` (closed request enum이 exact repository/run/release/tag/environment/deployment endpoint와 GET/header를 만들고 collection에는 `per_page=100`+`--paginate --slurp`를 강제한다. bounded page flattening과 jobs `total_count` 완결성, complete JSON/scalar cap, token control/size 검증, clean `GH_TOKEN`+`GH_PROMPT_DISABLED` 환경, supplied-buffer capture provenance, stdout-only protocol bytes, absolute exec·positive timeout·process-group kill을 Debug·ReleaseFast 실제 process와 injected executor 11개 행으로 검증한다. caller가 넘긴 GitHub CLI executable의 공급망 권위와 release workflow 배선은 후속 경계다.)
- session-host GitHub CLI executable 권위 집중 gate(macOS): `zig build test-session-host-release-adapter-github-cli-authority` (공식 GitHub Release CI의 checkout 전 canonical absolute path+lowercase SHA-256 capture 계약, exact GitHub-hosted macOS ARM64 runner observation, no-follow regular executable의 device/inode/size/digest와 transport 직전 재검증을 Debug·ReleaseFast 실제 filesystem에서 검증한다. 로컬 빌드·앱 인증서 upgrade는 범위 밖이며 release workflow의 실제 capture/transport 배선은 후속 gate다.)
- session-host release adapter absolute deadline 집중 gate(macOS): `zig build test-session-host-release-adapter-deadline` (한 phase의 positive budget과 단일 `CLOCK_MONOTONIC` 관측에서 final-address `Deadline`을 exact once 만들고, non-increasing remaining, exact expiry, rollback·overflow·zero/negative budget, copied/pre-owned owner를 Debug·ReleaseFast에서 검증한다. 하위 GitHub/Apple composition의 pointer 배선과 전체 executable/workflow 완료는 후속 gate가 소유한다.)
- session-host current GitHub release 권위 집중 gate(macOS): `zig build test-session-host-release-adapter-github-current-release-authority` (strict context↔canonical manifest를 외부 호출 전 결속하고 repository/run/protected environment/deployment 전체, authenticated paginated release 목록의 exact-one mutable draft, 최대 8-hop tag chain을 하나의 absolute deadline과 request 직전 pinned-CLI 재검증으로 조립한다. published 전용 tag-name endpoint나 목록 order/latest를 draft 권위로 쓰지 않는다. predecessor/current가 공유하는 owned tag-chain helper, lightweight/1/8-hop, 9-hop/cycle/mismatch, reusable response buffer, copied owner·CLI/child/allocation failure의 부분 publication 0을 Debug·ReleaseFast에서 검증한다. current manifest attestation, Apple 제품 관측, summary/executable/workflow 배선은 후속 범위다.)
- session-host current manifest attestation 집중 gate(macOS): `zig build test-session-host-release-adapter-github-current-manifest-attestation` (current B canonical bytes를 strict context와 move-only current release authority에 결속하고, 기존 descriptor-owned `ManifestFile`의 canonical name/computed SHA와 before/after identity·0400·link-count-1을 재검증하면서 pinned CLI의 exact artifact attestation을 실행한다. role/predecessor/context/current authority/file mismatch, copied owner, CLI/attestation/allocation failure의 publication 0을 Debug·ReleaseFast에서 검증한다. certificate·subject 세부 변조는 재사용하는 `test-session-host-release-adapter-github-attestation`이 소유하며, Apple 제품 관측, summary/executable/workflow 배선은 후속 범위다.)
- session-host published-A manifest attestation 집중 gate(macOS): `zig build test-session-host-release-adapter-github-manifest-attestation` (`verify-predecessor`가 현재 protected tag context와 exact role-A manifest를 직접 결속하고 artifact attestation을 검증한다. 아직 없는 successor B의 predecessor를 합성하지 않으며 role B/predecessor 존재/context·file drift, copied owner, deadline/result/output·context backing과 pinned CLI owner를 포함한 전 입력 alias, shared deadline·CLI·attestation/allocation failure의 publication 0을 Debug·ReleaseFast에서 검증한다. 전체 verify-predecessor transaction과 workflow/U5 E2E는 후속 범위다.)
- session-host GitHub release attestation 집중 gate(macOS): `zig build test-session-host-release-adapter-github-release-attestation` (published immutable release statement의 exact repository/release/tag/tag-ref와 공개 시점에 붙은 manifest 내부 세 asset 및 canonical manifest 파일 자체, 총 네 attached artifact의 name/SHA-256 exact set을 결속한다. release/verify-asset closed argv, clean token environment, local basename 선택, malformed·missing·duplicate·additional subject, allocation fail-index와 child failure를 Debug·ReleaseFast에서 검증한다. CLI pin/revalidation, predecessor composition과 live workflow는 후속 범위다.)
- session-host current manifest pathname composition 집중 gate(macOS): `zig build test-session-host-release-adapter-github-current-manifest-input` (canonical basename의 absolute caller pathname을 no-follow regular fd에서 한 번만 bounded read하고, owned bytes/computed digest를 descriptor-owned 0700 work-directory의 0400·link-count-1 canonical leaf로 materialize한 뒤 current manifest attestation까지 move-only owner 하나에 결속한다. 원본 read 뒤 mutation과 attestor가 보는 private path의 격리, basename/symlink/non-regular/empty/oversize/destination/materialization/attestation 실패, residue 0, copied/pre-owned owner와 allocation unwind를 Debug·ReleaseFast 실제 filesystem에서 검증한다. local DMG/frozen executable·Apple product 관측, summary/executable/workflow 배선과 frozen U5 E2E는 후속 범위다.)
- session-host release adapter 계약 집중 gate: `zig build test-session-host-release-adapter-contract` (`pre-publish`·`verify-predecessor` 두 command와 caller가 absent leaf로 지정해야 하는 canonical absolute `--work-dir`(root·`.`/`..` component·중복/후행 slash 거부), 모든 pathname의 exact·component-boundary ancestor/descendant alias 거부, phase별 exact option, `ohah/maru`, canonical `v<major>.<minor>.<patch>`, `Maru-<version>-session-host-release.json`, `release` protected environment, summary schema 및 observation JSON 입력 금지를 Debug·ReleaseFast에서 검증한다. 이 allocation-free OS-neutral parser gate는 canonical 절대경로·alias 금지만 검증하며 실제 leaf absence·생성·cleanup은 filesystem workspace owner gate가 소유한다. GitHub API·codesign·DMG를 실행하는 adapter executable이나 workflow 배선 완료를 뜻하지 않는다.)
- session-host release adapter 파일 권위 집중 gate(macOS): `zig build test-session-host-release-adapter-files` (absolute path의 모든 component를 `openat(O_NOFOLLOW)`로 순회하고 최종 fd에서 regular/type·bounded bytes·SHA-256·device/inode identity를 만든다. final/intermediate symlink, directory, oversize, input hardlink alias를 거부한다. summary owned publication은 0600 `O_RDWR` temp의 complete write·fsync 뒤 같은 fd에서 size/SHA를 만들고 `RENAME_EXCL`+parent fsync+final inode 재검증 뒤 held file/parent fd를 move-only owner로 반환한다. 기존 void publication은 이 경계를 닫아 durable output만 남기는 wrapper이며 predecessor work-dir은 absent leaf에만 0700으로 만든다. Debug·ReleaseFast 실제 filesystem 6개 테스트이며 evidence/manifest 조립, GitHub API·codesign·DMG 의미 검증이나 완성된 adapter executable을 뜻하지 않는다.)
- session-host pre-draft candidate attestation 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-attestation` (trusted tag context에서 canonical DMG/frozen basename을 유도하고 두 no-follow held file의 identity·size·SHA를 exact GitHub artifact attestation과 같은 absolute deadline 아래 결속한다. draft/release ID 입력 0, CLI와 file pre/post revalidation, canonical DMG→frozen 순서, final publication deadline, copied/pre-owned owner, pathname·subject·context drift와 partial observation/OOM unwind를 Debug·ReleaseFast actual filesystem+injected attestor로 검증한다. draft 생성, Apple product 판정, evidence/manifest authoring과 workflow/U5 E2E는 후속 범위다.)
- session-host predecessor manifest download 집중 gate(macOS): `zig build test-session-host-release-adapter-github-manifest-download` (verified predecessor tag/SHA에서 exact manifest download argv와 supplied-buffer digest를 고정하고, executable `fetchUntil`이 pinned CLI owner와 모든 borrowed/mutable input의 메모리 disjoint를 먼저 검증한 뒤 하나의 final-address deadline을 pinned CLI 재검증 전후와 `Observed` publication 직전에 확인해 fresh remaining만 child에 전달하는지 검증한다. malformed preflight·alias·중간·최종 만료의 CLI/child/publication 0과 product wrapper compile을 Debug·ReleaseFast에서 고정하며, manifest materialization·attestation·전체 orchestration 완료를 뜻하지 않는다.)
- session-host predecessor manifest input composition 집중 gate(macOS): `zig build test-session-host-release-adapter-github-predecessor-manifest-input` (authenticated current B에서 predecessor expectation을 유도하고 exact `predecessor-manifest` workspace child에서 download→descriptor materialize→canonical A cross-bind→artifact attestation을 하나의 shared deadline/pinned CLI로 실행한다. historical protected-tag 값을 합성하지 않고 B exact A endorsement와 A artifact provenance를 결속하며 current publication의 protected-tag admission은 유지한다. final-address owner/caller-buffer reuse, current/A drift, 단계별 실패 cleanup residue 0과 product wrapper compile을 Debug·ReleaseFast actual filesystem에서 검증하며 predecessor assets·전체 orchestration 완료를 뜻하지 않는다.)
- session-host pre-publish workspace owner 집중 gate(macOS): `zig build test-session-host-release-adapter-pre-publish-workspace` (caller-owned absolute absent root를 no-follow parent 아래 0700으로 만들고 held parent/root device·inode에서 exact `current-manifest`·`predecessor-manifest`·`predecessor-assets`·`dmg`·`current-assets` absent child pathname만 유도한다. copied/pre-owned owner, parent/root replacement, occupied child, 초기화/empty-root cleanup 실패 시 durable removal까지 retry 권위를 보존하는지 Debug·ReleaseFast actual filesystem에서 검증한다. 이 gate는 child composition과 전체 phase orchestration·workflow wiring 완료를 뜻하지 않는다.)
- session-host pre-publish phase transaction 집중 gate: `zig build test-session-host-release-adapter-pre-publish-phase` (validated token 뒤 failure-pristine deadline과 workspace를 exact-once 생성하고, 닫힌 leaf 순서, deadline start 뒤 실패하면서 retry 권위를 남긴 attempted owner까지 fail-index별 역순 cleanup, observation setup 실패와 private cleanup 실패의 terminal observation cleanup, cleanup 실패 시 summary publication 0, private cleanup 뒤 final deadline 검증·deadline cleanup·summary publish·terminal observation cleanup 순서를 Debug·ReleaseFast에서 검증한다. leaf 의미 검증과 production adapter 타입 배선·workflow/U5 E2E는 각 별도 gate가 소유한다.)
- session-host pre-publish product execution 집중 gate(macOS): `zig build test-session-host-release-adapter-pre-publish-product` (caller-owned final-address `Execution`이 bootstrap의 exact pre-publish command/context/pinned CLI, validated token, shared deadline, private workspace와 기존 production leaf 타입을 transaction에 직접 배선하고 private cleanup 뒤 자체 소유 final deadline을 검증한다. result/bootstrap/token/context/command/buffer/Apple storage alias를 외부 동작 전에 거부하고 foreign/expired deadline의 publication 0, invalid/copied/pre-owned input의 side effect 0, ordinary failure의 empty state, cleanup 실패의 retry authority 보존을 Debug·ReleaseFast에서 검증하며 실제 GitHub/Apple 성공은 workflow/U5 E2E가 소유한다.)
- session-host frozen executable pathname 권위 집중 gate(macOS): `zig build test-session-host-release-adapter-frozen-executable-authority` (`release_adapter_files.PinnedExecutableFile`이 executable regular file을 no-follow fd와 final-address owner로 보존하고 manifest expected size/SHA-256을 64 KiB stack-buffer streaming hash로 결속한다. pin 전후와 후속 revalidation의 pathname hash·reopened/held fd identity/type/mode/link-count/size/time/digest, copied/pre-owned owner, relative/symlink/directory/non-executable, zero/cap/size/digest mismatch, pathname 교체와 in-place mutation을 Debug·ReleaseFast actual filesystem에서 검증한다. DMG 내부 executable·Apple product·current manifest 조립과 workflow/U5 E2E는 후속 범위다.)
- session-host current local product composition 집중 gate(macOS): `zig build test-session-host-release-adapter-github-current-product` (authenticated `CurrentManifestInput`의 exact DMG/frozen asset name·size·SHA와 version만 빌려 frozen executable no-follow pin→첫 revalidation→DMG/Apple observation→두 번째 revalidation→DMG 내부 SHA↔frozen SHA와 manifest signing↔Apple observation 대조 순서를 고정한다. 공통 release asset cap SSOT, absolute basename/path alias, unauthenticated/copied/pre-owned owner, asset/cap/signing/digest mismatch, observer failure 중 frozen mutation, cleanup/allocation unwind를 Debug·ReleaseFast actual frozen filesystem+injected DMG observer로 검증한다. 실제 hdiutil/Apple command와 mount residue는 기존 DMG authority gate가 소유하며 compatibility/evidence/asset attestation/final observation, summary/executable/workflow 배선과 frozen U5 E2E는 후속 범위다.)
- session-host current evidence provenance composition 집중 gate(macOS): `zig build test-session-host-release-adapter-github-current-evidence` (move-only authenticated current B/predecessor A manifest와 evidence summary pathname만 받아 summary asset basename·size·SHA, manifest evidence 필드, B↔A predecessor와 A asset digest를 교차검증하고 B에서 current identity·candidate·test UUID·signer expectation을 유도해 canonical `upgrade_b` evidence에 bind한다. caller identity scalar 0, no-follow bounded read, manifest-summary inode alias, copied/pre-owned owner, A/B·candidate·predecessor·signer·UUID·summary drift, malformed bytes와 allocation unwind publication 0을 Debug·ReleaseFast actual filesystem에서 검증한다. summary artifact attestation, compatibility·세 current asset attestation, final observation, workflow/U5 E2E는 후속 범위다.)
- session-host current asset private-file composition 집중 gate(macOS): `zig build test-session-host-release-adapter-github-current-asset-files` (authenticated current B manifest, move-only current product/evidence와 DMG/frozen pathname만 받아 manifest의 exact three-role name·size·SHA를 유도하고 source 네 inode distinct, held frozen 재검증과 DMG no-follow streaming read, owned evidence bytes를 absent 0700 directory의 exact-name 0400·link-count-1 private leaves로 complete copy·sync한다. fixed storage만 써 large asset heap copy 없이 source/destination digest와 pre/post fingerprint, path/role/alias/mutation/collision, copied/pre-owned owner, I/O failure의 publication 0과 cleanup retry를 Debug·ReleaseFast actual filesystem에서 검증한다. GitHub attestation, compatibility, final observation, workflow/U5 E2E는 후속 범위다.)
- session-host bounded 외부 명령 집중 gate(macOS): `zig build test-session-host-bounded-process` (일반 release adapter와 frozen compatibility가 공유하는 async-signal-safe `fork`/`fchdir`/`execve` 경계의 absolute diagnostic argv, stdin `/dev/null`, stdout-only/merged exact cap, exit 0+EOF, monotonic deadline, child process-group SIGKILL과 direct-child reap, fd 3 이상 전량 폐쇄를 검증한다. explicit held-directory API는 caller directory fd를 child cwd vnode로 고정하고 exact `./leaf` read/exec, pathname ancestor 교체 저항, regular/stdio/closed fd 사전 거부, parent `FD_CLOEXEC`·identity·ownership 불변과 sibling ambient fd 비상속을 Debug·ReleaseFast 실제 process 9개로 검증한다. 기존 upgrade codesign도 같은 일반 경계를 재사용한다. 이 gate는 `CurrentAssetFiles` attestation composition이나 GitHub/codesign/DMG 결과의 의미 해석·workflow 배선 완료를 뜻하지 않는다.)
- session-host current asset artifact-attestation composition 집중 gate(macOS): `zig build test-session-host-release-adapter-github-current-asset-attestation` (authenticated current B manifest와 final-address `CurrentAssetFiles`만 권위 입력으로 받아 exact three-role asset을 canonical role 순서로 검증한다. checkout 전 pinned CLI를 각 command 직전에 재검증하고, 하나의 absolute deadline 아래 held directory fd를 child cwd로 고정해 `gh attestation verify ./<exact-name>`만 실행한다. 세 leaf의 identity/mode/link-count/size/SHA를 command 전후 재검증하고 기존 certificate/context/run/subject semantic verifier로 결속하며, copied owner, role/context/subject·CLI·filesystem drift, timeout/child/OOM 실패의 publication 0과 partial observation cleanup을 Debug·ReleaseFast에서 검증한다. release verify/verify-asset, final observation과 workflow/U5 E2E는 후속 범위다.)
- session-host current frozen compatibility observation 집중 gate(macOS): `zig build test-session-host-release-adapter-github-current-compatibility` (authenticated current B manifest와 move-only current product에서 frozen executable SHA와 held parent-directory fingerprint를 유도하고 exact frozen pathname의 clean-environment hidden probe가 출력한 canonical bounded compatibility JSON을 protocol/screen/handoff/app-host ABI와 manifest에 결속한다. actual separate process와 injected probe로 실행 구간 pathname replacement·복원의 directory ctime 탐지, exact argv, duplicate/unknown/missing/trailing·noncanonical/range 거부, pre/post frozen identity·digest revalidation, copied/pre-owned owner, mutation·timeout·child failure publication 0을 Debug·ReleaseFast에서 검증한다. final observation, release verify/verify-asset, workflow/U5 E2E는 후속 범위다.)
- session-host current final manifest observation 집중 gate(macOS): `zig build test-session-host-release-adapter-github-current-observation` (authenticated current B/predecessor A와 predecessor release/assets, move-only current product/evidence/private asset files/asset attestations/compatibility만 받아 repository/release/source/build, manifest·asset attestation, frozen SHA·Apple signing, evidence와 predecessor publication을 caller scalar 없이 유도한다. held frozen fd와 held private directory/세 leaf를 final 조립 직전에 다시 검증하고 모든 owner/context를 교차결속한 뒤 canonical current/predecessor bytes로 `release_manifest.parseAndValidateObservation`을 통과한 strict `Parsed`만 final-address move-only owner로 게시한다. copied/pre-owned/stale owner, output/input storage alias, A/B·role/context/predecessor/executable/evidence drift, validator rejection과 allocation unwind publication 0을 Debug·ReleaseFast에서 검증한다. workflow command ordering, draft redownload/publish와 signed frozen U5 E2E는 후속 범위다.)
- session-host release validation audit summary encoding 집중 gate(macOS): `zig build test-session-host-release-adapter-summary` (`pre-publish`는 final-address `CurrentObservation` B, `verify-predecessor`는 authenticated role-A manifest+revalidated published asset owner만 받고 caller phase/성공/digest/identity scalar 0으로 `maru.session-host-release-validation.v1` canonical JSON을 만든다. exact `schema→phase→result→manifest_sha256→manifest_size→manifest` key/LF, nested strict manifest, role-phase binding, digest/size round-trip, owner/attestation/download/source drift, duplicate/unknown/missing/type/cap/trailing/noncanonical 변조와 allocation unwind를 Debug·ReleaseFast로 검증한다. output pathname의 exclusive atomic publish, adapter executable, workflow/GitHub publish는 후속 범위다.)
- session-host verify-predecessor phase transaction 집중 gate(macOS): `zig build test-session-host-release-adapter-verify-predecessor-phase` (failure-pristine deadline→workspace→published role-A manifest→immutable assets→summary bytes 준비→private owner 역순 cleanup→live deadline 최종 검증·cleanup→exclusive publication의 닫힌 순서, shared deadline identity, 실패하면서 retry 권위를 남긴 attempted owner를 포함한 단계별 reverse cleanup과 후속 호출 0, cleanup failure와 deadline 만료의 publication 0, retry authority 및 prepared bytes 해제를 Debug·ReleaseFast에서 검증한다. leaf 의미 검증, production executable/workflow와 frozen U5 E2E는 후속 범위다.)
- session-host verify-predecessor production execution 집중 gate(macOS): `zig build test-session-host-release-adapter-verify-predecessor-product` (bootstrap의 exact verify command/context/pinned CLI, validated token과 caller-owned disjoint buffers를 phase transaction의 production published-A manifest·immutable assets·prepared summary leaf에 연결하고 private cleanup 뒤 owned live deadline을 최종 검증한다. result/bootstrap/token/context/command/buffer alias를 외부 동작 전에 거부하고 invalid/copied/pre-owned owner의 side effect 0, foreign/expired deadline의 publication 0, success/ordinary failure empty state, cleanup failure retry authority와 sensitive borrow scrubbing을 Debug·ReleaseFast에서 검증한다. 실제 GitHub child 성공과 workflow/U5 E2E는 별도 gate가 소유한다.)
- session-host release validator executable 집중 gate(macOS): `zig build test-session-host-release-validator-executable` (bootstrap→exact GH_TOKEN→closed command dispatch, pre-publish/verify-predecessor exact-one product 호출, 20분 compile-time phase budget, component-cap 고정 disjoint storage, phase 교환·fallback 0, product failure 전파와 production type compile을 Debug·ReleaseFast에서 검증한다. 실제 GitHub/Apple child와 release workflow/U5 E2E는 후속 범위다.)
- session-host release validation summary publication 집중 gate(macOS): `zig build test-session-host-release-adapter-summary-publication` (production entrypoint가 `CurrentObservation` B 또는 authenticated predecessor A+held assets와 absolute absent output pathname만 받아 canonical encoding 뒤 기존 `publishSummaryExclusive`를 exact once 호출한다. caller summary/phase/success/digest/manifest scalar 0, encode 실패 publication 0, publish 실패 전파, actual filesystem exact bytes·0600·기존 destination 보존, output lifetime과 allocation unwind를 Debug·ReleaseFast에서 검증한다. 하위 file adapter의 전체 fault matrix, CLI parsing/orchestration, workflow/GitHub publish는 후속 범위다.)
- session-host Apple 제품 관측 집중 gate: `zig build test-session-host-release-adapter-apple-product` (frozen executable SHA에 bounded codesign detail/designated requirement, plist JSON, lipo output과 strict signature·app/DMG staple·DMG Gatekeeper 성공 receipt를 결속해 manifest `Signing`을 직접 만든다. identifier/team exact-one, `product_identity.bundle_id`·`bundle_version`, Apple team requirement digest, release version, exact `arm64 x86_64`, malformed/duplicate/mismatch/cap과 allocation fail-index unwind를 Debug·ReleaseFast에서 검증한다. 실제 command argv 실행, DMG no-follow extraction과 receipt 진위는 후속 executable 배선 범위이므로 이 gate만으로 제품 provenance 완료를 주장하지 않는다.)
- session-host Apple command transport 집중 gate(macOS): `zig build test-session-host-release-adapter-apple-transport` (closed command vocabulary가 system `plutil`/`codesign`/`lipo`/`xcrun stapler`/`spctl`의 exact executable·argv를 만들고 empty child environment, bounded capture 또는 exit-0 receipt, timeout·process-group kill/reap과 all-or-nothing `Captures` 조립을 Debug·ReleaseFast에서 검증한다. 입력 pathname의 no-follow authority와 DMG mount/extraction은 후속 gate다.)
- same-PID exec 다중 연결 fresh-process 회귀 gate(macOS): `zig build test-session-host-upgrade-multifd-fresh`. canonical GUI 연결을 둔 채 ephemeral inventory 연결을 열고 닫은 뒤 제품 session-host로 같은 PID exec, 동일 host identity 재접속, upgrade epoch 증가를 검증한다. 이 테스트는 daemon namespace와 exec 전역 상태를 소유하므로 큰 aggregate에서는 명시적으로 skip하고 이 exact 1-test artifact에서만 실행한다.
- macOS Swift/Zig app host ABI static library 빌드: `mise run macos-app-host-abi-lib` (Swift host가 링크할 Zig exported C ABI static library를 `zig-out/lib/`에 만든다. 이 명령 자체는 앱을 실행하지 않는다)
- macOS Swift app host type-check: `mise run macos-app-host-swift-check` (`MaruAppHost.swift`가 `app_host_abi.h`를 import하고 AppKit 타입을 type-check할 수 있는지 확인한다)
- macOS app 단일 writer 제품 스모크: `mise run macos-app-instance-lease-smoke` (격리한 user home에서 첫 앱이 lease 뒤 명시적 keep-alive config bootstrap까지 끝내고 AppKit 이전 barrier에 서는지, 제한적 umask에서도 lock mode가 `0600`인지 확인한다. 실행파일을 직접 두 번 띄워 두 번째 프로세스가 config bootstrap 전에 `second instance unsupported`와 exit 2로 거부되고 config/workspace/cache sentinel을 바꾸지 않으며 첫 프로세스는 계속 살아 있는지, 첫 프로세스를 `SIGKILL`한 뒤 새 프로세스가 같은 `workspace.v1.lock`을 다시 획득하고 bootstrap하는지 검증한다)
- 영속 세션 호스트 G2 config override 보존 집중 gate: `zig build test-session-host-config-override-retention` (Debug·ReleaseFast에서 앱 전역 bootstrap owner 4개 계약, stale Window 전체 Reset·row Reset/Backspace no-op·atomic replace 실패 제품 경로 3개와 source-order/단일-owner boundary 1개를 exact-count로 검증한다. 실제 fresh-process lease/loser mutation 0은 위 `macos-app-instance-lease-smoke`가 별도로 담당한다.)
- macOS Mermaid 격리 helper 빌드: `mise run macos-mermaid-helper-build` (`zig-out/MaruMermaidRenderer.app`을 App Sandbox로 서명하며 parent/helper 모두 Zig `mermaid_protocol.zig` wire v2 codec ABI를 링크한다)
- macOS Mermaid helper lifecycle smoke: `mise run macos-mermaid-helper-smoke` (실제 별도 helper process로 strict Mermaid SVG·sanitize, Hello/HelloAck, duplicate/result-flood 거부, cold 5초/warm 2초 deadline과 3회 hang kill/restart failure latch·exact termination ACK, stdin 미소비 max request의 bounded 종료, stdout/stderr EOF 1회 종료, executor deadline 지연 시 physical spawn 0, A→B→A path ABA에서 actual PID mismatch와 capability frame/Result commit 0, running=nil 종료 barrier 뒤 late callback/control 0, 100개 요청 bounded admission을 검증하고 `zig-out/maru-macos-mermaid-helper-smoke/mermaid-helper.summary.json`을 남긴다)
- macOS Mermaid native 성능 gate: `mise run macos-mermaid-perf` (`ReleaseSafe`; Debug 빌드 오버헤드가 helper의 제품 cold 5초/warm 2초 end-to-end deadline과 native tick 예산을 왜곡하지 않게 한다. `tests/artifacts/perf/live-preview-macos.json`; 제품과 같은 `MermaidProductTickAdapter`·`MermaidAcceptedResultDrainer` 1,000 tick, exact 512 KiB accepted SVG copy/decode≤20ms, helper queue/restart/latch, completion drain≤8, tick의 process/pipe/blocking wait 0을 검증한다)
- macOS Swift app host app shell 빌드: `mise run macos-app-build` (`zig-out/bin/maru-macos-app`를 만든다. 이 executable은 Zig ABI static library를 링크하고, Swift placeholder window 뒤에서 Zig app session ABI를 호출할 수 있어야 한다)
- macOS Swift app host app shell 실행: `mise run macos-app` (지속 실행되는 `NSApplication` window를 띄우고 Zig 쪽 interactive shell surface와 `FrameLoop`를 설정형 timer로 tick한다. 기본은 `render.frame-rate = 60`이며 Cmd+, 설정 화면에서 30~120Hz로 바꿀 수 있다. view의 `keyDown`, window resize, window close는 app session ABI로 내려간다. CoreText로 shape/raster한 glyph를 Metal로 그리며, HiDPI(Retina)와 번들된 JetBrains Mono를 쓴다. summary에 `terminal_surface=true`와 frame/output/input/resize/close 통계, `diag_*`(화면/창 scale, cell 픽셀 크기)를 남긴다)
- **렌더링·입력 디버그**: `MARU_DEBUG=1 mise run macos-app`로 켠다. ad-hoc getenv/fprintf가 아니라 진단을 데이터가 있는 곳에 둔다. (1) glyph rasterize 경로(`coretext_raster.zig`)에서 `std.log.scoped(.font_metrics)`로 `cell=…x… slot=…x…`(cache_key cell 메트릭 vs atlas slot 크기)와 codepoint/scale_milli/ink/status를 stderr로 찍는다 — cell과 slot이 어긋나면(slot ≠ cell × span) 글자 간격 버그라, 이 비교로 "m i s e" 류를 바로 잡는다. (2) 창 제목줄에 live `scr/win/cell/draw`(화면·창 backing scale, cell 픽셀, drawable 폭)를 띄운다. (3) summary 파일에 `diag_*`(화면/창 scale, cell, drawable) 필드를 덧붙인다. (4) 입력 경로 트레이싱: `SurfaceRuntime.debug_input`이 켜져 PTY로 나가는 입력 바이트를 escape해 `core->pty NB: …`로 찍는다 — 키 인코딩(`Cmd+←`→`\x01`, 기능키 `\e[5~` 등)·붙여넣기·zsh SIGWINCH redraw 시퀀스가 실제로 무엇을 보내는지 PTY 캡처 없이 본다. env 미설정 시 일반 실행/summary 계약에는 영향이 없다(로깅도 제목도 diag 필드도 안 나온다)
- **편집기 pane 렌더 관측**: `MARU_DEBUG=1`이면 `app_session.zig`의 편집기 렌더 블록이 `std.log.scoped(.editor)`로 프레임마다 `pane rect=(x,y wxh) lines=… ops=… visual_rows=… grid=colsxrows cells=…` 한 줄을 찍는다(lowering 실패면 `pane lowering failed: …`). "본문이 비었다"의 원인을 ⑴ 사각이 0이라 op이 안 나옴 ⑵ op은 나왔는데 셀이 0 ⑶ 셀까지 갔는데 합성이 덮음으로 가른다 — 셋은 고치는 자리가 다르다(단일 출처 [native-editor-visual-mapping.md §4.1b](native-editor-visual-mapping.md)). 예: `MARU_DEBUG=1 MARU_NATIVE_EDITOR=<절대 파일 경로> MARU_SCREENSHOT=/tmp/ed.ppm MARU_SCREENSHOT_DELAY_MS=2000 ./zig-out/Maru.app/Contents/MacOS/maru-macos-app 2> /tmp/ed.log` → `grep '(editor)' /tmp/ed.log`와 캡처를 함께 본다.
- **텍스트 파일을 CM6로 되돌려 열기**: 탐색기에서 클릭한 텍스트 파일(`.text` kind)은 **기본이 네이티브 편집기**다(2026-08-19). 네이티브는 아직 읽기 전용이라 **그 파일을 고칠 수 없으므로**, 편집해야 하면 `MARU_NATIVE_TEXT=0 mise run macos-app`으로 CM6를 부른다(단일 출처 [plans/native-editor.md](plans/native-editor.md) N1). UTF-8이 아니거나 상한을 넘어 네이티브가 못 읽는 파일은 기본 상태에서도 조용히 CM6로 열린다. 시작할 때 한 파일만 여는 `MARU_NATIVE_EDITOR`는 이제 필요 없다 — 아무 파일이나 클릭해서 연다.
- **sync(2026) 게이트 관측**: `MARU_DEBUG=1`이면 `app_session.zig`의 `logSyncGateDiag`가 `std.log.scoped(.sync)`로 tick별 sync hold/투영 결정을 한 줄씩 stderr에 찍는다(`active`·`hold/timeout`·`gproj`·`cproj`·`esuadv`/`scr`=투영 unblock 실제 이유·`bsu/esu`=리더 처리 BSU/ESU 누적·`voff` 등). 폴링 렌더 루프가 라이브 프레임을 언제 붙잡고 언제 flush하는지, `maru ssh` 원격 sync 어긋남이 어디서 나는지 본다(단일 출처 [io-render-present.md §11.6](io-render-present.md)). 캡처한 로그는 `python3 tools/sync/analyze_sync_log.py <log>`로 half-frame(active 중 grid 투영) 원인(esu_edge/timeout/scroll/force)을 자동 분해한다. **로그 자리 주의**: 앱은 tty 가 아니면 **자기 stderr 를 `$XDG_CACHE_HOME`(없으면 `$HOME/.cache`)`/maru/app.log` 로 스스로 돌린다**(`app_host_abi.zig` 의 리디렉션 + 4MB 캡). 그래서 셸에서 `2> log` 로 받으면 **빈 파일**이 남는다 — 로그는 그 캐시 파일에서 읽는다. 예: `HOME=/tmp/sh XDG_CACHE_HOME=/tmp/sh/.cache MARU_DEBUG=1 ./zig-out/Maru.app/Contents/MacOS/maru-macos-app &` → `python3 tools/sync/analyze_sync_log.py /tmp/sh/.cache/maru/app.log`.
- **프레임 타이밍 관측(스피너 지연 진단)**: `MARU_DEBUG=1`이면 `app_session.zig`의 `logFrameTime`이 `std.log.scoped(.frametime)`로 tick의 wall-clock을 단계별(`pre`·`titles`=syncAutoTitles 전체 코어 lock·`drain`=리더 PTY pump·`mid`·`project`=활성 surface CoreText build+투영)로 분해해 찍는다. 느린 tick(총>8ms)은 `SLOW total=…ms … project=…` 한 줄, 약 1초 창마다 `window=…s rate=…Hz … | titles=…% drain=…% project=…%` 요약. 스피너/blink cadence가 tick 카운트에 묶여 무거운 tick이 실효 tick rate를 떨어뜨리는(탭 전환 reshape=project vs SSH lock 경합=titles/drain) 지배 요인을 데이터로 가른다(단일 출처 [io-render-present.md §10.5](io-render-present.md)). 예: 탭 전환/원격 쉘 포커스를 반복하며 `MARU_DEBUG=1 ./zig-out/Maru.app/Contents/MacOS/maru-macos-app 2> /tmp/maru-ft.log` → `grep '(frametime)' /tmp/maru-ft.log`로 `SLOW`의 지배 단계와 창 요약의 rate 하락을 본다.
- **에이전트 상태(사이드바 카드) 검증**: `MARU_DEBUG=1 mise run macos-app`에서 실제 claude/codex를 실행하고 prompt→작업→권한 질문→ESC 중단→새 작업 순서를 반복한다. stderr의 `agent previous=… state=… rule=…` 로그와 카드가 함께 `idle→running→blocked/idle`로 바뀌는지, 화면 증거를 지우면 700ms 뒤 `unknown`이 되는지 확인한다. provider 훅·트랜스크립트는 사용하지 않으므로 부모 세션 환경을 별도로 해제할 필요가 없다(단일 출처 [agent-session.md "검증"](agent-session.md)).
- **제품 renderer 스크린샷**: `MARU_SCREENSHOT=/tmp/maru.ppm mise run macos-app`로 켠다. 제품 Metal renderer가 내용이 있는 첫 frame을 화면(CAMetalLayer drawable)이 아니라 같은 크기·픽셀포맷 오프스크린 텍스처에 그려 BGRA readback 후 PPM(P6)으로 그 경로에 쓰고 프로세스를 종료한다("한 frame 캡처 후 종료"). drawable은 `framebufferOnly=true`라 직접 못 읽으므로 오프스크린에 그린다. PPM은 smoke와 같은 `maru_ppm_writer.h`를 공유하며, `sips -s format png /tmp/maru.ppm --out /tmp/maru.png`로 변환해 본다. env 미설정 시 이 경로는 전혀 타지 않아 일반 실행/frame-loop present에 영향이 없다. 셀 readback gate가 없는 시각 확인용 하니스다(자동 시각 회귀 비교는 후속). **PTY 출력을 찍으려면 `MARU_SCREENSHOT_DELAY_MS=2500`을 함께 준다** — 첫 frame은 탭바/커서만으로 '내용'이 돼 셸 출력 도착 전에 캡처가 끝나므로, 이 지연이 캡처를 "내용 첫 frame 시점부터 N ms 지난 다음 frame"으로 늦춘다(예: `MARU_CONFIG=<테스트 config> MARU_SCREENSHOT=/tmp/x.ppm MARU_SCREENSHOT_DELAY_MS=2500 mise run macos-app` + config `shell.command`로 색 테스트 스크립트 실행 — 라이트 테마 가독성 검증에 쓴 패턴).
- **원격 파일 트리 실측 캡처(RF3a·RF4)**: `MARU_OPEN_EXPLORER=1`이 도크를 탐색기 뷰로 **유지**하고(첫 frame 한 번이 아니라 매 tick 재단언 — 원격 SCM follow가 나중 tick에 뷰를 덮는다), `MARU_OPEN_EXPLORER_FILE=<원격 절대경로>`가 그 파일 행을 **클릭과 같은 길**(`activateFileTreeRow`)로 한 번 연다. 원격 pane을 만드는 결정론적 방법은 `MARU_CONFIG`의 `shell.command`를 «OSC 5379+OSC 7을 찍고 `sleep`하는 스크립트»로 두고 `MARU_FORCE_SPLIT=1`로 그 셸의 새 pane을 띄우는 것이다 — 프롬프트가 있는 셸은 매 프롬프트마다 자기 OSC 7로 로컬 cwd를 되보고해 `MARU_FORCE_REMOTE_SCM`과 경합한다(실측). 캡처 하니스는 마지막 셸 pane을 활성으로 돌려놓는다(복원된 workspace의 활성이 편집기면 follow 게이트가 안 선다). 예: `MARU_CONFIG=/tmp/rf-config MARU_SCREENSHOT=/tmp/rf.ppm MARU_SCREENSHOT_DELAY_MS=20000 MARU_FORCE_SPLIT=1 MARU_OPEN_EXPLORER=1 MARU_OPEN_EXPLORER_FILE=/home/u/proj/build.zig ./zig-out/Maru.app/Contents/MacOS/maru-macos-app`. 전부 env 미설정 시 무동작하는 디버그 훅이다([계획 §10.7](plans/remote-file-tree.md)).
- **원격 트리 변경 실측 캡처(RF6b·RF6c)**: `MARU_OPEN_EXPLORER_RENAME=<원격 절대경로>|<새 이름>`이 그 행을 골라 **제품 문**(`enqueueFileTreeEdit` — 원격 갈림이 사는 자리)으로 이름을 바꾸고, `MARU_OPEN_EXPLORER_DELETE=<원격 절대경로>`가 삭제를 **요청**한다(확인 모달까지 — 확인을 누르는 것은 사람의 몫이라 픽스처가 대신하지 않는다). 원격 pane을 만드는 방법은 위 항목과 같다. ⚠️ 헬퍼 **판을 올린 직후 첫 캡처는 「못 읽는다」로 뜬다** — 설치가 그 tick을 가져가므로 두 번 돌리거나 지연을 더 준다([계획 §10.12](plans/remote-file-tree.md)).
- **파일 도크 FP3 시각 픽스처**: `MARU_FILE_PANEL=<절대 파일 경로> MARU_SCREENSHOT=/tmp/file-panel.ppm mise run macos-app`로 right 도크 placeholder+탭/경로 밴드를 연다. `MARU_FILE_PANEL_DOCK=bottom`을 함께 주면 bottom 배치를 검증한다. 두 env는 미설정 시 무동작하는 디버그 훅이다.
- **세팅 화면 자동 열기(시각 확인)**: `MARU_OPEN_SETTINGS=1 mise run macos-app`(또는 `MARU_OPEN_SETTINGS=1 MARU_SCREENSHOT=/tmp/s.ppm ...`)로 켠다. 첫 frame에서 세팅 화면(⌘,)을 자동으로 연다 — 스크린샷 하니스가 입력 없이 모달 상태를 캡처하게 하는 디버그 훅(MARU_DEBUG와 같은 env-gate). `MARU_OPEN_SETTINGS_SECTION=N`을 함께 주면 0부터 시작하는 특정 섹션을 열고, `MARU_OPEN_SETTINGS_SEARCH=<쿼리>`를 함께 주면 검색 결과를 캡처한다. env 미설정이면 무동작(일반 실행은 ⌘,로 연다).
- **붙여넣기 자동 주입(시각 확인)**: `MARU_PASTE='echo hi\nrm -rf ~' MARU_SCREENSHOT=/tmp/p.ppm MARU_SCREENSHOT_DELAY_MS=1800 mise run macos-app`로 켠다. 첫 frame에서 그 텍스트를 붙여넣어(`pasteText`, `\n`은 실제 개행으로 해석) paste protection 확인 모달을 캡처한다(개행이 있으면 모달이 뜬다). MARU_OPEN_SETTINGS류 debug-gate로 env 미설정이면 무동작. 확인 모달이 첫 frame에 뜬 뒤 캡처하려면 `MARU_SCREENSHOT_DELAY_MS`로 살짝 늦춘다.
- **세션 trace 레코딩(replay용)**: `MARU_TRACE=/tmp/session.trace mise run macos-app`로 켠다. 세션의 base kind 이벤트(원시 PTY 출력·resize·process-exit·사용자 입력)를 `maru.trace.v1`로 그 파일에 **이벤트마다 증분 append**한다(`app/trace_recorder.zig`의 `TraceRecorder` — `SurfaceRuntime` 훅, host file writer에 write+flush). 굳힌 trace는 `observability.replay.replayTrace`로 GUI 없이 화면을 byte-for-byte 재생한다(회귀 fixture·오프라인 분석 — 단일 출처 [trace-replay.md](trace-replay.md)). opt-in(미설정이면 오버헤드 0), 8 MB 상한(초과 시 앞부분만 유효). **크래시가 나도 직전까지 디스크에 남아** 재생된다(clean 종료 불필요). 캡처한 trace를 **git fixture로 승격**하려면 먼저 `maru trace anonymize /tmp/s.trace out.trace`로 PII(홈 경로·IP·`user@host`·유저명, env HOME/USER 기준)를 일반화한다(구조 보존 → 여전히 재생 가능; 남은 `TOKEN=…` secret은 경고). ⚠️input 이벤트는 화면에 안 뜨는 타이핑(비밀번호 프롬프트 등)까지 담으니, 승격 전 반드시 사람 검토(익명화/가드는 bare 비밀을 못 잡는다). 예: `MARU_TRACE=/tmp/s.trace mise run macos-app` → `maru trace anonymize /tmp/s.trace tests/fixtures/traces/x.trace.txt` → `MARU_UPDATE_GOLDEN=1 zig build test-replay`로 golden 생성.
- **IME 콜백 트레이스**: `MARU_IME_DEBUG=1`(렌더링 디버그와 **독립된 게이트** — IME만 따로 켤 수 있다)이면 `MaruMetalTerminalView`가 입력기 콜백을 `[IME] …`로 stderr에 찍는다 — `keyDown`(`mods=[Cmd Ctrl Opt Shift]`·characters·keyCode), `doCommand:`(편집 selector), `insertText`/`setMarkedText`/`unmarkText`, 텍스트는 `U+XXXX` 코드포인트 + utf16 len으로 표기. 입력기가 실제로 보내는 콜백 순서·인자를 그대로 봐서 한글 조합·마지막 자모 삭제(insertText+deleteBackward 상쇄)·확정 후 커서 이동 같은 IME 동작을 실측으로 잡는다("추측 말고 캡처"). 물리 키가 어떻게 정규화되는지(keyDown 수정자/keyCode)도 이 게이트로 본다 — 이번 macOS 편집키 디버깅이 이 경로로 진행됐다.
- macOS Swift app host app shell smoke 실행: `mise run macos-app-smoke` (`MARU_MACOS_APP_SMOKE_MS=15000`으로 실제 AppKit/CAMetalLayer+WKWebView를 띄운다. 빌드 target은 원본 fixture를 `zig-out/`으로 복사하고 `MARU_FILE_PANEL_EDIT_SMOKE=1`을 명시한 뒤, FP4 Markdown 본문·로컬 SVG·renderer 격리뿐 아니라 새 파일의 기본 live mode, 초기 hydration과 경쟁한 dirty-sync의 완료형 recovery, FP11b `EditableProjection` running·decoration>0·일반 fragment iframe=0, 실제 AppKit 글자 입력→CM6 dirty→실제 `⌘S`의 Zig `WebKeyRoute.web_editor` 판정→pathless atomic write의 disk marker까지 확인한다. 저장 성공 뒤 isolated bridge로 test-only Mermaid hang을 제출하고 helper in-flight에서 실제 `WKWebView.reload()`을 호출해 provisional navigation의 exact Promise 취소와 pending reply 0도 검증한다. 이 마지막 reload는 editable recovery latch를 의도적으로 세우므로 이후 편집 성공을 주장하지 않는다. 이어 FP5 HTML fixture를 별도로 실행해 ephemeral store·브리지/스킴 핸들러 부재·부모 read scope·내부 SVG 성공·scope 밖 SVG 차단·top-level pin을 확인한다. Markdown 결과는 `zig-out/maru-macos-app/markdown.summary.txt`에 보존하고 마지막 HTML 결과는 `app.summary.txt`에 남긴다. 수동 `MARU_FILE_PANEL=<절대 .md|.html>`·`MARU_WEB_APP_ROOT=<web/dist>` 재현은 파일을 읽기만 하며, **복사한 테스트 Markdown을 실제로 수정·저장하려는 경우에만** `MARU_FILE_PANEL_EDIT_SMOKE=1`을 함께 준다.)
- CR6c 실제 AppKit 세션 복구 스모크: `mise run macos-session-host-recovery-smoke` (unique current daemon/manifest와 marker를 출력하는 `/bin/cat` runtime을 만든 뒤 실제 Swift 앱의 일반 launch discovery가 `Recovered Sessions` row를 그리게 한다. read-only published rect로 만든 실제 `NSEvent` click 전/후의 CAMetalLayer frame을 `zig-out/maru-macos-app/session-host-cr6c-home/captures/`에 남기고, row one-shot 소멸·remote Term/marker publication·실제 Quit confirm 뒤 runtime 생존과 controller/observer 0·bounded child cleanup을 검증한다. IME·OS clipboard·장시간 soak 완료를 뜻하지 않는다.)
- R1 실제 AppKit durable tombstone 다회 재실행 스모크: `mise run macos-session-host-r1-tombstone-smoke` (격리된 실제 Application Support의 ended manifest를 제품 앱이 복원하고 background checkpoint로 원자 교체한다. 그 생성본을 다시 입력으로 두 번째 실행·checkpoint까지 반복하며 exact handle/state, 관측 시점의 직접 child process 0, `.bak`/temp 0과 두 생성본 byte equality를 검증한다. ad-hoc 서명 제품 경계이며 provisioned Developer ID artifact 증거는 아니다.)
- CR6d 실제 AppKit IME·clipboard 연속성 스모크: `mise run macos-session-host-input-continuity-smoke` (CR6c와 같은 실제 recovery click 뒤, opt-in smoke에서만 system-global source를 한국어 2벌식으로 전환하고 HID event tap에 물리 key-code `CGEvent`를 보내 `한글` 조합·확정 callback과 PTY exact1을 확인한다. 실행 중 recovery된 Maru 창을 한 번 클릭해 25초 안에 system frontmost focus를 넘긴다. 이 자동화는 macOS Privacy & Security의 Accessibility에서 빌드된 Maru 테스트 앱에 event-post 권한을 사용자가 미리 허용한 전용 환경만 대상으로 하며, 권한이 없으면 source 전환 전에 `accessibility-unavailable` RED로 끝난다. original/selected source ID는 전환 전에 atomic record로 남기며 정상·RED·종료 또는 앱 강제 종료 뒤 parent helper가 복원한다. current가 제3 source이면 사용자 선택을 덮지 않고 실패한다. `NSPasteboard.general`의 구분 가능한 한 줄은 실제 Cmd+V `NSEvent`로 remote input에 exact1 전달하고, recovery 전 historical line은 exact1을 유지하며 historical OSC52는 새 process의 pasteboard sentinel을 바꾸지 않아야 한다. 실제 Quit 뒤 current source exact original, restore record 소멸, runtime 생존·controller/observer0·bounded cleanup까지 확인한다. stalled socket/backoff·장시간 soak·성능 완료를 뜻하지 않는다.)
- signed baseline-A 제품 leaf 실행: `zig build macos-session-host-default-false-evidence macos-session-host-signed-app-quit-evidence -Dsession-host-signed-candidate-app=/절대경로/session-host-candidate-<버전>/Maru.app -Dsession-host-signed-candidate-dmg=/절대경로/session-host-candidate-<버전>/Maru-<버전>-universal.dmg -Dsession-host-signed-candidate-exe=/절대경로/session-host-candidate-<버전>/maru-session-host-<버전> -Dsession-host-default-false-test-uuid=<trusted UUID> -Dsession-host-signed-app-quit-test-uuid=<같은 trusted UUID> -Dsession-host-default-false-home=/절대경로/격리-root/default-false -Dsession-host-signed-app-quit-home=/절대경로/격리-root/signed-app-quit -Dsession-host-default-false-output=/절대경로/격리-root/default-false.json -Dsession-host-signed-app-quit-output=/절대경로/격리-root/signed-app-quit.json`. 격리 root는 caller가 새로 만든 mode `0700` empty directory이고 네 child pathname은 모두 absent여야 한다. 두 gate는 입력 app bundle의 main executable을 직접 실행하고 signed-app-quit은 같은 bundle의 CLI를 사용한다. 별도 ad-hoc app 재빌드나 checkout web asset fallback, 고정 `zig-out` HOME/leaf, ambient `/tmp` registry, 기존 pathname 삭제는 없다. 이 명령은 실제 Developer ID candidate·canonical UUID·격리 경로가 없으면 skip하지 않고 실패하며, leaf 실행만으로 GitHub provenance나 aggregate publication을 주장하지 않는다.
- bounded browser.executeScript WKWebView/socket smoke 실행: `mise run macos-browser-bounded-smoke` (`ReleaseSafe`; Debug의 byte-by-byte base64/JSON 비용이 16 MiB 운영 deadline을 왜곡하지 않게 제품 최적화에 가까운 빌드로 잰다. `MARU_WEB_PANEL=1`·test browser cap으로 `script-src 'none'`인 실 WKWebView에 소켓 요청을 보내 expression+Promise await+JSON args 성공, nested eval 차단, 고유 URL commit·실행 중 navigation 오류 분류, 구조화 strict JSON, persistent intrinsic 변조 방어, UTF-8 byte cap, typed error, depth 경계와 512 KiB inline/초과 chunk 전환·16 MiB 32-chunk 완료를 검증한다. `zig-out/maru-macos-app/app.summary.txt`의 11개 `browser_ctl_bounded_*` 필드가 모두 `true`이고 pump action>0·p95≤0.5ms·max≤1.0ms가 아니면 명령이 실패한다. GUI 눈검사는 필요 없다)
- macOS 배포용 .dmg 빌드: `mise run macos-dmg` (`macos-app-bundle`로 `Maru.app`을 ReleaseFast로 만든 뒤 codesign(Developer ID, hardened runtime, timestamp) → `dist/Maru-<버전>-arm64.dmg` 생성/서명 → `notarytool submit --wait`로 Apple 공증 → `stapler staple` → `spctl` 검증까지 한 번에 한다. arm64 단일 빌드이고 최소 macOS는 11.0(Big Sur, `build.zig`의 `default_target` os_version_min과 `MaruAppHost-Info.plist.in`의 `LSMinimumSystemVersion`을 함께 11.0으로 맞춘다). 제품 version은 `build.zig.zon`의 `.version` 하나가 소유하고 빌드가 생성 plist에 주입한다. release tag는 서명 전에 `zig build check-release-version -Drelease-tag=v<버전>`으로 exact 일치를 검증한다. **사전조건 두 가지**: ① 키체인에 Developer ID Application 인증서가 있어야 한다(서명 인증서 이름은 `-Dmacos-sign-identity=...` 또는 `MARU_SIGN_IDENTITY`로 준다 — 조직명·Team ID가 든 전체 이름은 저장소에 두지 않고, 키체인에 Developer ID 인증서가 하나뿐이면 기본 접두사만으로 찾힌다). ② 공증 자격증명을 notarytool 키체인 프로파일에 미리 저장해 둔다 — `xcrun notarytool store-credentials maru-notary --apple-id "$APPLE_ID" --team-id "$APPLE_TEAM_ID" --password "$APPLE_APP_SPECIFIC_PASSWORD"`(프로파일 이름은 `-Dmacos-notary-profile=...`로 변경 가능). 앱 전용 암호 같은 비밀값은 빌드 스크립트·리포에 두지 않고 키체인 프로파일에만 있다. 산출물 `dist/`는 git에 커밋하지 않는다)
- macOS 배포용 **universal** .dmg 빌드: `mise run macos-dmg-universal` (= `sh tools/build-macos-universal-dmg.sh`. arm64 + x86_64를 각각 빌드해 `lipo`로 합친 universal `Maru.app`을 만들고, codesign → `.app` 공증+staple → `dist/Maru-<버전>-universal.dmg` 생성/서명 → dmg 공증+staple → `spctl` 검증까지 한 번에 한다. 검증 뒤에는 같은 실행의 app·DMG와 app main executable의 byte-identical frozen 사본을 `dist/session-host-candidate-<버전>/`에 배타 게시한다. 기존 candidate 디렉터리는 자동 덮어쓰지 않으므로 같은 버전을 다시 만들 때는 호출자가 이전 staging 산출물을 명시적으로 치워야 한다. Intel·Apple Silicon에서 모두 실행된다. 사전조건은 `macos-dmg`와 동일(키체인 Developer ID 인증서 + `maru-notary` 프로파일; `MARU_SIGN_IDENTITY`/`MARU_NOTARY_PROFILE`로 덮어쓰기). x86_64 cross 빌드의 SDK 통합은 `build.zig`가 처리한다. 배포·업데이트 전략 전체는 [배포·업데이트 전략](distribution.md)을 단일 출처로 둔다. 공증 2회라 `macos-dmg`보다 느리므로 평소 개발엔 arm64 `macos-dmg`를 쓴다)
- GitHub Release draft-first/no-clobber 계약 검사: `zig build check-github-release-publication` (`tools/publish-github-release.sh`가 새 draft 생성 → exact-one DMG 무덮어쓰기 업로드 → 재다운로드 byte equality → publish 순서이고 기존 release 재사용·`--clobber`가 없음을 고정한다. 실제 GitHub Release를 만들지는 않는다.)
- session-host release workflow 권위 캡처 계약 검사: `zig build check-session-host-release-workflow` (`.github/workflows/release.yml`의 tag-only protected release job이 repository checkout 전에 runner-provided `gh`의 symlink-free canonical absolute path와 lowercase SHA-256을 exact step output으로 고정하는지 검사한다. `GITHUB_ENV`·checkout 뒤 PATH 재탐색·mutable Action ref를 거부하며, captured output의 validator 소비와 manifest/evidence publication은 후속 gate가 소유한다.)
- session-host GitHub draft creation 권위 집중 gate: `zig build test-session-host-release-adapter-github-draft-creation` (trusted tag context와 pinned CLI/token/single deadline에서 exact repository draft mutation을 한 번 실행하고 nonzero release ID·tag·source·title·mutable state를 strict response로 결속한다. existing release 재사용·caller endpoint/method/field·재시도·`--clobber`를 허용하지 않으며, remote create 뒤 local 실패는 결속된 ID가 있으면 cleanup-required, 없으면 remote-state-unknown terminal 상태로 보존한다. evidence/manifest authoring과 publish는 후속 gate가 소유한다.)
- session-host post-draft candidate file 권위 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-files` (pre-draft `CandidateAttestation`의 held DMG/frozen authority를 raw pathname 재개방 없이 revalidate하고 move-only draft ID와 trusted context에 결속한다. attestation owner lifetime/copy, context/draft/path drift, pathname·digest mutation과 copied/pre-owned owner를 Debug·ReleaseFast에서 검증한다. Apple product·evidence/manifest authoring은 후속 gate가 소유한다.)
- session-host post-draft candidate Apple product 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-product` (move-only candidate files가 차용한 pre-draft attestation의 DMG/frozen 관측에서 DMG expected와 tag version을 유도해 기존 DMG/Apple authority를 실행하고, child 전후 attestation source revalidation과 DMG 내부 executable SHA↔held frozen SHA를 결속한다. exact shared deadline, copied/pre-owned owner, path/work alias, mutation·observer·digest mismatch와 allocation unwind를 Debug·ReleaseFast actual filesystem에서 검증한다. 실제 hdiutil residue는 DMG authority E2E, artifact attestation과 evidence authoring은 후속 gate가 소유한다.)
- session-host source tree 권위 집중 gate(macOS): `zig build test-session-host-release-adapter-github-source-tree` (trusted context의 source commit으로만 pinned `gh` Git commit-object request를 만들고 응답 commit/tree SHA를 final-address move-only owner에 결속한다. exact shared deadline, CLI/capture/context/response drift, output alias, copied/pre-owned owner와 allocation unwind를 Debug·ReleaseFast에서 검증한다. evidence/manifest authoring과 publication은 후속 gate가 소유한다.)
- session-host candidate evidence identity 집중 gate(macOS): `zig build test-session-host-release-adapter-candidate-evidence-identity` (trusted context, canonical UUID v4, revalidated candidate product와 GitHub source-tree owner에서 `evidence.Common`과 designated-requirement digest를 final-address storage로 유도한다. caller scalar 재조립, copied/pre-owned owner와 UUID/context/product/tree/path drift를 Debug·ReleaseFast에서 검증한다. A/B profile·predecessor·leaf 조립/publication은 후속 gate가 소유한다.)
- session-host predecessor evidence identity 집중 gate(macOS): `zig build test-session-host-release-adapter-predecessor-evidence-identity` (authenticated role-A manifest·held manifest file·authenticated downloaded asset set에서 release/tag/source 및 manifest·DMG·frozen executable digest를 fixed `evidence.Predecessor` storage로 유도한다. attestation subject↔manifest file, manifest asset role↔downloaded inode와 source를 compose/revalidation 때 교차검증하고 copied/pre-owned/alias owner, role/source/asset 교환·누락·mutation을 Debug·ReleaseFast actual filesystem에서 거부한다. upgrade leaf aggregate·manifest/draft publication과 frozen U5 제품 E2E는 후속 gate다.)
- macOS CoreText font shaping/raster smoke 실행: `mise run macos-coretext-smoke` (창이나 GPU 없이 CoreText font resolve, glyph run 생성, `RendererState -> RenderFrame` 준비 계약, 제품 후보 `coretext_raster.zig` wrapper와 smoke native bridge를 통한 `GlyphRasterFrame` byte 생성, CPU bitmap raster를 확인한다)
- macOS CoreText smoke 계약 테스트: `mise run test-macos-coretext-smoke`
- macOS glyph texture smoke 실행: `mise run macos-glyph-texture-smoke` (창 없이 CoreText CPU bitmap을 Metal texture에 업로드하고 readback한다)
- macOS glyph texture smoke 계약 테스트: `mise run test-macos-glyph-texture-smoke`
- macOS glyph text smoke 실행: `mise run macos-glyph-text-smoke` (CoreText glyph texture를 실제 AppKit/CAMetalLayer 창에서 shader sampling으로 그리고 readback한다. `zig-out/maru-macos-glyph-text-smoke/glyph-text-frame.ppm` screenshot artifact도 남긴다. 창이 너무 빨리 닫히면 `MARU_GLYPH_TEXT_SMOKE_MS`로 노출 시간을 ms 단위로 늘린다. 기본 1500ms, 상한 600000ms)
- macOS glyph text smoke 계약 테스트: `mise run test-macos-glyph-text-smoke`
- 테스트: `mise run test`
- E2E 테스트: `mise run e2e`
- 오라클 비교 테스트: `mise run oracle`
- 외부 오라클(opt-in, libvterm 필요): `mise run oracle-ext`
- 외부 오라클(opt-in, Ghostty libghostty-vt 필요): `mise run oracle-ghostty`
- 외부 오라클(opt-in, Alacritty Rust dumper 필요): `mise run oracle-alacritty`
- vttest 시각 적합성 점검(opt-in, 수동): `mise run vttest` (Maru 앱을 띄운다 — 그 안에서 `vttest`를 직접 실행해 시각 점검한다. `brew install vttest`. [VT 적합성 테스트](conformance-testing.md) 참조)
- 빠른 스트레스 테스트: `mise run stress`
- 긴 opt-in 스트레스 테스트: `mise run stress-soak`
- 성능 예산 측정: `mise run perf`
- macOS PTY opt-in 테스트: `mise run pty` (macOS `openpty` controlled command, SurfaceRuntime routing, resize, reader close/reap, bounded queue stress, 그리고 `$MARU_INTERACTIVE_SHELL` 또는 `$SHELL`을 `-i`로 실행하는 interactive shell smoke를 검증한다. interactive shell smoke는 `tests/artifacts/integration/pty/interactive-shell.*` raw/screen/snapshot/summary artifact를 남기지만, 사용자 dotfile/prompt escape 영향을 받으므로 기본 `mise run check`에는 넣지 않는다)
- maru terminfo 검증(opt-in): `mise run terminfo-check` (`terminfo/`를 수정할 때 실행한다 — 컴파일 클린, Sync 2026 round-trip, Tc를 확인한다)
- maru terminfo 설치(opt-in): `mise run install-terminfo` (`~/.terminfo`에 설치한다, sudo 불필요. 설치 후 config에 `term = "xterm-maru"`를 쓴다)
- `maru ssh` 전파 smoke(opt-in): `mise run ssh-smoke` (`ssh localhost` 경유 — sshd/키가 없으면 skip한다. 먼저 `mise run build`가 필요하다)
- 포맷: `mise run fmt`
- 포맷 검사(변경 없이): `mise run fmt-check`
- facade import 경계 검사: `mise run check-boundaries` — 이 집계 step 은 boundary 테스트를 **Debug 로만** 돈다(예외 하나: `tests/boundary/imports.zig` 는 ReleaseSafe — 84개 판정자가 소스 트리를 걷고 스캔해 실행이 시간의 본체라 로컬 Debug 40초 → 7초, 안전 검사는 유지되고 제품 코드를 컴파일하지 않는다). **CI 는 이 step 을 인덱스 샤드 넷으로 나눠 병렬로 돈다**: `zig build check-boundaries -Dboundary-shard=i/4` 는 등록된 의존 목록을 인덱스 mod 4 로 골라 자기 몫만 돌고 `check-boundaries shard i/4: K of T steps` 한 줄을 찍으며, 전체는 `check-boundaries-all` 로 남는다. 옵션 없는 로컬 실행은 전체다. 워크플로의 `check-boundaries` 는 샤드 넷의 결론을 모으고 K 의 합이 T 와 같은지 다시 세는 집계 잡이다(아티팩트별 실행이 stderr 잠금으로 직렬이라 웜 캐시에서도 로컬 258초가 바닥이었다, 2026-09-06). 등록 68개 중 33개가 optimize 루프 안에서 두 모드로 링크되던 것을 Debug 하나로 걸렀다(2026-08-17). boundary 테스트는 46개 중 44개가 소스 파일을 문자열로 읽어 선언·caller 수를 세고 `maru` 를 import 하는 것이 0개라 최적화 모드가 결과를 바꾸지 않는다. 실측으로 `check` job 14분이 사실상 이 step 하나(13분 35초)였다. 충돌 마커 검사는 최상위 `.gitignore` read-only 외부 체크아웃 `references/`와 알려진 생성물·의존성 트리를 훑지 않는다. 개별 집중 gate(`test-session-host-*`)의 "Debug·ReleaseFast runtime N+boundary 1" 계약은 그대로이므로, 그 gate 를 직접 부르면 ReleaseFast boundary 도 돈다.
- external source digest 갱신: `mise run update-boundary-digest` — `check-boundaries`의 external reflection inventory는 대상 파일의 **비-test 토큰 전체**를 SHA-256으로 잠근다. 그래서 그 파일(특히 `app_session.zig`)을 건드리는 거의 모든 PR이 `tests/boundary/external_source_digests.zig`의 digest 한 줄을 다시 써야 하고, main이 같은 파일을 건드리면 **리베이스마다 또** 써야 한다. 이 태스크가 그 손작업만 없앤다(실패 메시지의 해시를 눈으로 옮겨 적을 필요가 없다). **`@field` 사이트 수(`count`)가 바뀌면 갱신을 거부하고 멈춘다** — 그건 digest 이동이 아니라 반사 접근 자체가 늘거나 준 것이고, 이 게이트가 잡으려는 바로 그 신호이기 때문이다. 갱신 뒤에는 그 파일의 관행대로 **사유 주석 한 줄**을 직접 남긴다(도구가 사유를 지어내지 않는다).
- config 문서 ↔ 로더 키 대조: `mise run check-config-docs`(문서가 실재하지 않는 키를 광고하는지 — [config 스키마](config-schema.md#문서--키-양방향-게이트))
- 문서 간 링크·절 참조 검사: `mise run check-doc-links` — 셋을 본다. ⑴ 링크 대상 파일 존재, ⑵ **`#절-앵커`가 그 문서 헤딩에서 실제로 생성되는지**, ⑶ **`foo.md §4.2`처럼 절 번호로 가리킨 참조가 실재하는지**(문서뿐 아니라 `src/`·`tests/`의 주석도 훑는다). 계약 문서를 여러 파일로 가르면 파일은 그대로 있고 절만 옮겨가므로, 파일 존재만 보는 검사는 통과하는데 링크는 죽는다(실측: 분할 한 번에 앵커 8건·절 참조 12건). 절을 다는 형식은 헤딩(`## 3.`)·볼드 문단(`**9.5.1 — …**`)·목록 라벨(`7. **(§9.7) …**`) 셋을 모두 인정하고, 참조는 파일명 **바로 뒤**의 §만 본다(사이에 문장이 끼면 진입점 참조다). 번호 절 체계가 없는 문서로의 `§N`은 판정하지 않는다 — 그건 줄 번호를 §로 쓴 표기 오용이라 이 게이트가 다룰 종류가 아니다. 아직 고칠 수 없는 기존 위반은 `tests/doc_links/links.zig`의 `known_broken`·`known_broken_sections`에 **사유와 해소 조건을 적어** 등재하고, 그 목록은 비우는 것이 목표다.
- 문서 → **코드** 좌표 검사: `mise run check-doc-line-refs` — 문서가 `` `심볼`(파일.zig:1234) ``로 코드를 지목한 자리가 아직 그 심볼을 가리키는지 본다(위 게이트가 문서 **사이**를 보는 것과 짝). 좌표는 손으로 관리하는데 코드는 자라므로 조용히 썩는다 — 2026-08-29 전수 대조에서 41건이 어긋나 있었고 최대 3,911줄 차이였다(원인 대부분은 `app_session.zig` 분해). **오탐이 이 게이트의 본체다**: 넓게 잡으면 곧바로 1,500건이 나오는데 대부분 거짓이라(문서가 사라진 것을 일부러 적고, 외부 API와 비교 대상 프로젝트 경로를 인용한다), ⑴ 심볼과 좌표가 40바이트 안에 붙어 있고 ⑵ 파일이 유일하게 풀리며 ⑶ 그 파일에서 정의가 **정확히 하나**일 때만 판정한다. 지금 지키는 인용은 8건이고 그 수를 테스트가 못 박는다 — 인용이 조용히 사라져 게이트 범위가 줄어드는 것을 막는다. 허용 오차는 ±25줄.
- 아이콘 SVG→coverage 재생성 동기 검사(opt-in): `mise run icons:check` (로컬에 `rsvg-convert`와 Python Pillow 필요; 기본 `check`는 외부 도구 없이 SVG SHA-256 manifest와 C/Zig registry를 검사)
- macOS AppSession 탐색기 성능 artifact gate: `mise run macos-file-explorer-perf` (전용 AppSession test 1개, 16,384 rows/1,000 events; Ubuntu에서는 skip되므로 별도 macOS PR CI job이 실행)
- 전체 확인: `mise run check`
- Zig 테스트 직접 실행: `zig build test`
- 세션 기본 전환 G1 config provenance 집중 gate: `zig build test-session-host-config-provenance`. Debug·ReleaseFast마다
  config aggregate carrier 2개와 loader provenance 2개, source boundary 1개를 exact-count한다. loader 행은 마지막 적용
  가능한 occurrence의 `absent | explicit_valid | explicit_invalid`, schema bool parser 단일 소유, foreign/current OS
  suffix 순서, 실제 파일의 missing/readable/unreadable/정확히 1 MiB/1 MiB+1을 검증한다. boundary는 G2 전 product
  consumer가 0이고 파일 write·notice·bootstrap이 시작되지 않았음을 고정한다.

- 영속 세션 호스트 CR0b connection incident gate: `zig build test-session-host-cr0b`. CR3b R1 gate를 상속하고 dependency-neutral `ConnectionIncident`의 exact 208-byte canonical payload, 256-byte envelope/32 KiB record storage 산술, closed `SourceSite`와 CR0a decision, host identity/outbound 결속, 120 detail+7 named+고정 other aggregation, repeat aggregate, digest 손상 거부, final-address service의 실제 fork-before-lock 거부와 동시 sequence 유일성·pending bitmap을 Debug·ReleaseFast에서 binding contract 6개+poison publication contract 7개+service transaction substrate 5개+Client incident operation prerequisite 5개+HostPool publication 11개+ClientSlot binding 7개+managed public caller 1개+prepared execution caller 1개+registered-operation deferred caller 1개+allocator callback deferred caller 1개+actual read-event pump caller 1개+actual outbound-RPC ambiguity caller 1개+AppSession named publication 5개(root/import sentinel 3개 포함 실제 8회)+GUI bootstrap 3개와 ABI prerequisite 3개(root/import sentinel 3개 포함 실제 9회)+daemon bootstrap prerequisite 1개+core component 20개+writer handoff component 11개+secure storage component 6개+runtime component 7개+boundary 1개로 exact-count한다. binding contract는 `Client -> ClientSlot -> HostAdapter -> HostPool` 방향의 pointer-free DTO와 closed lifecycle만 소유한다. storage는 strict detail/aggregate filename, 0700 current-uid directory, 0600 no-follow exclusive file, byte-exact idempotence와 1 MiB oldest-first prune를 닫는다. runtime component는 daemon process bootstrap의 canonical process identity, final-address heap owner, exact 한 writer, nonblocking wake, clean `joined|detached`, writer/clock/poll 실패의 `degraded_joined|degraded_detached`, process-monotonic 200 ms timeout과 detached backing 보존을 고정한다. Client binding은 두 actual product entrypoint가 공유하는 publication leaf가 HostPool capacity prepare 뒤 final Client address를 봉인하고 no-fail map publication하는 순서를 검증한다. 여섯 번째 gate는 process publisher 7개+최초/repeat suffix 10개+managed caller 전환 6개+GUI/daemon runtime 5개+subprocess proof-loss 4개+boundary 1개를 추가해 first-reason incident suffix를 닫는다.
- CR0b composite prerequisite는 위 gate 안에서 실제 Client operation·publisher lease·service mutex를 조합한 first/repeat 6개를 Debug·ReleaseFast exact-count한다. 별도 reconnect admission owner prerequisite 2개는 fixed-cap 64 inline row에서 first reconnect event의 exact-once admit/consume과 repeat·no-retry mutation 0을 검증한다. app-process publication owner가 first preflight→publication→Client terminalization→fd close→admit suffix를 유일하게 조합하며 repeat는 admission을 늘리지 않는다. 실제 scheduler consumer는 후속 gate 범위다. publisher exhaustion은 일반 aggregate에서 parent/child가 명시적으로 skip되고, canonical child artifact를 주입하는 전용 runner에서 authority 7개와 child 1개를 실행한다. 이 prerequisite만으로 suffix 10개와 scheduler consumer 완료를 뜻하지 않는다.
- CR0b GUI incident owner prerequisite 4개, managed public poison caller 1개와 prepared execution·registered-operation·allocator callback deferred·actual read-event pump·actual outbound-RPC ambiguity caller 각 1개는 module-static final-address app-global owner가 heap-pinned runtime과 inline registry를 exact once 설치하고 canonical coordinator를 호출함을 검증한다. caller 1은 closed reason/source/controller generation을 owner clock과 registered Client pin 아래 canonical `IncidentInput`으로 투영해 first/repeat, terminalization·fd close·reconnect admission을 닫는다. caller 2는 actual generation RPC가 operation 전 sealed timestamp를 받고 operation-held 실패에는 stack-local reason만 남긴 뒤 operation release 후 같은 publication port로 first suffix를 수행한다. caller 4는 같은 transaction의 checked allocator callback에서 public fence를 재진입하지 않고 `client_cleanup` source와 reason만 caller-final capture에 기록하며 caller 2 unwind 뒤 같은 suffix를 수행한다. caller 5는 actual generation `pumpDelta`의 첫 event/screen read 전에 capture를 결속하고 `client_read` source, 별도 reason presence, exact reason을 남긴 뒤 event/batch reservation·allocator scope·read callback 전체 unwind 후 같은 suffix를 수행한다. caller 6은 actual generation `resize`가 pre-existing pending outbound를 nonblocking socket에 부분 전송한 뒤 `EAGAIN`으로 ambiguity를 확정하고, operation-held snapshot에 partial offset/length·남은 queue bytes를 봉인한 뒤 unwind 후 같은 suffix를 수행한다. 실제 scheduler consumer는 아직 완료 증거가 아니다. allocator/create 실패는 whole owner pristine으로 rollback한 뒤 같은 owner를 재사용하며, active lease 종료는 즉시 실패 뒤 release·retry joined와 200 ms deadline의 detached backing 보존을 각각 실행한다. AppHost의 실제 termination 순서와 전체 outcome matrix를 닫는 bootstrap 5 완료 증거도 아니다. 실제 AppSession current-first, restore-first→current, multi-window 제품 entrypoint 3개와 조기·foreign ABI 거부 뒤 joined/replay, runtime failure의 degraded/replay, active-lease timeout detached/replay prerequisite 3개는 별도 GUI gate가 실행한다. joined prerequisite는 실제 AppSession init/deinit과 owned HostAdapter/Client pool을 함께 사용하며 live session, residual runtime, reservation, close operation, close sweep 각각에서 backend graph와 incident latch를 보존한 뒤 canonical settlement로 수렴한다.
- CR0b managed caller 2·3·4·5·6 exact 각 1은 app-process owner가 bootstrap 뒤 게시하고 termination admission 전에 revoke하는 process-sealed publication port를 actual generation failure ingress에 연결한다. caller 2는 prepared execution의 operation 전 timestamp receipt, operation pin 아래 stack-local poison capture와 pointer-free handoff, operation release 뒤 canonical publication/terminalization 순서를 실제 socket EOF로 실행한다. caller 3은 event take registered operation 전 timestamp receipt를 받고 operation 안 corruption을 reason/fd mutation 없이 caller-final capture에 기록한 뒤, operation 반환 후 같은 canonical port로 게시한다. caller 4는 actual response allocation의 checked allocator callback이 `Client.poison(.local_resource_exhausted)`을 호출해 `client_cleanup` source를 capture하고, callback 중 mutation 0과 caller 2 unwind 뒤 exact 한 canonical publication을 검증한다. caller 5는 actual `pumpDelta` 전체 read 구간이 public mutation fence 아래 socket EOF를 별도 presence+exact reason으로 capture하고, event/batch/allocator/callback unwind 뒤 `client_read` incident를 게시한다. caller 6은 actual `resize`의 pre-existing outbound partial write/EAGAIN을 held operation에서 `client_response` handoff로 봉인하고, operation release 뒤 partial snapshot을 보존한 canonical incident를 게시한다. attachment/runtime/allocator에 publisher·registry·incident runtime pointer를 저장하거나 managed reason-only poison으로 fallback하지 않는다. actual scheduler consumer는 후속 gate 범위다.
- 영속 세션 호스트 CR1 scheduler admission gate: `zig build test-session-host-cr1`. CR0b gate를 상속하고 Debug·ReleaseFast마다 production-type scheduler 4개와 boundary 1개를
  exact-count한다. bounded semantic stream 오류의 closed stream scope·usable transport와 admission 0, 상속한 actual partial read+write admission의 sealed dispatch
  schedule exact once, writer `degraded` 뒤 disk wait/retry 0, `scheduled|retry_later|discarded_stale` closed transition과 copy/replay/
  wrong-thread 거부를 검증한다. CR1은 socket connect, adapter 교체, runtime generation publish, workspace write를 하지 않으며 실제
  reconnect 성공은 CR4 증거가 아니다.
- 영속 세션 호스트 CR2a generation extraction gate: `zig build test-session-host-cr2a`. CR1 gate를 상속하고 Debug·ReleaseFast마다
  `RemoteGeneration` production-type 2개와 source boundary 1개를 exact-count한다. generation-owned field 12개, stable-shell
  field exclusion, Debug/ReleaseFast nested 정렬 증가 exact 16바이트(4,096 runtime에서 64 KiB)와
  잔류 owner의 closed inventory, distinct-value extraction/deinit parity를 검증한다. 이 gate는 stable proxy, ordered `InputOwner`,
  `PreparedReconnect`, 제품 generation 교체 또는 실제 socket reconnect를 포함하지 않는다.
- 영속 세션 호스트 CR2b stable screen proxy gate: `zig build test-session-host-cr2b`. CR2a gate를 상속하고 Debug·ReleaseFast마다
  `StableScreenSource` 6개, actual `RemoteRuntime` wiring 1개와 source boundary 1개를 exact-count한다. bounded unavailable marker, stable
  `ScreenSource.ctx`, exact pinned-target unlock, nested lock 선행 거부, writer-pending 뒤 late-reader 차단, checked generation
  zero/stale/skip/max 및 copied/foreign writer 거부, render 임계구역/publish wait count·total·max 계측, destroy-vs-borrow drain과 actual `RemoteRuntime`
  attach/detach/deinit sole wiring을 검증한다. 이 gate는
  fake target publish까지만 소유하며 실제 reconnect socket/`PreparedReconnect`와 old `RemoteGeneration` 교체·reclaim은 CR2e/CR4다.
- 영속 세션 호스트 CR2c input owner facade gate: `zig build test-session-host-cr2c`. CR2b gate를 상속하고 Debug·ReleaseFast마다
  neutral `InputOwner` 3개, `TermRuntimeBackend` opaque-handle 결속 1개, local parity 1개(root/import sentinel 7개 포함
  app artifact 실제 12회), remote parity 1개와 source boundary 1개를
  exact-count한다. 기존 blocking/nonblocking/core-command leaf의 성공·오류·partial progress를 그대로 전달하며,
  제품 facade caller 0을 유지한다. queue/epoch/sequence/paused paste storage 이동과 Window/reconnect 보존은 CR2d 이후 범위다.
- 영속 세션 호스트 CR2d1 remote input queue gate: `zig build test-session-host-cr2d1`. CR2c gate를 상속하고
  `InputOwner.enqueueBatch`의 local `caller_owned`와 remote `backend_owned`, paste·IME 확정·OSC52 closed kind,
  nonzero epoch/checked sequence, IME commit+replay atomic append, LF→CR, cap/OOM/exhaustion mutation 0, blocked-wire retained
  queue와 ordered drain golden trace, remote AppSession queue 0을 Debug·ReleaseFast에서 InputOwner 2개+local 1개(root/import sentinel 7개 포함
  app artifact 실제 10회), remote backend 1개, RemoteRuntime 1개, AppSession routing 1개(root/import sentinel 3개 포함
  실제 4회), source boundary 1개로 exact-count한다. key/control merge, event cursor,
  cross-Window transfer와 reconnect `PausedPaste`는 CR2d2~d4/CR2e 범위다.
- 영속 세션 호스트 CR2d2 ordered key/control gate: `zig build test-session-host-cr2d2`. CR2d1을 상속하고
  remote blocking key·paste-family batch·scroll-to-bottom·core-command를 closed six-kind stable transcript의 같은
  epoch/checked sequence로 합친다. blocked-wire golden physical order, byte/control record 순차 retire,
  sequence exhaustion mutation 0과 control transcript drift의 wire 전 거부를 RemoteRuntime 2개와 source boundary 1개로
  Debug·ReleaseFast exact-count한다.
  event cursor, Window 이동·close, reconnect sealing은 CR2d3~d4/CR2e 범위다.
- 영속 세션 호스트 CR2d3 stable event cursor gate: `zig build test-session-host-cr2d3`. CR2d2를 상속하고 stable shell의
  observer-generation baseline, BEL/OSC52 exact-once cursor, write prepare/commit retry와 AppSession Window-local cursor 0을
  Debug·ReleaseFast에서 cursor 2개+RemoteRuntime 1개+실제 AppSession routing 3개(root sentinel 포함 actual 6)+boundary 1개로
  exact-count한다. cross-Window move/close parity는 CR2d4 범위다.
- 영속 세션 호스트 CR2d4 cross-Window stable state gate: `zig build test-session-host-cr2d4`. CR2d3을 상속하고
  remote Term의 옛 Window paste transfer 제거와 실제 workspace move/source close·full-window merge parity를
  AppSession 2개(root/import sentinel 3개 포함 actual 5)+boundary 1개로 Debug·ReleaseFast exact-count한다.
  local caller-owned queue transfer는 유지하며 reconnect publish/reclaim은 CR2e 범위다.
- 영속 세션 호스트 CR2e-a reconnect reducer gate: `zig build test-session-host-cr2e-a`. CR2d4를 상속하고
  pointer-free `ReconnectReducer`의 job/runtime/local/mutation/close 상태, clean·ambiguous precommit 실패,
  controller evidence 없는 publish 거부, frozen retry reservation, exact terminal summary와 close 경쟁을
  reducer 5개+boundary 1개로 Debug·ReleaseFast exact-count한다. 제품 caller는 0이며 mutation sealing,
  `PausedPaste`, generation slot, 실제 `PreparedReconnect`와 executor parity는 CR2e-b~e 범위다.
- 영속 세션 호스트 CR2e-b mutation sealing gate: `zig build test-session-host-cr2e-b`. CR2e-a를 상속하고
  final-address owner-thread mutation lease 64개의 sealing/drain/replay 거부, kind별 paused metadata, paste당 1 MiB·runtime당
  1개·app-global 8 MiB atomic budget, `Clock.boot` 10분 TTL, resend staging 포함 peak accounting과
  `std.crypto.secureZero` 기반 prepared staging deinit/discard/expiry cleanup을 substrate 4개+boundary 1개로 Debug·ReleaseFast
  exact-count한다. 제품 caller와 실제 stable queue splice는 0이며 CR2e-c~e 전에는 reconnect 제품 동작 완료가 아니다.
- 영속 세션 호스트 CR2e-c generation slot gate: `zig build test-session-host-cr2e-c`. CR2e-b를 상속하고 stable shell의
  최초 inline node와 reconnect별 heap node가 같은 payload를 보존하는 final-address 저장소, current→retiring→tombstone/reclaimed,
  retiring 1개 backoff, inline/heap final-address payload 초기화, allocator OOM/empty·owned abort의 current 보존,
  copied/stale/cross-slot authority 거부를 4개+boundary 1개로
  Debug·ReleaseFast에서 검증한다. 실제 `RemoteGeneration` prepare/publish/destructor 제품 배선과 app-global byte/count budget은
  CR2e-d/e 범위다.
- 영속 세션 호스트 CR2e-d prepared reconnect gate: `zig build test-session-host-cr2e-d`. CR2e-c를 상속하고 실제
  `RemoteGeneration`을 inline/heap node final address에서 초기화하는 `PreparedReconnect`, stable screen writer gate 안의
  slot current+screen target 동시 publish, old generation in-place exact-one destructor, abort/current 보존과 allocator
  fail-index, copied/stale token 거부를 제품 owner 4개+boundary 1개로 Debug·ReleaseFast에서 검증한다. 기존
  `RemoteRuntime.generation` 호출 전체의 slot accessor 전환, reducer Decision executor parity, close 경쟁, app-global
  candidate/retiring count·byte budget과 peak RSS artifact는 CR2e-e 범위이며 executor caller는 아직 0이므로 CR2e 전체 완료나
  제품 reconnect publish를 뜻하지 않는다.
- 영속 세션 호스트 CR2e-e1 runtime generation access gate: `zig build test-session-host-cr2e-e1`. CR2e-d를 상속하고
  `RemoteRuntime` 내부 세대 접근의 단일 current accessor, `RemoteTermBackend`의 목적별 narrow facade, RPC pre-decode callback의
  attachment→generation→runtime `@fieldParentPtr` 역산 0을 runtime 2개+boundary 1개로 Debug·ReleaseFast exact-count한다.
  이 gate는 slot 저장소를 활성화하기 위한 물리적 선행 조건이다. 실제 slot-backed initial/current 전환과 reducer executor parity는
  CR2e-e2, close/mixed outcome과 app-global budget·peak RSS는 CR2e-e3 범위이므로 e1만으로 CR2e 완료나 제품 reconnect를 뜻하지 않는다.
- 영속 세션 호스트 CR2e-e2a product slot gate: `zig build test-session-host-cr2e-e2a`. CR2e-e1을 상속하고
  제품 `RemoteRuntime`의 최초/current 저장소를 actual `GenerationSlot(RemoteGeneration)`으로 바꾸며, final-address inline
  payload generation 2와 stable screen target의 결속, 제품 detach teardown의 payload exact-once 정산을 runtime 2개+boundary
  1개로 Debug·ReleaseFast exact-count한다. reducer Decision/executor 전수 parity와 reachable sequence는 e2b,
  close/mixed outcome·app-global budget·peak RSS는 e3 범위이므로 e2a만으로 CR2e 완료나 reconnect 제품 진입을 뜻하지 않는다.
- 영속 세션 호스트 CR2e-e2b product executor gate: `zig build test-session-host-cr2e-e2b`. CR2e-e2a를 상속하고
  제품 `RemoteRuntime`의 final-address executor와 inline `PreparedReconnect`, reducer state의 effect-success-last publication,
  actual candidate prepare/abort/publish/retiring reclaim, 31개 Decision의 closed generation-effect parity와 initial state에서 도달 가능한
  canonical sequence 전수를 runtime 4개+boundary 1개로 Debug·ReleaseFast exact-count한다. inline executor 증가는 runtime당
  256바이트, 4,096-runtime 상한에서 1 MiB이며 runtime size golden으로 고정한다. 현재 generation mutation이 없는 decision은
  `retain`으로만 분류한다. mutation seal·authority/retry/close effect의 실제 제품 결속, 외부 reconnect ingress,
  close 경쟁·mixed outcome·app-global count/byte budget·peak RSS는 e3 범위이므로 e2b만으로 CR2e 완료를 뜻하지 않는다.
  다음 e3는 e3a1 actual candidate/retiring allocator-ledger logical charge, e3a2 fixed inventory bound와 별도
  ReleaseFast RSS 측정 prerequisite, e3b 실제 동시 runtime 모델 기반 app-global cap/cap+1 및 budget lease 아래
  mutation·authority·retry·close actual effect, e3c sole external ingress와
  close 경쟁·mixed outcome 순서로 닫는다. e3a 측정 전 reconnect publish를 제품 entrypoint에서 열거나 임의 상한을
  정책으로 고정하지 않는다.
- 영속 세션 호스트 CR2e-e3a1 resident ledger gate: `zig build test-session-host-cr2e-e3a1`. CR2e-e2b를 상속하고
  empty-screen 제품 fixture에서 `RemoteGeneration` candidate prepare가 추가하는 structural base allocation 1개와
  CR6d typed event-payload allocator와 CR5b-2a retirement preparation owner 반영 뒤 Debug 3,504바이트/ReleaseFast 3,488바이트,
  abort의 baseline 복원, publish 동안 current+retiring 동시 resident, reclaim 뒤 heap current 1개만 남는 두 연속
  reconnect, owner teardown final 0을 runtime 2개+boundary 1개로 exact-count한다. 이는 가변 screen/metadata를
  포함하지 않는 allocator logical base lower bound이며 raw process RSS나 app-global cap을 대신하지 않는다.
  실제 bounded workload의 별도 ReleaseFast child RSS와 structural tracked bound는 e3a2, 근거 기반 정책 cap은 e3b다.
- 영속 세션 호스트 CR2e-e3a2 resident budget/RSS gate: `zig build test-session-host-cr2e-e3a2`. CR2e-e3a1을
  상속한다. generation당 구조적 charge는 host의 `base_update_max_bytes`이고 fixed inventory는 reconnect mutation
  lease와 같은 64개다. 둘의 곱은 표현 가능한 tracked bound일 뿐 app-global 정책 예산이 아니며, 실제 정책은 e3b의
  동시 runtime 모델에서 결정한다. fixed entry
  budget 5개와 typed JSON validator 2개를 Debug·ReleaseFast로, actual 64 current→64 candidate+retiring workload
  1개와 별도 exec ReleaseFast child artifact 1개 + parent/watchdog artifact 2개를 ReleaseFast로, source boundary 1개를 Debug·ReleaseFast로
  exact-count한다. child는 parent-minted 128-bit run nonce, `LOCAL_PEERPID`, stream type을 검증하고 stdio/FD 198 외
  inherited descriptor를 닫는다. RSS artifact는 `tests/artifacts/perf/session-host-reconnect-rss-macos.json`이며 독립 validator가
  baseline/pressure 원시 표본 각 7개의 process identity와 중앙값, logical/RSS/footprint delta, 64 MiB allocator
  측정 tolerance, child exit 0과 actual final generation/bytes/allocation zero receipt를 다시 계산한다. 64 MiB는
  allocator overhead의 측정 근거나 e3b 정책 예산이 아니라 gross anomaly를 잡는 conservative harness tolerance다. e3b의
  budget lease 실제 stable-owner 결속과 e3c의 sole ingress·close/mixed outcome은 아직 포함하지 않으므로 이 gate만으로
  CR2e 또는 CR4 완료를 뜻하지 않는다.
- 영속 세션 호스트 CR2e-e3b1 reconnect admission budget gate: `zig build test-session-host-cr2e-e3b1`. CR2e-e3a2를
  상속하고 process-global queued admission cap 64, active resident entry cap 8, GUI reconnect 전용 128 MiB resident
  byte cap을 Debug·ReleaseFast에서 exact-count한다. 작은 charge는 active 8개까지 성공하고 아홉 번째는 mutation 0이며,
  `base_update_max_bytes` charge는 7개까지 성공하고 8번째는 byte cap 때문에 mutation 0이다. 이 gate는 typed budget 거부와
  정책 상한, final-address/PID/process-nonce/incarnation/domain lease와 fork/same-address replay 거부를 닫는다.
  sealed admission queue 보존·후속 drain 및 actual `ReconnectProductExecutor` 결속은 e3b2,
  외부 ingress와 close 경쟁은 e3c다. daemon의 128 MiB budget과는 숫자만 같고 owner/process 회계를 공유하지 않는다.
- 영속 세션 호스트 CR2e-e3b2 admission drain gate: `zig build test-session-host-cr2e-e3b2`. e3b1을 상속하고
  process-global sealed admission row가 8-entry/128 MiB resident budget에 막히면 같은 projection으로 admitted 상태를
  보존하고, lease release 뒤 actual `RemoteRuntime`의 final-address executor에 다시 결속되는지 Debug·ReleaseFast에서
  drain 1개+executor 1개+boundary 1개로 exact-count한다. executor는 candidate lease를 mutation seal·authority commit의
  retain 구간에 보존하고 actual generation publish 뒤 current로 바꾼 다음 retiring reclaim과 함께 final zero로 회수한다.
  외부 reconnect ingress와 close 경쟁·mixed outcome은 e3c 범위다.
- 영속 세션 호스트 CR2e-e3c1 coordinator ingress gate: `zig build test-session-host-cr2e-e3c1`. e3b2를 상속하고
  final-address reconnect-only `SessionHostCoordinator`가 AppSession frame의 sole drain을 소유하는지 Debug·ReleaseFast로
  exact-count한다. queue/budget은 `AppProcessIncidentOwner`, runtime map은 `RemoteTermBackend`에 그대로 남고 coordinator는
  raw pointer를 저장하지 않으며 process identity·owner thread·backend singleton generation/seal을 매 turn 재검증한다.
  copied/stale/foreign-thread coordinator와 backend reinstallation ABA는 queue·budget·runtime mutation 0이다. typed external event와
  close/mixed outcome은 e3c2/e3c3, actual socket receipt는 CR4이므로 e3c1만으로 제품 reconnect 완료를 뜻하지 않는다.
- 영속 세션 호스트 CR2e-e3c2 typed external receipt gate: `zig build test-session-host-cr2e-e3c2`. e3c1을 상속하고
  Debug/ReleaseFast에서 coordinator receipt 1개와 boundary 1개를 실행한다. pointer-free evidence의 exact projection과
  direct-release receipt의 final address, process identity,
  backend singleton/runtime row/reducer projection, copy·replay·stale mutation 0을 검증한다. 실제 socket issuer와
  `connectExistingHost`는 CR4이므로 이 gate는 wire reconnect 완료 증거가 아니다.
- 영속 세션 호스트 CR2e-e3c3 typed close event gate: `zig build test-session-host-cr2e-e3c3`. e3c2를 상속하고
  Debug·ReleaseFast에서 close receipt 1개와 boundary 1개를 exact-count한다. coordinator가 pointer-free termination request,
  reconnect quiesced, timeout, abandon event를 current backend/runtime row에서 직접 projection하고 keyed one-shot receipt로
  봉인한다. preserve-old, paused notice, publish-new, retry freeze, terminal finish의 다섯 actual executor effect와 candidate/current
  lease 정산을 검증하며 invalid/copy/replay/stale-row/expired/deadline-before-timeout은 runtime·budget mutation 0이다. e3a2의
  별도 PID RSS/final-zero artifact를 상속하지만 actual socket close-event issuer와 `connectExistingHost`는 CR4 범위다.
- 영속 세션 호스트 CR3b R2a detach placeholder gate: `zig build test-session-host-cr3b-r2a`. CR2e-e3c3을 상속하고
  Debug·ReleaseFast에서 production `RemoteGeneration` R2a 행 2개, stable proxy reader exclusion 1개, Client raw-lifecycle
  fail-close 1개와 source boundary 1개를 exact-count한다. committed
  admission-close permit과 stable proxy writer gate를 결속해 `live -> unavailable`과 exact current Client의 detached tombstone을
  함께 게시하고, 실제 socket fd·poison 상태·connection generation을 보존한다. wrong generation preflight는 proxy·permit·Client
  mutation 0이다. 이 dormant leaf의 제품 caller는 0이다. final-address cleanup handle과 callback/fd close, 새 Client
  allocation/current 교체, retired Client destroy는 각각 R2b·R2c·R3 범위이며
  이 gate만으로 R2 또는 실제 reconnect 완료를 뜻하지 않는다.
- 영속 세션 호스트 CR3b R2b final cleanup handle gate: `zig build test-session-host-cr3b-r2b`. R2a를 상속하고
  Debug·ReleaseFast에서 production `RemoteGeneration` cleanup 행 3개, ClientSlot hostile 행 1개와 source boundary 1개를
  exact-count한다. admission close 뒤 caller-owned final-address handle이 실제 socket fd, nonzero pending frame와 allocator,
  external-mode deinit reservation을 봉인한다. stable proxy writer gate에서는 Client의 fd/pending owner만 handle로 이동하고,
  callback-bearing external cleanup과 close/free는 gate 밖에서 exact once 수행한다. copied handle, wrong generation과
  Client-owned allocation에 겹친 handle destination은 proxy·Client·permit·resource mutation 0이며 abort 뒤 admission을 다시 열 수 있다. 이 dormant leaf의 제품 caller는 0이고,
  새 Client allocation/current 교체와 retired Client destroy는 각각 R2c·R3 범위다. 따라서 이 gate만으로 R2 또는 실제
  reconnect 완료를 뜻하지 않는다.
- 영속 세션 호스트 CR3b R2c current publication gate: `zig build test-session-host-cr3b-r2c`. R2b를 상속하고
  Debug·ReleaseFast에서 final-address `PreparedClientReplacement`가 완성된 새 Client node와 checked-monotonic
  connection generation을 registry/current pointer에 함께 게시하는 성공 행과 copied-handle abort/old-registry 복구 행 2개,
  HostAdapter compatibility facade 행 1개, source boundary 1개를 exact-count한다.
  prepare는 usable한 live fd를 가진 replacement만 받아 no-fail publish의 전제를 미리 닫는다.
  게시 전 old registry admission은 닫히고 게시 뒤 old node는 detached retired inventory에 남으며 R3 전에는 destroy하지 않는다.
  managed incident binding은 새 final Client 주소와 generation으로 다시 봉인하고, 게시 뒤 첫 attachment reservation도 새
  current generation을 발급한다. prepared seal은 unpublished candidate 전체 backing digest를 결속해 publish/abort 전
  wire·codec·binding·owner graph 변조를 거부한다. 두 번째 행은 candidate codec drift와 max generation의 checked-add 실패도 mutation 0으로 고정한다.
  이 dormant leaf의 제품 caller는 0이고,
  R3 reclaim과 CR3c 실제 reconnect integration은 아직 완료 증거가 아니다.
- 영속 세션 호스트 CR3b R3 retired reclaim gate: `zig build test-session-host-cr3b-r3`. R2c를 상속하고
  Debug·ReleaseFast에서 exact 2-slot retired inventory의 generation 1→2→3 publication, cap 2 세 번째 prepare mutation-0 reject,
  pure Client readiness와 final-address `PreparedRetiredClientReclaim`, generation 1 첫 tick 뒤 generation 4 게시와 generation 2→3 tick destroy/final zero 행 1개,
  copied/stale/digest-readiness drift destroy-0 행 1개, HostAdapter tick-end facade 1개와 source boundary 1개를 exact-count한다.
  reclaim no-fail suffix만 Client graph·node registry·accounting과 allocator destroy를 소유한다. 이 R3 gate 자체는 제품 caller를
  실행하지 않으며 sole 제품 caller와 CR3c `RemoteGeneration` integration은 CR3c2 gate가, socket reconnect는 CR4가 소유한다.
- 영속 세션 호스트 CR3c C1 generation publication integration gate: `zig build test-session-host-cr3c-c1`. R3를 상속하고
  Debug·ReleaseFast에서 old attachment terminalization→admission close→R2a/R2b/R2c→forward-recovery candidate prepare를 실제 실행하고,
  R2c가 게시한 `PreparedClientReplacement` receipt와 production `RemoteGeneration` candidate가 같은 HostAdapter 및 exact
  old/next connection generation을 공유하고 R2a placeholder와 candidate shell generation이 같을 때 stable screen writer gate 안에서 screen target과 RemoteGeneration slot을
  게시하는 성공 행 1개, adapter/generation/copy drift가 screen/RemoteGeneration mutation 0이고 published Client graph를 보존하는 행 1개와 source boundary 1개를
  exact-count한다. 양쪽 generation 1 owner는 retiring으로 보존하고 destroy는 0이다. ordered retiring reclaim/final zero는 CR3c2,
  actual `connectExistingHost` socket issuer는 CR4이므로 이 gate만으로 CR3c 또는 실제 reconnect 완료를 뜻하지 않는다.
- 영속 세션 호스트 CR3c C2 ordered retiring reclaim gate: `zig build test-session-host-cr3c-c2`. C1을 상속하고
  Debug·ReleaseFast에서 retiring `RemoteGeneration`과 oldest retired Client의 exact connection generation을 final-address
  prepared authority로 함께 고정한다. process PID/nonce, owner·slot·node incarnation을 함께 검증하고 old generation attachment가
  이미 terminalized 된 경우만 no-fail suffix에 admit한다. 성공 행은 RemoteGeneration attachment·observation teardown 뒤 같은 tick-end turn에서
  Client node를 회수하고 양 inventory final zero를 검증한다. hostile 행은 generation mismatch, copied receipt, prepare 뒤
  RemoteGeneration drift를 첫 destroy 전에 거부하고 두 owner를 보존한다. 제품 source boundary 1개를 함께 exact-count한다.
  이 gate로 CR3c의 구조적 publication/reclaim integration은 닫히지만 actual `connectExistingHost` socket reconnect는 CR4다.
- 영속 세션 호스트 CR4a observer candidate prerequisite gate: `zig build test-session-host-cr4a`. CR3c2를 상속하고
  Debug·ReleaseFast에서 CR3c의 unavailable shell과 이미 게시된 fresh Client replacement를 같은 `HostAdapter`에 결속한 뒤,
  observer attach와 initial snapshot을 final-address RemoteGeneration candidate 안에서 실행한다. typed reject는 candidate와
  attachment 권위만 정산하고 게시된 Client를 usable로 보존한다. EOF는 동일 Client node·generation과 unavailable shell을
  보존하되 해당 Client transport를 fail-close하므로 다음 replacement 시도가 필요하다. 같은 gate의 screen/server prerequisite
  2개는 initial snapshot sequence 0, 이후 resync/fallback snapshot과 delta exact +1, output admission 전 rollback mutation 0과 commit 뒤 subscription frontier 전진을
  검증한다(최적화 모드당 observer candidate/catch-up 3 + frontier 2 + barrier contract 2 + catchup stage contract 2 + decoded-cell projection 1 + catchup byte-cap leaf 1 + host pending state 1 + host capability/subscription owner 1 + actual catchup response control-queue admission 1 + host frontier/screen+barrier queue admission 1 + client barrier demux 6 + poll-owner process-seal 1 + restore fresh-exec process-seal bootstrap 1 + MRSH kind/header 2 + bounded exact-host connect 1 + backend-owned connect job 6 + boundary 1). catchup stage contract는 일반 inbox와 별도로 batch 64개, encoded 16 MiB, decoded cell 1,048,576개 상한과 final-address receipt storage를 고정한다. host frontier 행은 `RuntimeManager`가 core-lock projection과 immutable frontier를 함께 발급하고, pending identity의 screen frame 뒤 fixed barrier를 같은 subscription batch 마지막에 실제 queue admission한 뒤에만 base/frontier와 admitted row를 함께 commit하며 copy/PID/process-nonce/Client-address/ConnectionKey/thread drift와 prepare rollback을 mutation 0으로 거부한다. client demux 행은 negotiated capability를 hello provenance에 고정하고, RPC/sibling screen 사이의 barrier를 기존 Client demux의 bounded inbox에 보존한 뒤 하나의 absolute deadline 아래 exact stream/identity만 소비한다. duplicate/cap+1/OOM과 buffered-expired deadline은 fail-close하고 socket flags는 성공과 복원 실패에서 exact 정산한다. actual socket candidate 행은 snapshot 0 뒤 delta 1과 exact barrier를 적용해 final-address staged receipt를 만들고, gap·malformed delta·batch/cell cap+1·missing barrier는 receipt 0과 새 Client fail-close로 닫는다. byte-cap leaf는 마지막 초과 batch를 화면 apply 전에 회수하며 accounting mutation 0을 검증한다. actual issuer 제품 행은 실제 daemon manifest/socket을 `connectExistingHostUntil`로 통과하고 fresh Client를 final-address `RemoteTermBackend` job 안에 exact once 보존한 뒤, 같은 adapter의 unavailable shell 전환과 Client replacement를 exact once 게시한다. 같은 job은 connect에서 발급한 absolute deadline을 observer attach·snapshot·catch-up barrier까지 재생성 없이 전달하고 final-address candidate와 staged receipt를 소유한다. replay·runtime/receipt/state drift는 mutation 0이며 deadline 만료는 candidate 전에 sealed failure와 새 Client fail-close로 닫고, staged receipt 만료 뒤에는 제품 consume 0이지만 canonical cleanup은 가능하다. backend 종료는 terminal RemoteGeneration 뒤 retired Client 순서로 정산한다. replacement node OOM은 forward-failed 상태로 봉인해 rollback·재시도를 금지한다. allocator fail-index 행은 connect/job, replacement node, observer/snapshot/delta/staged receipt의 첫 성공+1까지 순회하며 각 반복 뒤 retired Client·pin·cleanup/batch registry·barrier inbox와 allocator bytes final 0을 검증한다. local socket idle은 caught-up 증거가 아니다. 이 prerequisite는 status/takeover, RemoteGeneration publication, input/forced resize를
  완료하지 않으며 CR4b/CR4c가 각각 소유한다.
  중립 contract prerequisite로 시작한 host/client identity 분리와 fixed barrier DTO는 이제 host issuer가 소비한다.
  MRSH kind/header의 generic admission은 닫힌 채 host 전용 sealed batch만 subscription queue에 들어간다.
  실제 global queue-pressure 거부도 queued prefix·base/frontier/pending mutation 0으로 닫고, 화면 변화가 없는 제품 turn은 idle 성공 대신 barrier-only batch exact 1을 admit한다. projection/new-base/barrier/frame/list preparation의 allocation fail-index도 성공점까지 pending/frontier mutation 0으로 검증한다. candidate-local staged receipt는 actual backend issuer가 소유하며 CR4a 완료 증거에 포함된다.
  bounded issuer만 빠르게 재검증할 때는 `zig build test-session-host-cr4a-issuer`를 사용한다. Debug·ReleaseFast 각각 실제
  daemon/manifest/socket connect 1개와 backend job ownership/replacement/candidate/fail-index 6개를 exact-count한다. actual issuer는
  typed reject를 usable terminal job으로, deadline·transport·protocol failure를 exact first-poison reason의 fail-closed job으로 구분한다.
  `RemoteTermBackend` host job의 allocator fail-index는 host당 same-adapter replacement·staged receipt exact 1, 단일 absolute deadline,
  pre-publication graph mutation 0, post-publication Client fail-close와 candidate/receipt/ledger final zero를 첫 성공+1까지 검증한다.
  이 focused gate와 전체 CR4a/boundary gate가 green이면 CR4a를 완료로 세며 CR4b takeover와 CR4c publication은 여전히 미완료다.
- 영속 세션 호스트 CR4b controller takeover gate: `zig build test-session-host-cr4b`. CR4a를 상속하고 현재 제품
  slice는 Debug·ReleaseFast마다 mutation owner 1개, paused paste policy 2개, stable queue seal 2개, actual socket takeover 2개,
  actual backend job 3개와 boundary 1개를
  exact-count한다. CR4a placeholder publication은 mutation owner를 old generation에 남겨 새 input과 queue pump를 막으면서
  기존 queue를 보존한다. staged receipt 뒤 active lease drain을 기다린 다음 key/control/IME 원문은 secure-zero하고 metadata만,
  완전한 paste 하나만 runtime 1 MiB/app-global 8 MiB/10분 TTL owner로 이동한다. backend job은 canonical mutation digest를
  final-address seal에 넣고 `candidate_staged -> mutation_sealed`를 exact once 게시한다. 같은 absolute deadline 아래
  `controller.status`와 generation-CAS `controller.takeover`를 각각 최대 한 번 실행하며, 성공은 staged observer를
  local mutation 권위가 없는 quarantined `controller_evidenced` receipt로 바꾼다. conflict는 usable Client를 보존하고,
  response-loss/pre-takeover failure는 Client를 fail-close한 뒤 candidate를 정산한다. CR4c RemoteGeneration publication,
  cleanup-binding/controller role promotion, forced resize와 input은 아직 완료하지 않는다.
- 영속 세션 호스트 CR4c C1 controller-binding promotion gate: `zig build test-session-host-cr4c-c1`. CR4b를 상속하고
  Debug·ReleaseFast actual backend job에서 `controller_evidenced` observer의 cleanup registry RPC identity, transport binding,
  attachment-local role을 같은 controller generation으로 final-address 승격한다. success 뒤에도 stable shell은 unavailable이고
  mutation seal과 candidate ownership은 유지되며 public input은 Unauthorized, generation publication·forced resize·reclaim은 0이다.
  C2의 candidate 전용 forced-first-resize, CR3c stable screen+RemoteGeneration publication, 새 generation input과 ordered reclaim은
  이 C1 gate의 완료 증거가 아니다.
- 영속 세션 호스트 CR4c C2 publication gate: `zig build test-session-host-cr4c-c2`. C1을 상속하고 Debug·ReleaseFast마다
  actual manifest/socket backend 행 2개, publication proof-loss subprocess 1개, actual socket forced-resize matrix 1개,
  mutation reopen 행 1개와 source boundary 1개를
  exact-count한다. success는 현재 local
  viewport forced resize 뒤 RemoteGeneration+stable screen을 게시하고 reconnect executor와 mutation owner를 같은 새 shell
  generation으로 전진시킨 다음 controller evidence, retiring RemoteGeneration, retired Client 순서로 정산하며 public input을
  새 generation에서 실제 전송한다. expired resize는 candidate/controller stage를 소비하고 새 Client를 fail-close하되 이미
  봉인된 mutation digest와 unavailable shell을 보존한다. umbrella `zig build test-session-host-cr4c`는 이 gate를 상속한다.
  actual socket matrix는 success/stale/wrong-size/EOF/OOM과 exact poison provenance를 구분한다. publication suffix의
  controller-generation drift는 실제 host job subprocess에서 common proof-loss exit 86으로 끝나고, 자식 process group과
  manifest/socket artifact를 부모가 유계 정산한다. 이 gate가 green이어도 실제 AppKit 수동 사용과 다중 runtime/Window는
  각각 CR6/CR5 전까지 완료로 세지 않는다.
- 영속 세션 호스트 CR5a multi-runtime ledger contract gate: `zig build test-session-host-cr5a`. CR4c를 상속하고
  Debug·ReleaseFast마다 pointer-free runtime-set contract 4개와 source boundary 1개를 exact-count한다. 같은 host job의
  canonical row는 runtime handle 순으로 정렬되고 handle/runtime address/runtime ID 중복, 4,096 cap+1, zero identity, raw enum 손상과 illegal
  ledger/local/mutation 조합을 mutation 전에 거부한다. set과 summary는
  `{job_generation,host_id,pool_membership_generation,expected_connection_generation}`을 함께 결속한다. terminal summary는 `published_old=0`과
  `published_new + frozen_unavailable + ended = total`, `retry_reserved = frozen_unavailable`을 checked arithmetic으로 증명하며,
  입력은 `new_controller_evidenced + published_new + open`의 conjunction에서만 열린다. 이 gate는 CR5 host job이 소비할
  값 계약을 먼저 고정하는 prerequisite이며 제품 caller는 0이다. 실제 `HostReconnectJob` runtime-set owner, 2 Window·다중
  runtime publication, k번째 실패 forward resolution과 Window move/close 경쟁은 후속 CR5 제품 gate 전까지 완료로 세지 않는다.
- 영속 세션 호스트 CR5b-1 runtime-set product owner gate: `zig build test-session-host-cr5b1`. CR5a를 상속하고
  Debug·ReleaseFast에서 backend-owned final-address runtime-set 제품 행 2개와 source boundary 1개를 exact-count한다. actual connect가
  시작되기 전에 같은 host의 runtime만 handle 순으로 봉인하고 sibling host를 제외하며, empty/OOM/copy/replay와 runtime
  membership·address·generation·runtime-id drift는 Client publication 전 mutation 0으로 거부해야 한다. 이 gate의 green은
  runtime-set capture 제품 배선을 뜻하지만 runtime별 observer/takeover/publication, k번째 실패 forward resolution,
  Window move/close 경쟁이나 CR5 완료를 뜻하지 않는다.
- 영속 세션 호스트 CR5b-2a host-wide prepublication preparation gate: `zig build test-session-host-cr5b2a`. CR5b-1을
  상속하고 Debug·ReleaseFast에서 3-runtime 제품 행 1개와 source boundary 1개를 exact-count한다. attachment/stable-screen
  prepared authority를 전부 먼저 봉인하고, k번째
  Busy·corrupt·identity drift는 앞선 prepared row만 취소하고 runtime·screen·shared Client·ledger mutation 0이어야 한다.
  이 gate는 shared Client retirement/replacement, observer candidate, takeover/publication을 실행하지 않는다.
- 영속 세션 호스트 CR5b-2b shared replacement gate: `zig build test-session-host-cr5b2b`. 2a를 상속하고 모든 runtime의
  unavailable no-fail suffix 뒤에만 shared old Client cleanup과 same-adapter replacement를 각각 exact 1 수행한다. runtime별
  Client replacement 반복과 sibling attachment가 남은 상태의 Client deinit은 0이다. replacement node allocation·identity 발급은
  suffix 전 final-address reserved preparation에서 끝내고, 준비 실패는 fresh/old Client와 전 runtime mutation 0, commit suffix의
  allocation/callback 0을 검증한다. Debug·ReleaseFast 각각 reserved-node/OOM·abort 제품형 행 1개,
  three-runtime shared-publication 제품 행 1개, source boundary 1개를 exact-count한다.
- 영속 세션 호스트 CR5b-2c runtime transaction gate: `zig build test-session-host-cr5b2c`. 2b를 상속하고 동일 published
  replacement receipt에서 각 runtime의 CR4 observer/takeover/publication을 전진시킨다. 3-runtime success와 k번째
  `authority_conflict`에서
  rollback 0, finite forward resolution, `published_old=0` terminal summary와 final resource zero를 확인한다. 각 최적화
  모드에서 allocation-free cursor 계약 3개, 실제 manifest/daemon/socket을 지나는 three-runtime success 및 k=1/2/3
  usable-connection conflict
  제품 행 2개, source boundary 1개를 exact-count한다. success는 각 runtime의 retiring generation만 먼저 정산하고 마지막
  terminal summary에서 shared retired Client를 exact once 회수한다. failure는 앞선 published generation을 보존하고 retry가
  필요한 frozen suffix가 참조하는 shared retired Client/receipt를 terminal job에 유지했다가 backend 정산에서 remote-first로
  회수한다. 첫 success 뒤 shared Client terminal failure는 앞선 행을 `published_new/open`으로 잘못 봉인하지 않고 terminal
  summary 게시 0으로 거부한다. 그 all-row 전이는 바로 다음 CR5c gate가 소유한다.
- 영속 세션 호스트 CR5c host-wide terminal failure gate: `zig build test-session-host-cr5c`. CR5b-2c를 상속하고 첫 runtime
  publication 뒤 shared Client가 terminal이 된 실제 manifest/daemon/socket 행에서 이미 게시된 generation까지 전부
  `frozen_unavailable/closed`로 전환한다. 게시됐던 행은 `new_controller_evidenced` provenance를 유지하고 untouched suffix는
  `old_valid`로 남기되 input은 전 행에서 닫힌다. 전 published row의 retirement를 먼저 준비한 뒤 allocation/callback 없는
  no-fail unavailable suffix와 `published_new=0`, `retry_reserved=total` terminal summary를 검증한다. 두 번째 runtime
  lookup을 의도적으로 실패시킨 preflight 행은 첫 번째 prepared owner를 abort하고 job/row를 mutation 0으로 보존한 뒤 clean retry가
  성공함을 고정한다. retry에 필요한 terminal
  runtime/retired Client/replacement receipt는 job에 계속 결속하고 backend teardown에서 runtime-first로 정산한다. Debug·ReleaseFast마다 cursor 계약 1개, actual socket
  제품 행 1개, source boundary 1개를 exact-count한다. 2 Window move/close와 Take Control action 경쟁은 다음 CR5 Window gate다.
- 영속 세션 호스트 CR5d-1 Window transaction prerequisite gate: `zig build test-session-host-cr5d1`. CR5c를 상속하고
  Debug·ReleaseFast에서 terminal summary와 두 Window binding을 final-address owner/transaction으로 봉인하는 계약 3개와 source
  boundary 1개를 exact-count한다. canonical success는 handle 정렬·두 Window·full binding identity와 at-1 action admit을,
  hostile 표는 copy/move/replay, active/spent double-click, exact/+1 expiry-spent, Window/app-session/graph/runtime/surface/action generation 및
  terminal summary drift의 mutation 0을 검증한다. 제품 caller는 0이며 실제 AppSession topology, takeover/terminate wire,
  `termination_unconfirmed→abandoned_to_inventory`는 CR5d-2 전까지 완료로 세지 않는다.
- 영속 세션 호스트 CR5d-2 AppSession Window wiring gate: `zig build test-session-host-cr5d2`. CR5d-1을 상속하고
  backend terminal job의 canonical runtime row와 실제 두 AppSession Term binding으로 transaction을 준비한다. 기존
  `moveWorkspaceToSession` 뒤 stale action은 양 Window topology·close reducer·takeover/terminate wire mutation 0으로
  거부하고, close success는 typed `abandon_to_inventory` projection과 transaction을 함께 preflight한 뒤 one-shot consume,
  `termination_unconfirmed→abandoned_to_inventory`, terminate 없는 local detach/topology 제거 순서로 실행한다.
  Debug·ReleaseFast마다 actual AppSession 제품 행과 source boundary를 exact-count하며 double-click/replay, exact/+1 expiry,
  sibling Window/runtime와 같은 Window의 다른 host Term 보존을 포함한다. 이 gate는 CR5 Window 경쟁을 닫지만 real
  AppKit/IME/clipboard/soak CR6 완료는 아니다.
- 영속 세션 호스트 CR6a-1 Recovered Sessions derived projection gate: `zig build test-session-host-cr6a1`. CR5d-2를
  상속하고 Debug·ReleaseFast에서 app-global inert projection owner 4행(다중 live host 앞의 unreferenced dead-entry residue 포함), AppSession 제품 seam 1행과 source boundary 1행을 실행한다. 전체 Workspace
  binding+complete inventory reconcile, typed system rows, primary/opt-out/quick gating, transactional refresh를 검증하며
  launch collector/sidebar/adopt 제품 완료는 주장하지 않는다.
- 영속 세션 호스트 CR6a-2 launch collector/primary sidebar gate: `zig build test-session-host-cr6a2`. CR6a-1을
  상속하고 Debug·ReleaseFast에서 launch-before-terminal coordinator와 primary-only typed `Recovered Sessions` header/row
  3행(실 daemon/manifest/inventory 제품 경로와 dead/malformed host 보존 포함), ABI null/pointer 경계 1행,
  Swift source order 및 source boundary를 실행한다. secure discovery→canonical HostPool generation snapshot→별도
  ephemeral inventory→transactional projection→sidebar→restore/default-surface 순서와 secondary/quick 중복 0,
  rename/drag/drop/close/adopt/spawn/terminate/checkpoint 0을 고정한다. 이 gate는 row 표시까지이며 실제 click/Enter
  adopt와 fresh authority revalidation은 CR6b 전까지 완료로 세지 않는다.
- 영속 세션 호스트 CR6b explicit one-item adopt gate: `zig build test-session-host-cr6b`. CR6a-2를 상속하며
  Debug·ReleaseFast에서 projection generation·Workspace generation·HostPool adapter generation을 먼저 대조하고,
  fresh `host.info`의 host/upgrade/authority/lifecycle과 fresh `runtime.get`의 exact runtime identity를 검증하는 닫힌
  preflight 3행 및 source boundary를 실행한다. 제품 행은 실제 row click·검색 Enter, 하나의 5초 absolute deadline,
  orphan 새 비고정 탭·primary 검색 action에서 다른 실제 Window의 ended tombstone global manifest ordinal 제자리 교체와
  deferred surface 활성화를 검증한다. stale projection,
  stale manifest slot, fresh missing runtime, attach construction OOM과 peer-alive host stall은 topology/projection mutation 0과
  localized notice를 확인하고, OOM은 기존 host runtime 생존을 재조회하며 stall은 5초 deadline을 phase별로 재생성하지 않고
  bounded 종료한다.
  실제 daemon/manifest/HostPool을 쓰는 success·stale/missing 제품 행만 빠르게 재실행하려면
  `zig build test-session-host-cr6b-product`를 사용한다.
- CR0b runtime 수명 7개는 clean joined/detached와 writer failure 뒤 degraded joined 결과를 구분한다. stopping 이후 clock 실패와 실제 completion poll 오류는 backing을 해제하지 않는 degraded detached로 수렴하며 future AppHost ABI가 오류 provenance를 잃지 않게 한다.
- CR0b daemon bootstrap prerequisite 1개는 실제 `runSessionHost`와 같은 `bootstrapIncidentRuntime` 제품 leaf로 daemon PID·process/service nonce·runtime/service generation·초기 sequence 0과 unpublished joined 정산을 검증한다. 별도 pointer-free fixed-64 bootstrap transcript 계약 1개가 closed GUI/daemon role, zero reserved와 두 child 비교의 scalar 경계를 고정한다. bootstrap 4는 서로 다른 canonical artifact인 전용 GUI child(actual 4: named 1+root/import sentinel 3)와 daemon child(actual 1)를 fresh exec하고, expected role·각 64-byte transcript·EOF·exit 0을 2초 absolute watchdog으로 회수해 서로 다른 PID/process nonce/service nonce/app-instance nonce와 양쪽 sequence 0을 검증한다.
- 영속 세션 호스트 2c3d C3-3b2a process-seal prerequisite 집중 gate: `zig build test-session-host-2c3d-c3-3b2a` (neutral process-identity PID SSOT와 process-seal lifecycle, ready-last bootstrap, capability key source cutover, entropy/zero/terminal publication, Linux 실제 PID/fork 거부와 source boundary를 Debug·ReleaseFast로 실행하고 C3-3b1까지의 capability/reader/fork 회귀를 상속한다.)
- 영속 세션 호스트 2c3d C3-3b2b0 exact observation 집중 gate: `zig build test-session-host-2c3d-c3-3b2b0`. b2a gate를 상속하고 `RuntimeObservation.replace`의 0/1/near-cap exact capacity, canonical empty, progress 보존, allocation fail-index 원자성과 source boundary를 Debug·ReleaseFast로 실행한다. session-host event lifecycle과 제품 caller는 변경하지 않는다.
- 영속 세션 호스트 2c3d C3-3b2b1 trusted preparation seal 집중 gate: `zig build test-session-host-2c3d-c3-3b2b1`. b2b0을 상속하고 기존 trusted mirror의 take-time capability·canonical binding digest, package-internal instantaneous projection, named cleanup graph와 stateless typed transcript/progress MAC을 Debug·ReleaseFast 및 source-boundary로 검증한다. neutral 9개·service 8개·projection 1개·boundary 1개를 각 최적화 모드에서 exact-count로 실행한다. public EventView delta, correlation value/raw accessor, generic MAC·permit registry, allocation, `RemoteRuntime` field와 normal product caller는 모두 0이다.
- 영속 세션 호스트 2c3d C3-3b2b2 pure preparation recipe gate: `zig build test-session-host-2c3d-c3-3b2b2`. b2b1을 상속하고 pointer/slice/allocator/owned storage 0인 `EventPreparationRecipe`, allocation-free metadata recipe/size/fill, 기존 classifier 호환 adapter와 실제 `RemoteRuntime` 오류 출처 mapping을 Debug·ReleaseFast exact 17개로 검증한다. staged product caller와 `RemoteRuntime` field는 0이다.
- 영속 세션 호스트 2c3d C3-3b2b3 immutable owner preparation gate: `zig build test-session-host-2c3d-c3-3b2b3`. b2b2를 상속하고 final-address pending owner, canonical metadata real-take의 OOM 0..4+최초 성공 5, 세 preflight 거부, 4-part/3-part budget, reverse cleanup과 proof-loss/abort/actual-callback subprocess를 Debug·ReleaseFast로 검증한다. 최적화 모드당 unique 39개와 canonical adapter fresh replay 1개, 즉 actual 40회를 실행한다. 전체 `test-session-host`와 기본 `zig build test`의 core/exe aggregate는 이미 process seal을 초기화한 process에서 이 callback probe를 중복 실행하지 않고, 같은 exact test 하나만 compile한 fresh filtered artifact가 child의 fail-stop exit 86을 검증한다. probe 선택은 mutable environment marker가 아니라 compile-time test count와 runner의 `--maru-expect-tests=1` 검사를 함께 사용한다. umbrella `zig build test-session-host-2c3d-c3-3b2b`는 b2b0~b2b3 전체를 상속한다. normal product pump caller 0과 b4 전 product activation 미증명은 그대로 유지한다.
- 영속 세션 호스트 2c3d C3-3b3 atomic settlement gate: `zig build test-session-host-2c3d-c3-3b3`. b2b umbrella를 상속하고 final-address settlement lease `prepared -> admitted`, original owner+lease 없는 binding 단독 권위 0, Pending owner `prepared -> settling`과 sealed disposition, exact pending receipt 기반 registry release, closed ClientSlot action/evidence, same-owner Busy 3회 mutation 0 뒤 같은 attempt 성공, callback/fork/ABA/proof-loss를 Debug·ReleaseFast와 source-boundary로 검증한다. 최적화 모드당 unique component 36개와 product-prepared coordinator fresh replay 1회, fresh subprocess 5개, boundary 1개를 exact-count한다. subprocess의 pre-admission 행은 닫힌 argv 부정 matrix exact 11개와 canonical fd 198 non-FIFO 행도 같은 child artifact에서 실행하며 Client effect POST callback drift도 common proof-loss leaf로 수렴시킨다. watchdog 행은 marker 전 무응답, prefix를 쓴 채 열린 pipe hang, marker pipe EOF 뒤 child 생존, 정확히 16-byte를 쓴 뒤 정상 EOF, 16-byte cap 뒤 trailing marker, signal 종료를 실제 child와 typed observation으로 구분한다. deadline 행은 exact `SIGKILL` wait status까지 확인한다. b3 allocation callsite와 normal product pump, semantic publication/cleanup은 b4 전까지 0이다.
- 영속 세션 호스트 2c3d C3-3b5 common close progress gate: `zig build test-session-host-2c3d-c3-3b5`. b3 gate를 상속하고 기존 VTable 메서드 수를 유지한 `CloseProgress`·`RemoveProgress`, dormant Pending lifecycle readiness, final-address `RemoteRuntime.CloseAuthority`, backend-global 4,096 runtime cap, pointer-free 256 KiB 이하 scan scratch, frozen-max/cursor 기반 tick당 최대 16개 fair sweep, stale receipt relookup과 `CloseOperationPin`, final-address window close graph, in-process/AppSession 동기 close parity를 Debug·ReleaseFast와 source-boundary로 검증한다. inventory는 최적화 모드당 neutral contract 6개, lifecycle readiness 6개, close authority 8개, close sweep 8개, remote backend 8개, close graph 2개, AppSession parity 4개인 unique component 42개와 boundary 1개를 exact-count한다. b4가 실제 `event_pending` pump를 활성화하기 전이므로 generation pending close 제품 E2E와 semantic commit은 이 gate에서 완료로 주장하지 않는다.
- 영속 세션 호스트 2c3d C3-3b4 product semantic commit/pump gate: `zig build test-session-host-2c3d-c3-3b4`. b5 gate를 상속하고 중립 pump 계약 6개, Pending semantic commit 9개, 실제 Runtime event kind 9개, pump·round-robin 8개, async close parity 4개인 unique component 36개와 fresh proof-loss subprocess 3개, boundary 1개를 Debug·ReleaseFast로 exact-count한다. 실제 generation pump만 prepare→settle→semantic commit을 호출하며 Busy는 error나 ended가 아니라 다음 tick의 `event_pending`으로 보존한다.
- 영속 세션 호스트 2c3d C3-3b6 app-quit/current+이전 wire 회귀 shutdown gate: `zig build test-session-host-2c3d-c3-3b6`. b4 gate를 상속하고 중립 계약 8개, attempt/outcome 권위 10개, 종료 manifest snapshot 2개, app-quit transaction owner 2개, current admin connector 3개, current-host admin 9개, 이전 wire profile 7개, 진단 sink/bridge 8개, AppSession app-quit 8개인 unique component 57개와 actual-socket product replay 4개, fresh proof-loss subprocess 3개, boundary 1개를 Debug·ReleaseFast로 exact-count한다. target당 terminate 3회와 전역 15초 deadline, current barrier/inventory, 이전 wire ambiguous at-most-once, redacted fixed-64 진단, fd-only terminalization→owner zero→graph-last destroy와 host EOF 재접속을 실제 제품 경로로 검증한다. 보존한 universal ad-hoc 바이너리는 배포 호환성이나 공개 릴리스 증거가 아니라 C3-3b5 base+patch의 회귀 오라클이며, 실제 실행 전에 artifact와 patch SHA-256을 모두 확인한다.
  - frozen N-1 binary는 격리 override 도입 전 산출물이라 기본 실행에서는 실제 프로세스 행을 skip한다. 로그인한 GUI 사용자의 UID와 다른 전용 OS 계정인 일회용 runner에서만 `MARU_SESSION_HOST_EMPTY_SHARED_FIXTURE=ephemeral-runner-v1`을 명시한다. 로그인 사용자의 UID에서는 marker가 있어도 `SharedUserNamespace`로 실행 전에 실패하고, 전용 계정에서도 `/tmp/maru-<uid>`가 이미 존재하면 실패하며 기존 내용을 읽거나 정리하지 않는다. current 제품 자식 fixture는 이 예외를 쓰지 않고 launcher가 받은 격리 root를 반드시 상속한다. 공용 test runner는 상속된 `MARU_SESSION_HOST_ROOT`를 신뢰하지 않고 PID별 root로 항상 덮어쓰며, 테스트 자식 앱의 workspace checkpoint도 실제 사용자 저장소로 새지 않도록 `CFFIXED_USER_HOME`을 같은 PID별 격리 home으로 덮어쓴다.
  - **실제 `.app`/제품 실행파일을 띄우는 스모크도 같은 규율을 따른다.** `HOME`이나 `CFFIXED_USER_HOME`만 임시 위치로 바꾸는 것은 workspace만 격리할 뿐이다. non-test 제품 자식은 그대로 `/tmp/maru-<uid>`를 선택하므로 실제 앱의 manifest·owner lock·socket을 열거나 정리할 수 있다. 따라서 모든 제품 자식 fixture는 실행 전에 `HOME`·`CFFIXED_USER_HOME`을 fixture home으로 묶고 `MARU_SESSION_HOST_ROOT`를 그 fixture만 소유하는 짧은 `/tmp` 절대 root로 묶는다. 저장소 아래의 긴 home을 socket root로 재사용하면 macOS `sun_path` 상한을 넘을 수 있으므로 두 경로는 같을 필요가 없고, **둘 다 실제 사용자 경로와 분리돼야 한다.** 한 하네스가 둘 이상의 독립 앱 fixture를 띄우면 각 자식의 세 값을 함께 다시 지정한다. 수동 `macos-app` 실행은 테스트가 아니므로 이 규칙의 대상이 아니다.
- 영속 세션 호스트 2c3d C3-3c product socket/source-zero gate: `zig build test-session-host-2c3d-c3-3c`. b6 gate를 상속하고 열린 peer의 revoked·unknown·semantic failure actual socket component 3개, 봉인된 unknown의 ended 빠른 판별 회귀 1개, repo-wide raw `Client` event/effect source boundary 1개를 Debug·ReleaseFast로 exact-count한다. 세 제품 행은 wire read→generation take→prepare→settle→semantic commit을 통과하며 Pending/EventOwner/correlation/mirror/event queue가 source-zero이고 blocker/pin/quarantine이 event 전 live attachment 기준선으로 복귀하며 payload callback이 exact 1회인지 검증한다.
- 영속 세션 호스트 2c3e C1 scoped decoder gate: `zig build test-session-host-2c3e-c1`. Debug·ReleaseFast마다 neutral contract 4개, scoped owner 8개, actual-socket replay 3개, fresh proof-loss subprocess 3개, boundary 1개를 exact-count한다. typed request의 accepted inline response만 lexical decoder callback에 exact 1회 빌리고, callback 복귀 뒤 response source-zero와 reusable rearm 또는 protocol-failure terminal을 완료한다. C1의 제품 caller는 0이며 bound RPC family 전환은 C2, immediate EOF·unread RX-first parity는 C3가 소유한다.
- 영속 세션 호스트 2c3e C2 bound RPC family gate: `zig build test-session-host-2c3e-c2`. C1 gate를 상속하고 Debug·ReleaseFast마다 실제 `RemoteRuntime` 제품 RPC family 12개와 boundary 1개를 exact-count한다. resize, observation, selected text, link at, clipboard write, find, select op, core command, mouse report, notification, terminate, detach가 각각 typed `RuntimeRequest`와 C1 decoder owner를 통과하며, generation arm의 raw `callOrdered`·`Client.call` 직접 호출은 0이다. C1 bridge의 제품 caller는 attachment-owned seam 하나로 고정한다. immediate EOF·unread RX-first 및 legacy/generation cadence parity는 C3가 소유한다.
- 영속 세션 호스트 2c3e C3 socket cadence gate: `zig build test-session-host-2c3e-c3`. C2 gate를 상속하고 Debug·ReleaseFast마다 legacy/generation 공통 actual-socket cadence 12개와 source boundary 1개를 exact-count한다. immediate EOF와 완성 response+EOF, partial header/payload+EOF, response 전후 revoke/metadata/snapshot, malformed/unknown-correlation, unread revoke와 queued TX의 RX-first가 모두 제품 fixture로 green이며 boundary가 RX→settlement→TX와 decoder owner를 고정한다.
- 영속 세션 호스트 2c4 RuntimeConnection gate: `zig build test-session-host-2c4`. 2c3e C3를 상속하고 Debug·ReleaseFast에서 two-arm mode SSOT와 legacy/generation 제품 parity component, semantic facade exact 15 및 source boundary 1개를 검증한다. `RemoteRuntime.client`/nullable adapter 병렬 필드와 generation raw Client fallback은 0이어야 한다.
- 영속 세션 호스트 2d1 generation release gate: `zig build test-session-host-2d1`. 2c4를 상속하고 Debug·ReleaseFast에서 registry 결과·permit 4개, ClientSlot completed/retryable/strict-token 3개, 실제 GenerationAttachment retry→teardown 1개와 boundary 1개를 검증한다. unique component는 최적화 모드당 exact 8개이며 첫 retryable 동안 target free와 신규 sibling read/release는 0, teardown fresh permit 뒤 target free는 exact 1이어야 한다.
- 영속 세션 호스트 2d2 terminal handoff gate: `zig build test-session-host-2d2`. 2d1을 상속하고 Debug·ReleaseFast에서 neutral handoff 3개, registry aggregate 4개, RemoteAttachment trigger/tombstone 3개, ClientSlot typed teardown 3개, 실제 GenerationAttachment terminal handoff 1개와 boundary 1개를 검증한다. unique component는 최적화 모드당 exact 14개다.
- 영속 세션 호스트 2d3 callback/proof-loss gate: `zig build test-session-host-2d3`. 2d2를 상속하고 Debug·ReleaseFast에서 unique component 12개, stage별 전용 fresh-exec subprocess 3개, boundary 1개를 실행한다. pre-callback proof loss는 free 0, post-callback proof loss는 target free exact 1 뒤 공통 `fatalIntegrity(.proof_loss)` exit 86, callback reentry는 같은 Client mutation `Busy`와 독립 Client read 허용을 exact transcript로 검증한다.
- 영속 세션 호스트 CR3a-2e actual attach parity gate: `zig build test-session-host-2e`. 2d3을 상속하고 generation attach의 binding·cleanup row·pin·batch adapter가 wire 전에 준비되는지, accepted stream이 allocation 없는 suffix로 exact once 결속되는지 검증한다. 최종 gate는 Debug·ReleaseFast마다 준비 계약 4개, actual socket 6개, rollback 4개, boundary 1개를 exact-count한다.

### `/tmp` 픽스처 잔재를 거둔다 (로컬 위생)

session host 픽스처는 `/tmp/maru-<이름>-<pid>` 를 만들고 그 안에 daemon 을 띄운다. daemon 이 owner lock·
`incidents/`·manifest·소켓을 남기므로 **`rmdir` 로는 못 지우고** 그 자리가 실행마다 쌓인다.

```sh
mise run clean-tmp-fixtures            # 주인이 죽은 자리만 거둔다
sh tools/clean-tmp-fixtures.sh --dry-run   # 지우기 전에 세어 본다
```

**왜 신경 쓰는가 — 디스크가 아니라 pid 재사용이다.** 이름이 pid 라, 같은 번호를 받은 다음 실행이 «이미
채워진» 디렉터리에서 시작한다. 죽은 manifest 가 «살아 있는 host» 로 읽히거나 stale 소켓·lock 이 정리
사슬을 다른 길로 보내고, 증상은 **코드와 무관해 보이는 간헐 실패**다(진짜 결함을 flake 로 넘기거나 그
반대를 하기 쉽다).

⚠️ **`/tmp/maru-<uid>`(예: `maru-501`)는 실 세션 host 의 루트다.** 그 스크립트는 이름이 숫자뿐인 자리와
pid 를 읽을 수 없는 자리를 **건드리지 않는다** — 지우면 사용자의 살아 있는 keep-alive 세션이 통째로
사라진다. 살아 있는 pid 의 자리도 남긴다(지금 돌고 있는 테스트일 수 있다).

CI 러너는 매번 새 머신이라 이 명령이 필요 없다 — 개발 머신용이다. 실측(2026-08-25): 이 저장소를 개발하던
머신에서 21,474 개를 거뒀다.

## 완료 전 확인

코드나 빌드 설정을 바꾼 PR은 기본적으로 다음을 통과해야 한다.

```sh
mise run check
git diff --check
```

문서만 바꾼 PR은 `git diff --check`를 최소 검증으로 사용한다. 다만 문서가 명령, 구조, 테스트 경로를 바꾸면 관련 명령도 함께 실행한다.

### 다른 OS의 컴파일을 로컬에서 확인한다 (크로스 컴파일)

**호스트가 아닌 OS에서만 깨지는 컴파일 오류가 있다.** 대표적으로 Zig의 **추론 error set**은 OS별로 달라진다 —
어떤 오류를 내는 코드가 한 OS에서만 살아 있으면 그 오류는 그 OS의 error set에만 들어가고, 다른 OS에서
`switch`하면 *"not a member of destination error set"*으로 **컴파일이 깨진다**. 실제로 그렇게 한 번 깨졌고
(W2 적대적 검증), 그때는 CI가 잡았다.

```sh
zig build -Dtarget=aarch64-macos        # exe + 중립 레이어의 macOS 컴파일
zig build -Dtarget=x86_64-linux         # 같은 것을 Linux로
zig build test -Dtarget=aarch64-macos   # **macOS 플랫폼 코드(L4)까지** 컴파일 — 아래
```

**셋째 줄이 중요하다.** `-Dtarget=aarch64-macos`는 build.zig의 macOS 전용 테스트 스텝을 **켜므로**
`src/platform/macos/**`가 컴파일 대상에 들어온다. 앞의 두 줄은 그러지 않는다 — exe 그래프에 macOS 소스가
아예 없어서, L4만 건드린 변경은 **통과해도 아무것도 검증하지 않는다**.

끝에 가면 `error: unable to find framework 'Foundation'`으로 **링크가 실패한다**. 그건 정상이고 무시한다 —
**컴파일 오류는 링크보다 먼저 나오므로 그 위에 다 찍힌다.** 그래서 이렇게 걸러 본다:

```sh
zig build test -Dtarget=aarch64-macos 2>&1 | grep -E '\.zig:[0-9]+:[0-9]+: error'
```

이 검사가 실제로 그 부류를 잡는지 확인했다 — 구조체 필드 이름을 일부러 옛것으로 되돌리자
`no field named 'zdotdir' in struct 'server.RuntimeSpawnParams'`가 그대로 나왔고, 그 자리는 CI가 아니면
못 봤을 자리였다(실제로 그렇게 놓친 자리를 하나 잡았다).

**한계 — 실행이 아니라 컴파일이다.** macOS 테스트의 실제 실행과 링크는 여전히 CI 몫이다.

L4(플랫폼 어댑터)나 OS 갈래(`builtin.os.tag` 분기, 호스트별 스텁, 중립 계약 필드 rename)를 건드린 PR은
**CI에 올리기 전에** 위 세 줄을 돌린다. 중립 레이어(L1~L3)만 바꾼 PR은 `mise run check`로 충분하다.
