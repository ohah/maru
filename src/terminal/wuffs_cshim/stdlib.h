// wuffs 셰임 — README.md 참조. wuffs 는 이 헤더에서 크기 타입만 필요로 한다.
// `calloc`/`free` 는 일부러 선언하지 않는다(png_wuffs.c 가 자기 스텁으로 바꿔친다).
#ifndef MARU_WUFFS_CSHIM_STDLIB_H
#define MARU_WUFFS_CSHIM_STDLIB_H
#include <stddef.h>
#endif
