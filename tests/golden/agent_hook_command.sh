IFS= read -r mh_p || :; while IFS= read -r mh_x; do :; done; [ -n "$MARU_PANE_ID" ] || exit 0; if [ ${#mh_p} -gt 32768 ]; then mh_p='{"hook_event_name":"__oversized__"}'; fi; { printf 'claude\t%s\n' "$mh_p" >> '__LOG_DIR__'"/$MARU_PANE_ID.ndjson"; } 2>/dev/null; exit 0 # MARU_HOOK_V3 managed by maru: added and removed automatically, do not copy
